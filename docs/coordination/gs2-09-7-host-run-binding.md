# GS2-09.7 protected host run-binding source handoff

Status: **source-only, disabled**. The reviewed `.github` #3690 mint workflow is an unadmitted source-delivery observation, and Coordination [#558](https://github.com/FS-GG/FS.GG.Coordination/pull/558) is a draft verifier scaffold at `8b519bf1ba417b36fcf7941763645eee6d67ec08`. Neither supplies an admitted GS2-09.7 migration run. This branch adds no workflow step and does not receive a signing credential.

The additional workflow revision admission control is recorded in
`gs2-09-7-protected-revision-admission.md`. The source placeholder is empty;
an independent protected authority must supply the admitted commit SHA.

`scripts/gs2-09-7-host-run-binding.py` is the protected-host counterpart to #558's envelope schema. From protected runner facts it checks the repository, exact workflow path/ref/SHA, run ID and attempt, candidate SHA and derived nonce. It checks the retained native preflight for the registered App actor, private sandbox repository and Project 2; it checks the selected-repository mint proof, effective grants, expiry and token hash. It signs canonical, domain-separated JSON with RSA-PSS/SHA-256 only after its signer key's SPKI digest matches a source-pinned fingerprint. The envelope binds the raw proof digest, token digest, target IDs, workflow and run facts. It contains neither token nor key. Inputs reject duplicate JSON members and symlinks; a missing pin, key FD, proof or preflight refuses. The committed pin is empty, so the command refuses before reading any credential.

## Exact installation prerequisites

1. Independently admit the protected #3690 source route and review an exact workflow revision and Coordination candidate. The host checkout must still be pinned to `github.sha` and verified before App secret use. The current Q4 command is not an installed nine-authority migration interpreter.
2. Provision a protected RSA signer through an inherited file descriptor, with no PEM in argv, candidate environment, logs or artifacts. If the existing App private key is reused, explicitly admit this separate signing purpose and its rotation plan. Derive and review its SPKI SHA-256, then pin that exact digest in this host source and pin the matching public key/digest in the reviewed Coordination candidate. A candidate-supplied pin is not a trust anchor. Any key rotation requires new reviewed exact heads.
3. In the **protected** workflow, after mint and native actor/repository/Project preflight and before either candidate execute or cleanup step, invoke the host utility from the verified host checkout. Supply `GITHUB_REPOSITORY`, `GITHUB_REF`, `GITHUB_WORKFLOW_REF`, `GITHUB_SHA`, run ID/attempt, the exact candidate input and derived nonce, the scoped token, evidence directory and signer FD. Retain `run-binding.json` and its SHA-256. Verify the envelope and signed facts on the host before releasing the token to candidate code; separately require the candidate verifier from #558 before its first provider call. The currently installed workflow does none of this.
4. On a failed host binding after mint, give no token to candidate execute **or cleanup**; revoke the token through the protected host and retain the refusal and revocation verdict. After an effect has started, preserve a separately reviewed recovery and cleanup route so a missing envelope does not turn an uncertain effect into an assumed absence.
5. Keep the later Q5/Q6 migration evidence distinct: complete nine-authority reads, exact manifest and operation population, independent native effect readback, interruption and replay controls, archive verification, five rollback domains, second round, cleanup, and token revocation. This envelope is a provenance binding, not migration qualification or acceptance.

The source-only tests use disposable RSA keys, fake token and preflight data, and no provider calls. The command's missing-pin refusal is an intentional negative control. No key installation, sandbox dispatch, provider write, protected merge, receipt, or cutover is authorized by this document.
