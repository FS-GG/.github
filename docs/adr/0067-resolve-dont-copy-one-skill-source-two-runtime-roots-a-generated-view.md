# ADR-0067: Resolve, don't copy — one skill source of truth, two runtime roots, and a generated view

- **Status:** Accepted
- **Date:** 2026-07-27
- **Affects:** FS-GG/.github, every coordination-kit receiver, FS.GG.SDD, FS.GG.Rendering, FS.GG.Templates, and scaffolded product workspaces
<!-- One `**Amends:**` line PER TARGET, deliberately. `adr-coherence`'s declaration pattern stops at
     the first `.`, and every ADR link carries a `.md` — so a single field naming three records
     registers only the FIRST, and the other two links become invisible to the gate from this end
     (measured on this record; filed as .github#1637). One target per line keeps all three links
     visible from BOTH ends, which is the whole point of house rule 3. -->
- **Amends:** [ADR-0011](0011-agent-skill-roots-full-union-orchestrator-owned-mirror.md) Decision 1 — **in DIRECTION only;** it stays in force and this record retires nothing it mandates. See §9.
- **Amends:** [ADR-0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) Decision 5 — **in DIRECTION only;** the declared root set is unchanged today. Decision 6 is untouched and re-affirmed. See §6, §9.
- **Amends:** [ADR-0065](0065-one-agent-skill-root-contract.md)'s root set — **§5 is EXECUTED as of 2026-07-28 ([#1636](https://github.com/FS-GG/.github/issues/1636)): the ordered set is now TWO, `.claude/skills` + `.agents/skills`, and ADR-0065 was amended in that same change.** Its transport contract still governs unchanged; ADR-0065 §Retiring a root now states how a root leaves the set without violating it. §6's generated view is NOT landed. See §5, §9.
- **Applies:** [ADR-0058](0058-adopt-one-governing-principle-derive-dont-restate.md) — *derive, don't restate* — to files rather than to facts.

## Context

Filed and decided on [`#1611`](https://github.com/FS-GG/.github/issues/1611); the measurement, the
per-item attribution and the maintainer's decision comment are there and are not restated here.

The short form: almost none of 2026-07-27's ~50 completed items were product work. They were
generated by one answer to *"how do N repos share M artifacts and stay coherent?"* — **copy, then
police**. Copying produced the repairs; policing produced the gates that verify against a moving
tip, and the gates that cannot see their own subject. The maintainer's decision (`#1611`,
2026-07-27) was to adopt **resolve-don't-copy** as the intended architecture and to plan a rewrite
of the materialize/verify apparatus, **not** an incremental erosion of it.

That decision left one sub-question open, and it was load-bearing: *does some runtime force
materialization because it cannot be told where to look?* Phase 1
([`#1621`](https://github.com/FS-GG/.github/issues/1621)) answered it by experiment on Claude Code
2.1.220 and Codex CLI 0.145.0, and the evidence — the per-runtime capability table, the end-to-end
demonstration on FS.GG.Net (69 tracked skill files → 24 objects, both runtimes resolving,
`skill-union-assert` passing through its existing `--roots` seam), and the fixture inventory — is
recorded on that issue. Three of its findings changed the shape of the answer, and are decided
below as §5, §6 and §8.

## Decision

**§1 — Resolve, don't copy.** Shared agent skills have **one source of truth**, and a runtime's
skill root is a *view* of it, not a *receipt* of it. Nothing that can be derived from that source is
committed a second time. (`#1611` Q1: **yes**, resolve-don't-copy is the intended end state.)

**§2 — Verify against an immutable pin, never a moving tip.** A gate's verdict MUST be a pure
function of (tree under test, pinned ref). A gate that reads another repository's `main` at check
time has no verdict, because its answer changes with no change to its subject. (`#1611` Q2: **yes**,
this is a stated rule; new gates are held to it. `FS.GG.SDD#724` passed on a merged SHA at 08:15Z
and failed on byte-identical content at 08:21Z.)

**§3 — One implementation per invariant, with a callable seam.** A language boundary is a reason to
expose a CLI, not a reason to reimplement. (`#1611` Q3: **yes**. The canonical skill digest reached
**five** implementations that disagreed on CRLF and share a fail-open on invalid UTF-8 —
[#1585](https://github.com/FS-GG/.github/issues/1585), [#1547](https://github.com/FS-GG/.github/issues/1547),
[#1589](https://github.com/FS-GG/.github/issues/1589).)

**§4 — The multi-repo split is settled and is NOT revisited.** A monorepo was named as the genuinely
radical option and was **not** chosen. The rewrite happens inside the existing repo topology.
(`#1611` Q4: closed as settled.)

**§5 — The end state has TWO runtime roots, not three.** `.agents/skills` is **Codex's second native
root**, not a third runtime's root: phase 1 swept `.github` and all seven receivers and found that
every in-tree reader of `.agents/skills` is an FS-GG *verifier*, never an agent runtime, and
confirmed by experiment that Codex 0.145.0 discovers `.agents/skills` with no configuration at all.
ADR-0065 already stated the native-discovery half; ADR-0011's *"the generic agent convention"*
phrasing is what kept the third root looking like a third consumer. The end state is therefore
`.agents/skills` (canonical, because it needs no pointing) plus `.claude/skills` (Claude's only
project root). `.codex/skills` carries no runtime that the other two do not — and removing it also
removes the duplicate catalog entry Codex measurably emits when a skill appears in both of its
roots, which host configuration currently has to suppress on every developer machine.

> **EXECUTED 2026-07-28 by [#1636](https://github.com/FS-GG/.github/issues/1636),** on the maintainer's
> explicit authorisation of 2026-07-27, ahead of phases 2–4 and depending on none of them. ADR-0065 was
> amended in the same change, as this record's Consequences require. The premise was **re-measured**
> rather than inherited from phase 1: in a three-root tree the same skill appears **twice** in
> `codex debug prompt-input` (from `.codex/skills` and `.agents/skills`, never from `.claude/skills`);
> in a two-root tree it appears **once**, from `.agents/skills`, with no configuration at all. Measured
> benefit: Codex's effective catalog cost on this repo's own driver skills fell **6335 → 3174**
> characters. One finding not anticipated here: the kit materializer could not by itself complete the
> retirement on receivers, because dropping a root from the declared set makes the materializer stop
> looking at that root — so a **retired-root declaration** was required and is now part of the contract
> (ADR-0065 §Retiring a root). §6's generated view is untouched and still unbuilt.

**§6 — The mechanism is a view generated at checkout, never a committed symlink.** Both runtimes
accept an untracked, generated directory and cannot distinguish it from a committed one; neither
consults git. A **committed** symlink, by contrast, does not degrade under
`git -c core.symlinks=false` (the git-for-Windows default without Developer Mode) — it checks out as
a small regular text file, and both runtimes then exit 0 with zero skills and no diagnostic. ADR-0011's
and ADR-0014 Decision 6's rejection of committed symlinks therefore **stands and is re-affirmed**,
now on a measurement rather than an argument. Phase 1's own demonstration used a committed symlink
because it needed no new tooling; that form is Linux-only and is **not** the end state.

**§7 — Do not build a resolver.** Every "point the runtime elsewhere" mechanism works and every one
costs something: session-only scope (`--plugin-dir`), relocating an entire config home including
auth (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`), or a per-machine bootstrap (the local directory
marketplace). Codex's `[[skills.config]]` is a **filter, not an adder** — measured in both
directions. Producing the view at each runtime's expected path is strictly cheaper than all of them.
The right use of "yes, it can be pointed" is *to not need it*.

**§8 — The rewrite MUST ship a loud absence check; it is not optional and it is not deferrable.**
Today a materialization bug turns a gate red. Under resolution, **every** failure mode measured in
phase 1 — absent directory, dangling symlink, Windows text-file symlink — is exit 0, no diagnostic,
in **both** runtimes, and the result is an agent that quietly has no skills. The replacement must
assert, at checkout and in CI, that the skills the manifest declares are visible at each runtime's
expected path, and fail loudly when they are not. Without it the rewrite trades a loud failure for a
silent one, which is `#1611`'s own category-D finding ("a gate that never runs and a gate that
always passes are indistinguishable from outside") re-appearing on the far side of the rewrite.

**§9 — Sequencing: nothing is retired before its replacement is proven.** ADR-0011, ADR-0014 and
ADR-0065 remain **in force**, and no *mechanism* is retired until the phase that lands its replacement
amends them in the same change that proves it. (Their root set was **three**; §5 narrowed it to two on
2026-07-28 — a root-set narrowing is not a mechanism retirement, it retires nothing that has a
replacement pending, and the Alternatives below call it *separable rather than optional*.) The order is: build the view and the
absence check; run it alongside the existing gates unchanged; **port** the fixtures rather than
re-derive them; retire the old apparatus **per repo**, with the freshness sweep last.

## Consequences

- The apparatus named for eventual replacement — `coordination-sync`, kit materialization,
  `kit-published-coherence`, `coordination-coherence`, `skill-union-assert`, the kit-pin freshness
  sweep, the kit Renovate bump loop — keeps running unchanged, and keeps being repaired, until §9's
  order reaches it. This record is not authority to delete any of them.
- `skill-union-assert` needs **no code change** to run alongside the resolved layout: its existing
  `--roots` / `.agent-skill-roots` override — which ADR-0065 already provides for as a reviewed
  exception — accepts a two-root tree, measured green on phase 1's demonstration.
- §5 is a **contract migration** with a package-publication and receiver-re-materialization tail:
  the root set is a declared constant consumed by `Fsgg.Schemas.agentSkillRoots`, `coordination-sync`,
  `FS.GG.Kit`, `.agent-skill-roots`, `KitDigest.fs` and the composition gate, and ADR-0065's transport
  contract **forbids** deleting a mirror to hide a duplicate. Executing §5 therefore requires amending
  ADR-0065 in the change that lands it — this record decides the direction, not the flip.
  **DONE 2026-07-28 (#1636):** all six migrated in one change, ADR-0065 amended in it, and the
  transport contract kept rather than bent — see ADR-0065 §Retiring a root.
- §2 and §3 bind **now**, independently of the rewrite. They are the rules
  [#1584](https://github.com/FS-GG/.github/issues/1584) and
  [#1585](https://github.com/FS-GG/.github/issues/1585) are the first instances of, and a new gate
  that reads a remote tip at check time is refusable on this record alone.
- The fixtures do **not** all retire at the same rate, and phase 1 measured which: the
  *coherent-but-incomplete* class ([#1504](https://github.com/FS-GG/.github/issues/1504)) genuinely
  dissolves — with one source there is no subset to be coherent about — while the
  *two-implementations-disagreeing* class (`#1506`, `#1513`, `#1547`, `#1589`) survives the rewrite
  untouched and is retired by §3 instead, and the *verify-against-a-moving-target* class (`#1549`) is
  orthogonal and owned by §2. Losing the first is cheap; losing either of the others is a straight
  regression.
- ADR-0011's *"the generic agent convention"* framing is now known to be false, and it has been
  copied into consumer documentation. Correcting the copies is board work, not ADR work.
- Sequencing, phase items and who-does-what live on the Coordination board (ADR-0001), which is where
  they will still be right next week.

## Alternatives considered

- **Incremental erosion** — keep copying, and keep hardening the policing apparatus one gate at a
  time. This was the board driver's recommendation on `#1611`, and the maintainer overruled it. It is
  recorded because it remains the fallback if §9's sequencing stalls: the standing rules §2 and §3
  reduce live harm whether or not the rewrite proceeds.
- **A monorepo.** Named on `#1611` as the actual root of most of this cost, and declined — see §4.
- **Committed symlinks** — rejected, twice before and again here on new evidence; see §6.
- **A resolver: point each runtime at a shared directory.** Technically available in four separate
  mechanisms, all measured working, all rejected on cost; see §7.
- **Keeping three roots and only changing the mechanism.** Rejected: it preserves a root with no
  runtime behind it, and preserves the duplicate-catalog suppression that root forces on every
  developer machine. §5's cost is a contract migration; its benefit does not depend on any other part
  of the rewrite, which makes it separable rather than optional.
- **Deferring the absence check to a later phase.** Rejected as the one thing that must not slip; see
  §8. A rewrite that removes the loud failure and adds the quiet one is worse than no rewrite.
