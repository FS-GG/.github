# Architecture Decision Records (cross-repo)

ADRs for decisions that span more than one FS-GG repo. Per-repo decisions live in that repo.
Use [template.md](template.md) for a new record; number sequentially.

**Four house rules**, learned the hard way:

1. **A cross-repo boundary is decided in an ORG ADR** ([ADR-0033](0033-fixed-step-double-buffer-is-a-simulation-primitive.md) §5).
   A repo-local ADR may *execute* such a decision, and must *cite* the org ADR that made it.
   (ADR-0033 exists because one boundary was decided repo-locally and an accumulator ended up split
   across two repos, where the hardened copy was not the one products used.)
2. **This index is NAVIGATION — one line per record.** The reasoning, the corrections, and the
   supersession *scope* live in the record itself, never only in the row. When corrections landed
   here instead of there, the Title column grew into a second copy of the corpus (rows reached 2,700
   characters) while readers who opened the actual record were the ones who got misled.
3. **An amendment has TWO ends.** If A supersedes or amends B, say so in **both** files — B's
   `**Status:**` banner *and* a dated note at the section that actually went stale.
   [ADR-0021](0021-parallel-intra-repo-work-claim-worktree-touchset.md) is the house form.
4. **Withdrawn numbers are retired, not reused.** A withdrawn record keeps a row, so the sequence
   stays gap-free to a reader.

All four are enforced by `adr-coherence` (`scripts/check-adr-coherence.py`) — the corpus is a
registry like any other, and this org gates its registries.

| ADR | Title | Status |
|---|---|---|
| [0001](0001-cross-repo-coordination-via-issues.md) | Cross-repo coordination via GitHub issues + a registry | Accepted |
| [0002](0002-composition-by-scaffold-lifecycle-parameter-governance-populated.md) | Composition by scaffold; `lifecycle` template parameter; governance populated by default | Accepted |
| [0003](0003-rename-fs-skia-ui-version-machinery-to-fs-gg-ui.md) | Rename the `fs-skia-ui` version machinery to `fs-gg-ui` (clean break) | Accepted · *executed* |
| [0004](0004-constitution-ownership-for-lifecycle-sdd-products.md) | SDD owns the `lifecycle=sdd` constitution, shipped at `.fsgg/constitution.md` | Accepted |
| [0005](0005-fsgg-slot-ownership-sdd-project-governance-governance.md) | `.fsgg/` slot ownership — SDD owns `project.yml`, Governance owns `governance.yml` | Accepted |
| [0006](0006-org-shared-dotnet-build-config-and-unified-restore-locked-mode-gate.md) | `.github` owns the org-shared .NET build config; `RestoreLockedMode` gates on `GITHUB_ACTIONS` | Accepted |
| [0007](0007-reference-gate-set-package-version-derivation.md) | `FS.GG.Governance.ReferenceGateSet` version-derivation rule | Superseded |
| [0008](0008-fsgg-sdd-cli-first-class-member-of-coherent-set.md) | The `fsgg-sdd` CLI is a first-class member of the coherent set (orchestrator axis) | Accepted |
| [0009](0009-cli-single-orchestrator-detect-and-remediate.md) | The `fsgg-sdd` CLI is the single orchestrator — detect-and-remediate, not silent auto-update | Accepted |
| ~~0010~~ | *SDD-native scaffold (inline `--provider-source`, explicit currency, config-driven governance default)* — declined ([#100](https://github.com/FS-GG/.github/pull/100)) in favour of a clone-free scaffolder working through existing machinery. Its withdrawal was **completed by [0016](0016-retire-templates-local-new-fullstack-single-scaffolder.md)**, and that scaffolder is now the `new-sdd-workspace` dotnet tool. | **Withdrawn** |
| [0011](0011-agent-skill-roots-full-union-orchestrator-owned-mirror.md) | Every agent-skill root carries the full skill union; `fsgg-sdd` owns the mirror | Accepted |
| [0012](0012-dual-publish-to-nuget-org.md) | Dual-publish FS-GG packages to nuget.org (public) alongside the org GitHub Packages feed | Accepted |
| [0013](0013-trusted-publishing-oidc-for-nuget-org.md) | Publish to nuget.org via Trusted Publishing (OIDC), not a long-lived API key | Accepted |
| [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) | Skill vendoring & mirroring — one manifest, one materialize-and-verify, content-addressed | Accepted |
| [0015](0015-register-the-registry-schema-as-a-governed-contract.md) | Register the registry schema as a governed contract (`registry-schema`) | Accepted |
| [0016](0016-retire-templates-local-new-fullstack-single-scaffolder.md) | Retire the Templates-local `new-fullstack.sh`; one sole scaffolder (now `new-sdd-workspace`) | Accepted |
| [0017](0017-skill-registry-condition-aware-materialization.md) | Org skill registry + condition-aware `materializes-when` on the manifest | Accepted |
| [0018](0018-transient-durable-sdd-artifact-taxonomy.md) | Transient vs durable SDD artifact taxonomy; regenerable output is gitignored by role | Accepted |
| [0019](0019-org-repo-roster-registry-and-coordination-kit.md) | Org repo roster registry (`registry/repos.yml`) + the mirrored coordination kit | Accepted |
| [0020](0020-platform-workspace-component-vocabulary.md) | Name the products precisely — **platform** (the org), **component** (one repo), **workspace** (what you scaffold) | Accepted |
| [0021](0021-parallel-intra-repo-work-claim-worktree-touchset.md) | Parallel intra-repo work — a claim lock, one git worktree per item, a declared `Paths:` touch-set | Accepted |
| [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md) | Extract **FS.GG.Game** as the platform's sixth component (BCL-only `Game.Core`; `Scene.Geometry` Option D) | Accepted |
| [0023](0023-onboard-fs-gg-audio-as-an-sdd-driven-component.md) | Onboard **FS.GG.Audio** as the seventh component — render-independent, depends on no FS-GG component | Accepted |
| [0024](0024-wire-fs-gg-audio-into-the-game-scaffold-profile.md) | Wire FS.GG.Audio into the `game`/`sample-pack` profile on its own `$(FsGgAudioVersion)` axis; complete the extraction | Accepted |
| [0025](0025-first-class-shipped-surface-mutation-event.md) | First-class **shipped-surface mutation** — a changed `.fsi` baseline of a *published* surface is a governed event | Accepted |
| [0026](0026-committed-compact-ship-verdict.md) | **Committed compact ship verdict** — the merge-boundary answer survives in git history | Accepted |
| [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) | The parallel-work lock is keyed on the **worker**, not the account — comment-order CAS, leases, a worker channel | Accepted |
| [0028](0028-keyboard-input-config-mechanism-policy-boundary.md) | Keyboard input-config boundary — **mechanism** (Rendering) vs **policy** (Game); the command id is an opaque token | Accepted |
| [0029](0029-game-owns-the-testspec-corpus.md) | The game TestSpec corpus is FS.GG.Game-owned; `.github` keeps pointer stubs | Accepted |
| [0030](0030-creation-time-scaffolding-self-updates-by-default.md) | Creation-time scaffolding self-updates the CLI by default — a bounded carve-out to 0009 | Accepted |
| [0031](0031-republished-package-is-a-named-failure.md) | ~~A silently re-published package is a NAMED failure~~ — **premise false**: `FSharp.Core` was never re-published. Surviving decisions folded into [0032](0032-the-lock-hash-must-not-depend-on-the-machine.md) §5 (cold restore) and §4 (never hand-write a `contentHash`). | **Withdrawn** |
| [0032](0032-the-lock-hash-must-not-depend-on-the-machine.md) | The lock file's `contentHash` must not depend on the machine — `FSharp.Core` resolves from nuget.org everywhere | Accepted |
| [0033](0033-fixed-step-double-buffer-is-a-simulation-primitive.md) | The fixed-step double buffer is a **simulation** primitive, owned by `FS.GG.Game.Core` — one accumulator in the org | Accepted |
| [0034](0034-typed-coordination-engine.md) | The coordination engine is a **typed core**; the tool is the model, and the docs are its projection | Accepted |
| [0035](0035-observed-run-receipts.md) | A test obligation is satisfied by a run SDD **read**, not by a `pass` an agent **typed** | Accepted |
| [0036](0036-the-build-config-drift-check-pins-its-source.md) | The shared-build-config drift check compares against a **pin**, not against `main` | Superseded |
| [0037](0037-schema-growth-is-publish-before-flip.md) | Schema growth is **publish-before-flip** — two ordered PRs; the validator gates on the declared `schemaVersion` | Accepted |
| [0038](0038-the-corpus-is-the-cut-over-gate.md) | The **defect corpus** is the cut-over gate; the shadow clock is demoted to telemetry | Accepted |
| [0039](0039-nuget-org-is-the-read-path.md) | **nuget.org is the read path; the org feed is the publish path** | Accepted |
| [0040](0040-port-the-io-layer.md) | The **IO layer is ported to F# too** — an `IGitHub` seam (HTTP adapter + call-counting fake) driven by the shell corpus through a configurable API base; each write's precondition lives in its type | Accepted |
| [0041](0041-the-chore-lock-is-the-item-cas-on-another-subject.md) | A **chore takes the item CAS, unchanged**, on a closed per-repo lock issue — `Writes.claim` is already substrate-general (comments only; the lease is a parameter; the board coupling is a callback), so the refactor #873 priced was never needed | Accepted |
| [0042](0042-the-chore-lock-ref-is-embedded-beside-the-roster.md) | The **chore-lock ref is embedded beside the roster**, keyed on owner and repo — `registry/repos.yml` is unreadable exactly where the queue has to work, because the shim ships to receivers without it | Accepted |
| [0043](0043-a-superseded-run-is-the-one-its-group-replaced.md) | A **superseded run is the one its group replaced** — the conclusion leaves the test: a re-run creates no run, so the `cancelled`-only clause never guarded the re-run-until-green it was written for, and what it kept was a metadata gate's stale failure | Accepted |
| [0044](0044-generated-artifacts-are-derived-from-their-generators.md) | **Generated artifacts are derived from their generators, not declared** — every generator answers `--list`, and `verify-paths` subtracts the whole-file ones, so the advisory stops firing on the behaviour #309 mandates; there is still only ONE declaration surface | Accepted |
| [0045](0045-machine-readable-sentinels-for-human-block-and-chore.md) | **A body-line sentinel says what an empty field cannot** — `Blocked on: human/decision\|action` refuses scheduling regardless of `Paths:` (so a decision item keeps its fix-scope and is still un-takeable, #918/#1081), and `Paths: any` splits a schedulable file-less chore from an unschedulable `Paths: none` epic; `lint` reds an unmarked `Blocked` park | Accepted |
| [0046](0046-one-exit-code-union-renumber-the-github-layer-collisions.md) | **One exit-code union, and the two GitHub-layer codes move off the verdict codes they collided with** — `ExOffboard 3→8`, `ExPartial 4→9` off `Red`/`NoVerdict`; one `FS.GG.Coord.ExitCode` with `toInt`, so a command's return set is enumerable and the generated `take`/`landable` tables are checked complete (#918) | Accepted |
| [0047](0047-client-fs-decomposition-seams-kit-digest-first.md) | **The Client.fs decomposition seams — extract the kit-digest advisory first** — the 4,557-line collision magnet (#979) gets its first `.fsi`-fronted cut, `KitDigest` (the two `repos.lock` advisories + their IO), shrinking Client.fs ~227 lines; the JSON-renderer seam is named and deferred; `declaredWarn` decouples by taking `Transport`, not `Client.Context` (#1164, #1158) | Accepted |
| [0048](0048-fr-level-classification-keys-a-per-requirement-evidence-obligation.md) | **FR-level classification keys a per-requirement, non-synthetic evidence obligation** — an opt-in `{gameplay}` facet on FRs (today the FR grammar has none; `Priority` is inert) derives a kind-gated `pass ∧ synthetic:false` test obligation per classified FR, reported as a per-FR ship disposition; follows the `visualSurface` precedent one granularity finer. SDD half of the headless-gameplay initiative | Accepted |
| [0049](0049-template-profile-gate-inheritance.md) | **A Governance gate can bind to a scaffold template-profile, and every product of that profile inherits it** — policy is copied-and-diverging today (`TemplateProfile` binds nothing); a profile-bound gate becomes an org-owned floor a product cannot lower, keyed on `TemplateProfile`. Carries ADR-0048's per-FR gate to every `game` product at block-on-ship. Governance half of the initiative | Accepted |
| [0050](0050-predicate-at-transition-re-verify.md) | **Re-verify a registry predicate against its owning manifest at the transition** — the #923↔#1194 deadlock is one defect at two seams: a cross-repo request's asked-for `field: value` is never checked against the owning producer at filing, and `Blocked→Ready` clears on the blocker CLOSING, not on the item's own predicate re-verifying. One oracle `P(id, field, value)` → Agrees / Contradicts / Unknown (absent ⇒ Unknown, never False), a reader in the engine, called at a lint-adjacent filing check (on a new structured request field) and inside the `BlockerCleared` chore. Unknown fails closed. Applies ADR-0044 at a new seam; stays inside ADR-0042's carve-out (authority-scoped, fail-closed when the registry is absent) | Accepted |
| [0051](0051-coordination-rooms-derive-from-referencing-items.md) | **A coordination room is an item-referencing rendezvous whose roster and lifecycle derive from the items it references** — the worker channel (ADR-0027 §5) rides one claimed item, which is the wrong shape for the deadlock/negotiation tail (a pair blocked on each other's items shares no item, so no channel). A room is an issue a contended cluster references with a soft `Rooms: #R` body line; membership and lifecycle derive from that link, not a roster or sub-issue parent. One code delta — `inbox`'s subject set widens to include referenced rooms; `say`/`messages` are already subject-general. Amends ADR-0027 §5 | Accepted |
| [0052](0052-onboard-fs-gg-net-transport-component.md) | **Onboard FS.GG.Net as a transport component, and model wire contracts in the registry** — a new bottom-layer sibling (onboarded like Audio, ADR-0023; reaches up to nothing, domain-neutral) for two first uses that share protobuf but not transport: an SC2 client (raw protobuf over WebSocket, `Sequential` id-verified correlation) and a BAR client (owned gRPC proxy). A deliberately small two-tier seam — `ITransport` (byte-message duplex, WebSocket only; gRPC sits above) and `IMessageChannel<'Req,'Resp>` (typed protobuf req/resp) — with the stable core knowing no `.proto` and no game types; schemas live in app repos. Absorbs `fsGRPCSkills` (library encodes the F#/protobuf-net type-gotchas, skills carry the buf/`protoc-gen-fsgrpc` operational knowledge). Adds a wire-contract dimension to `dependencies.yml` in three provenances (vendored external `.proto`, owned `.proto`, code-first protobuf-net surface) — a registry-schema growth governed by ADR-0037's two-ordered-PR procedure (SDD first; ADR-0015 §3) | Accepted |
| [0053](0053-roadmap-driven-milestone-loop-disposable-sdd-subagents.md) | **A roadmap is driven one milestone at a time, each in a fresh disposable subagent that runs the full SDD lifecycle** — the markdown roadmap is plan and ledger both; the parent re-derives the next unchecked milestone from the file, spawns a subagent that ships it via `fs-gg-sdd-*` → ticks the roadmap → runs the `fs-gg-feedback-*` pair → opens/reviews/merges its own PR, then dies; strictly sequential (N+1 branches from a `main` containing N), fails closed on a milestone that can't land, and ends with a timestamped `docs/reports/` report landed as its own PR. Composes the SDD lifecycle (FS.GG.SDD) and the feedback pair (FS.GG.Game) into a loop it does not own — delegating stage order to `fs-gg-sdd-lifecycle` and PR discipline to ADR-0021/0027. Canonized as the `workRoadmap` skill. | Accepted |
| [0054](0054-workroadmap-delivery-fabric-a-github-authored-product-materialized-driver.md) | **A `.github`-authored, product-materialized *driver* is a third `skill-registry` class — `workRoadmap` rides it** — resolves the delivery fabric ADR-0053 deferred (#1224). The two fabrics reach different targets: the coordination kit (ADR-0019) delivers to the 8 framework repos, the producer catalog (ADR-0014/0017) to scaffolded product trees. ADR-0053 §6 fixes `workRoadmap`'s target as scaffolded trees, which only the producer catalog reaches — so A (kit) is disqualified on *target* (it ships the skill where §6 makes it refuse), D defeats §6's purpose, and C (re-home to SDD) is rejected on authorship. Instead `.github` becomes a producer of its own authored drivers: `owner: .github, scope: driver`, a composed (sdd ∧ feedback) `materializes-when`, reconciled into `skills.yml`. Schema growth on `skill-registry`, so publish-before-flip, SDD first (ADR-0037; ADR-0015 §3). | Accepted |
| [0055](0055-reference-gate-set-versioning-plain-semver-with-schema-manifest.md) | `FS.GG.Governance.ReferenceGateSet` versioning — plain SemVer with an in-package schema manifest | Accepted |
| [0056](0056-sdd-is-the-default-lifecycle-spec-kit-is-legacy-and-scheduled-for-removal.md) | **`sdd` is the default lifecycle for every vendored product; `spec-kit` is legacy, frozen, and scheduled for removal** — reverses ADR-0002's default (chosen only to be byte-identical to the pre-decomposition monolith) now that the platform composes via CLI and dogfoods `sdd` (Rouge1 carries the full SDD lifecycle + report yet not `/workRoadmap`, because capture is Spec Kit hook machinery). Default flips on **both** the raw `fs-gg-ui` template and `fsgg-sdd scaffold`, guarded so a standalone consumer can't get a silently lifecycle-less tree. `spec-kit` + its Spec Kit hook fabric (`.specify/`, `after_*`, `fs-gg-feedback-capture`, `spec-kit`-gated `fs-gg-samples`) is scheduled for removal on a published date; existing spec-kit trees are grandfathered with a deadline. Dissolves the `workRoadmap` capture coupling as a *consequence* (→ report-only, predicate widens to `sdd`). Amends ADR-0002 §D2; interacts with ADR-0053 §6 / ADR-0054 / ADR-0004. | Accepted |
| [0057](0057-operator-scope-a-github-authored-never-materialized-skill-class.md) | **A `.github`-authored, never-materialized *operator* skill is a fourth `skill-registry` class — `drive-board` rides it** — amends ADR-0054, whose three classes all *deliver somewhere* (kit → framework repos, producer catalog + driver → product trees). `drive-board` (the cross-repo sibling of `workRoadmap`, ADR-0053) runs **only** from an operator checkout where every repo is a sibling, so it can be delivered nowhere — a single product tree has no siblings. The fourth quadrant: `owner: .github, scope: operator`, `materializes-when: "false"` (the never-true predicate ADR-0017 already carries), so the scope-agnostic union gate reads it as a justified downstream absence with no new gate code. Schema growth on `skill-registry`, so publish-before-flip, SDD first (ADR-0037; ADR-0015 §3). | Proposed |
| [0058](0058-adopt-one-governing-principle-derive-dont-restate.md) | **Adopt one governing principle — *derive, don't restate; gate capabilities, not declarations*** — the 2026-07-20 root-cause analysis traced five friction engines to one defect: the org models reality as hand-maintained projections and builds a gate per projection to catch the drift the projection made possible. The principle becomes the explicit test every new cross-repo ADR must pass — name what a new gate/field/scope *derives from*, or justify the copy as the exception; prefer a generator to an assert-equality gate; gate the capability, not the declaration. A review test, not a mandate to rewrite anything today — [ADR-0044](0044-generated-artifacts-are-derived-from-their-generators.md) is the seam where it already ships. Companion policy: the P5 taxonomy freeze (#1263). | Accepted |
| [0059](0059-freeze-the-coordination-taxonomy-two-instances-before-a-class.md) | **Freeze the coordination taxonomy — two real instances before a new class** — the 2026-07-20 root-cause analysis (§3E, §7 P5) measured a decision cadence exceeding the feature cadence: two of ten recent ADRs (0054, 0057) each added a scope class for a *single* skill. Adopt a restraint rule — no new scope class, body-grammar family, or predicate surface until **two** real cases demand it; re-home a skill to a producer repo (zero schema growth) before minting a class; model a one-axis difference as a **field**, not two classes. A policy that adds no class/grammar/predicate/gate — the cheapest form of [ADR-0058](0058-adopt-one-governing-principle-derive-dont-restate.md). Proposes, as a *separate* follow-up (not executed here), collapsing `driver`+`operator` (ADR-0054/0057) into one delivery-varies class. | Accepted |
| [0060](0060-generate-derived-registry-fields-instead-of-hand-flipping-them.md) | **Generate the derived registry fields instead of hand-flipping them** — the derived fields (`package-version` = feed-newest, `version` = SDD source `<Version>`, live tags, `compatibility.md`'s literal region) are pure functions of sources the coherence gates *already* read on every run and then assert against a hand-typed literal instead of emitting. **Recommends** (Option A) generating them at generation time — fail-closed on an unreadable source — and keeping the gate on the *semantic* fields (ownership, intent, meaning) only, collapsing the publish-before-flip step-2 commit class and the `feed-autofix`/reconcile satellites. The flagship application of [ADR-0058](0058-adopt-one-governing-principle-derive-dont-restate.md). Decision-first; implementation is [#1260](https://github.com/FS-GG/.github/issues/1260). | Accepted |
| [0061](0061-collapse-the-publish-before-flip-schema-validator-split.md) | **Collapse the publish-before-flip schema/validator split** — the registry schema-of-record lives in `.github` and its validator (`Fsgg.Registry`) ships from SDD, and no PR spans two repos (`.github#689`), so every schema change rides the two-ordered-PR rail (ADR-0037): one enum value costs an ADR + two cross-repo PRs + a CLI release + a pin advance. **Recommends** Option (b) — a schema-version-**agnostic** validator that checks *structure*, not a pinned enum, so adding an enum value is one PR in `.github` and the `.github#686` additive tax disappears; Option (a) (schema-of-record as a `.github` data file the CLI reads) recorded as the lower-risk fallback. Decision-first; implementation is [#1261](https://github.com/FS-GG/.github/issues/1261). | Accepted |
| [0062](0062-versioned-kit-package-replaces-byte-copy-sync.md) | **A versioned kit package replaces the byte-copy sync fan-out** — shared skills + `dist/dotnet` build config fan out by digest-addressed file copy, so any edit (even whitespace) opens one sync PR in each of 7 (kit) / 4 (build-config) receivers — 80–116 sync commits/repo per 10 days. **Recommends** Option A: publish one versioned `FS.GG.Kit` artifact to the org feed, referenced (not copied) and picked up via the existing Renovate + dispatch fabric — generalizing the `fsgg-coord` `dotnet tool` precedent (ADR-0034) and deleting the `*-propagate`/`-selftest` family; a materialize+verify step (ADR-0011/0014) preserves on-disk skill roots. Decision-first; implementation is [#1262](https://github.com/FS-GG/.github/issues/1262). | Accepted |
| [0063](0063-scaffold-materializer-sources-skills-from-the-owner-repo.md) | **The scaffold materializer sources a skill from its owner repo, not a frozen provider template** — `fs-gg-playtest` (owner `fs-gg-game`, `mirrored:false`) and `workRoadmap` (owner `.github`, `scope:driver`) are both declared, gated-in, and reach **zero** scaffolds: the materializer sources bytes only from the frozen *provider* template, so a skill whose bytes live in an owner repo that is not that provider has no channel. **Decides** Route C of [#1299](https://github.com/FS-GG/.github/issues/1299) — source each skill from the `owner`/`source` its `skills.yml` row already names (ADR-0058 applied to delivery), **subsuming** ADR-0022 §4's deferred `game` provider and **closing** the sibling driver gap [FS.GG.SDD#620](https://github.com/FS-GG/FS.GG.SDD/issues/620). Route A (bespoke provider) point-fixes one shadow; Route B (live re-source) is ADR-0022-rejected. Decision-first; implementation is a sequenced Coordination epic. | Accepted |
| [0064](0064-workboard-single-repo-board-driven-driver-skill.md) | **`workBoard` — a single-repo, board-driven driver skill fills the fourth loop quadrant** — the loop family has three of four cells filled (`workRoadmap` = single-repo/markdown; `drive-board` = cross-repo/board, `operator`/never-materialized per [ADR-0057](0057-operator-scope-a-github-authored-never-materialized-skill-class.md)); the empty **single-repo/board** cell is exactly "drive a product workspace's own board." **Decides** a *new* `.github`-authored `scope: driver, materializes-when: always` skill (not a materialized `drive-board`, which has no siblings in a product tree): it composes the coordination kit a wired workspace already carries (`check-board`/`pnext-item`/`fsgg-coord`, board id in `FSGG_COORD_*`), targets the already-wired board (no board creation in `new-sdd-workspace`), and **fails gracefully at runtime** when the workspace is not board-capable (`--no-coordination`). Worker is `pnext-item` with SDD-lifecycle escalation by item complexity. Delivery rides the `FS.GG.Drivers` fabric ([ADR-0054](0054-workroadmap-delivery-fabric-a-github-authored-product-materialized-driver.md)/[0063](0063-scaffold-materializer-sources-skills-from-the-owner-repo.md)) and is **not** schema growth ([ADR-0061](0061-collapse-the-publish-before-flip-schema-validator-split.md)). Design: [`docs/reports/2026-07-21-workboard-single-repo-board-driver-design.md`](../reports/2026-07-21-workboard-single-repo-board-driver-design.md). | Proposed |
| [0065](0065-one-agent-skill-root-contract.md) | **One agent-skill root contract for framework repos and product workspaces** — every materializer defaults to `.claude/.agents`; lane-specific triggers and ownership remain separate. Root set narrowed from three by [0067](0067-resolve-dont-copy-one-skill-source-two-runtime-roots-a-generated-view.md) §5, executed 2026-07-28. | Accepted |
| [0066](0066-class-is-a-body-line-and-the-board-field-is-its-projection.md) | **`Class` is a body line and the board field is its projection** — the board had words for *when*, *where* and *how big* and none for **how bad**, so a burn-down could not terminate: measured 2026-07-27, 5 non-Done rows became 34 while 35+ items merged, and a red `main`, a stale test comment and an unmade decision were one undifferentiated row to `batch`, `ready` and the stopping rule ([#1588](https://github.com/FS-GG/.github/issues/1588)). Adopts a `Class: defect\|hardening\|decision` body line in [ADR-0045](0045-machine-readable-sentinels-for-human-block-and-chore.md)'s grammar as the **authority**, with a `[decision]` title prefix and ADR-0045's `Blocked on: human/decision` sentinel as zero-cost derivations; the Projects v2 `Class` field is a read-only **projection** `reconcile` writes, which is why this does *not* reverse ADR-0045's rejection of a field (that rejection is an objection to a field as an **input**). `lint` reports `CLASS-UNSET` and never defaults; `drive-board` stops on **no startable `defect`**, counting an unclassed row as a *possible* defect — a driver keying on `defect` over unpopulated rows would terminate immediately, which is the very failure being fixed (#266's rule on a new axis). | Accepted |
| [0067](0067-resolve-dont-copy-one-skill-source-two-runtime-roots-a-generated-view.md) | **Resolve, don't copy — one skill source of truth, two runtime roots, and a generated view** — the decision of record for [#1611](https://github.com/FS-GG/.github/issues/1611), backed by phase 1's measurements ([#1621](https://github.com/FS-GG/.github/issues/1621)). Adopts resolve-don't-copy; states *verify against an immutable pin, never a moving tip* and *one implementation per invariant, with a callable seam* as standing rules; closes the multi-repo question as settled; and records three things phase 1 established by experiment — there are **two** runtimes not three (`.agents/skills` is Codex's own second native root), the mechanism is a checkout-time **generated view** not a committed symlink (which evaporates silently under `core.symlinks=false`), and the rewrite must **add** a loud absence check because every resolution failure is exit 0 with no diagnostic in both runtimes. All three amended records stay in force. **§5 is EXECUTED (2026-07-28, [#1636](https://github.com/FS-GG/.github/issues/1636)):** the root set 0011/0014/0065 declare is now two and `.codex/skills` is retired — see rows 109, 115 and 130. Every other clause is direction only, and nothing else is retired before its replacement is proven. | Accepted |
| [0068](0068-the-engine-tool-manifest-leaves-kit-ownership.md) | **The engine tool manifest leaves kit ownership, and #1077's invariant is asserted instead of arranged for** — the decision of record for [#1615](https://github.com/FS-GG/.github/issues/1615). `dist/dotnet/.config/dotnet-tools.json` stops being a `kit:` row: because the kit is content-addressed, bumping `fs.gg.coord.cli` by one integer edited kit content, staled `registry/repos.lock`, and obliged a republish plus a seven-receiver fan-out — four of the nine republishes measured on 2026-07-27/28, none of which carried a skill change. Each receiver now owns its manifest and Renovate bumps it directly. [#1077](https://github.com/FS-GG/.github/issues/1077)'s *"receives the tool ⇒ can run the tool"* is **preserved by replacement, not dropped**: `repos.sh validate`'s shim/manifest co-fabric rule (`f(this roster)`) gives way to a daily `repos-audit` sweep over every `coordination-kit` receiver's actual `.config/dotnet-tools.json` (`f(roster, receiver tree)`) — strictly stronger, since a receiver that deleted its own manifest was invisible to the rule it replaces. Un-retires [#1586](https://github.com/FS-GG/.github/issues/1586)'s criterion 5. Records #1077's reasoning, which lived only in `registry/repos.yml` comments. | Accepted |
| [0069](0069-fable-lockstep-is-a-profiled-game-core-package-contract.md) | **Fable lockstep is a profiled Game.Core package contract** — FS.GG.Game owns a supported Fable source view in the existing `FS.GG.Game.Core` package; exactness is graded per stable function and backed by shared canonical-byte fixtures, never inferred from “deterministic .NET” or assembly-wide compilation. The bounded spike covers `Cell`, one edge relation, integer LOS, bounded pathfinding, and a packed-artifact-only consumer. Producer package/profile publication precedes the registry flip and S.I.R. adoption; permanent project references, copied algorithms, and an unproved second public package are rejected. Decision of record for [FS.GG.Game#526](https://github.com/FS-GG/FS.GG.Game/issues/526). | Accepted |
| [0070](0070-restore-is-a-precondition-no-committed-skill-content-in-a-receiver.md) | **Restore is a precondition of the repo being usable — a receiver commits no skill content, and both runtime roots are generated** — the decision of record for [#1837](https://github.com/FS-GG/.github/issues/1837), on [#1834](https://github.com/FS-GG/.github/issues/1834)'s measurement. Applies ADR-0067 §1 between the **package and the repo**: `.claude/skills` stops being committed, so a `coordination-kit` receiver commits no skill content at all. Names what the guarantee does **not** cover (a consumer that never invokes the toolchain — GitHub's web view, a `grep` over a fresh clone), argues that ADR-0067 §7 does **not** reach it (§7 prices *resolvers*; generating at the runtime's own expected path is what §7's own conclusion prefers), and records the reasoning of every clause it amends rather than deleting it. **Retires nothing** — in particular not `kit / coordination-kit`, red **534 times in ~24 days**. Scoped to **kit-derived** content: §1.1 records that a runtime root also holds content the receiver owns and no package can regenerate (SDD's producer manifest, Audio's 16 own skills), which is an unresolved per-receiver precondition owned by [#1855](https://github.com/FS-GG/.github/issues/1855). Blocked in execution on [#1845](https://github.com/FS-GG/.github/issues/1845): a materialize ran on `main` in **0 of 842** receiver runs. | Accepted |
| [0071](0071-two-web-workspace-providers-one-template-package.md) | **Two web workspace providers, one template package** — `FS.GG.Web.Template` exposes independent `fs-gg-web` and `fs-gg-fable-game` identities through `web` and `fable-game` providers. `new-sdd-workspace --template` selects the provider while omitted selection preserves rendering compatibility and `--profile` remains provider-specific. Plain TypeScript/Vite is the neutral web baseline; the bounded Fable/Elmish game shape uses **plain HTTP endpoints with explicit versioned DTOs for typed HTTP request/response (Fable.Remoting is superseded — see the supersession map, [0073](0073-plain-http-with-explicit-dtos-replaces-fable-remoting.md))**, a narrow binding over official `@microsoft/signalr` for real-time sessions, and only Game's published `fs-gg-game-core-fable-lockstep-v1` exact surfaces. Game's skill ships independently in `FS.GG.Game.Skills`; both that package and `FS.GG.Web.Template` require dual-feed/public-read proof. S.I.R. is the first forcing consumer but remains self-contained and application-owned. Publication precedes registry/wizard activation; FS.GG.Net remains unchanged in v1. Decision of record for [#2068](https://github.com/FS-GG/.github/issues/2068). | Accepted |
| [0072](0072-console-and-fable-bindings-are-separate-workspace-providers.md) | **Console and Fable bindings are separate workspace providers** — before publication, widens the package to `FS.GG.Workspace.Template` with independent `console`, `web`, `fable-game`, and `fable-bindings` identities. Console is a minimal tested F# executable with no npm lane. Bindings is a versioned F# interop library over an exactly pinned npm/declaration closure: generation is provisional, maintained Fable interop is curated, and compile, emitted-import, runtime, declaration-drift, coverage, and clean-consumer evidence gate release. `EHotwagner/babylonjsBindings` is the first reference and forcing consumer without making Babylon-specific choices universal. Decision of record for [#2072](https://github.com/FS-GG/.github/issues/2072). | Accepted |
| [0073](0073-plain-http-with-explicit-dtos-replaces-fable-remoting.md) | **Plain HTTP with explicit versioned DTOs replaces Fable.Remoting for `fable-game`'s typed request/response** — `Fable.Remoting.MsgPack`'s Fable-target source is unfixed-upstream defective (unbuildable under the pinned Fable compiler since 5.5.0, `FS.GG.Templates#370`), so ADR-0071 §5's Fable.Remoting bullet and §6's Fable.Remoting pin are superseded: plain ASP.NET Core HTTP endpoints with hand-written, explicit versioned DTO codecs now own typed request/response. SignalR keeps owning connection-oriented traffic unchanged, and the "Alternatives considered" rejection of "Use only Fable.Remoting" is untouched — this record does not merge the two adapters, only replaces one's mechanism. Decision of record for [.github#2082](https://github.com/FS-GG/.github/issues/2082). | Accepted |
| [0074](0074-live-prose-citations-are-local-file-contracts.md) | **Live prose citations are local-file contracts** — gates every tracked live Markdown citation when its top-level root or `src/` namespace is owned by this repository; URLs, bare basenames, and `OWNER/REPOSITORY@REVISION:path:line` references remain cross-repository. ADRs and dated reports preserve historical observations. Issue/PR bodies, free-form counts, suite-green claims, and line-range semantics remain review-time evidence obligations until they have a structured authority; mandatory inversion and zero-corpus legs prevent a vacuous green detector. Decision of record for [#2587](https://github.com/FS-GG/.github/issues/2587). | Accepted |

## Supersession map

Which record currently rules, and where a decision was amended. Every edge below is recorded in
**both** files (rule 3) — this table is a convenience, never the authority.

| Amended | § | By | What changed |
|---|---|---|---|
| [0071](0071-two-web-workspace-providers-one-template-package.md) | §1, §6 | [0072](0072-console-and-fable-bindings-are-separate-workspace-providers.md) | Before first publication, `FS.GG.Web.Template` becomes `FS.GG.Workspace.Template`; independent `console` and `fable-bindings` identities join `web` and `fable-game`, and the release/wizard activation gate covers all four. Web/game-specific decisions stand. |
| [0071](0071-two-web-workspace-providers-one-template-package.md) | §5, §6 | [0073](0073-plain-http-with-explicit-dtos-replaces-fable-remoting.md) | Fable.Remoting is dropped as `fable-game`'s typed request/response mechanism (unfixed upstream `Fable.Remoting.MsgPack` compile defect, `FS.GG.Templates#370`) and replaced by plain HTTP endpoints with explicit, hand-written versioned DTO codecs. SignalR's role, the `@microsoft/signalr` binding, and the "Alternatives considered" rejection of "Use only Fable.Remoting" stand. |
| [0002](0002-composition-by-scaffold-lifecycle-parameter-governance-populated.md) | D4 | [0004](0004-constitution-ownership-for-lifecycle-sdd-products.md) | Constitution ownership — the open P0 gate is closed: SDD owns it. |
| [0002](0002-composition-by-scaffold-lifecycle-parameter-governance-populated.md) | D2 | [0056](0056-sdd-is-the-default-lifecycle-spec-kit-is-legacy-and-scheduled-for-removal.md) | **Default flips.** `lifecycle` default `spec-kit → sdd` on both scaffold surfaces; `spec-kit` becomes legacy + scheduled for removal. D1/D3 stand. |
| [0054](0054-workroadmap-delivery-fabric-a-github-authored-product-materialized-driver.md) | class set | [0057](0057-operator-scope-a-github-authored-never-materialized-skill-class.md) | **Fourth class added.** `scope: operator` (`.github`-authored, materialized nowhere) joins kit / producer-catalog / driver. The three named classes and the `scope: driver` decision stand. |
| [0006](0006-org-shared-dotnet-build-config-and-unified-restore-locked-mode-gate.md) | D3 | [0036](0036-the-build-config-drift-check-pins-its-source.md) | `--check` compares against the receiver's committed pin, not `.github@main`. |
| [0007](0007-reference-gate-set-package-version-derivation.md) | **all** | [0055](0055-reference-gate-set-versioning-plain-semver-with-schema-manifest.md) | **Superseded.** Version is a plain SemVer bumping on any change; the four `schemaVersion`s move into an in-package manifest. The 4-segment schema-derived rule could not represent content-only drift (#1228). |
| [0009](0009-cli-single-orchestrator-detect-and-remediate.md) | — | [0030](0030-creation-time-scaffolding-self-updates-by-default.md) | Creation-time scaffolding self-updates; every in-project verb still does not. |
| [0011](0011-agent-skill-roots-full-union-orchestrator-owned-mirror.md) | impl | [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) | The invariants stand; the four hand-maintained mirrors become one content-addressed materialize-and-verify. |
| [0011](0011-agent-skill-roots-full-union-orchestrator-owned-mirror.md) | D1 / Context | [0067](0067-resolve-dont-copy-one-skill-source-two-runtime-roots-a-generated-view.md) §5 | **EXECUTED 2026-07-28 ([#1636](https://github.com/FS-GG/.github/issues/1636)):** D1's root set is now two — `.claude/skills`, `.agents/skills`. `.codex/skills` is retired; "the generic agent convention" was never a third runtime. D1's union rule and 0011's other four invariants are untouched and still govern, over two roots; the symlink rejection is re-affirmed. Amended by [#1703](https://github.com/FS-GG/.github/issues/1703), a day late — #1690 landed the flip and amended 0065 only. |
| [0011](0011-agent-skill-roots-full-union-orchestrator-owned-mirror.md) | D1 / D2, **receivers only** | [0070](0070-restore-is-a-precondition-no-committed-skill-content-in-a-receiver.md) §1, §5 | **NOT executed — a contract decision that retires nothing** ([#1837](https://github.com/FS-GG/.github/issues/1837), 2026-07-28). In a `coordination-kit` receiver the roots stop being **committed**; both are generated from the restored kit package. The union rule is untouched and, **for kit-derived content**, becomes structural — it all descends from one restored package, so there is no second copy to diverge; content the receiver itself put in a root is outside that and is ADR-0070 §1.1's obstacle. The symlink rejection is re-affirmed **as written** — it is about a **committed** link degrading under `core.symlinks=false` — and is not widened to uncommitted links. The scaffolded-product-workspace lane is out of scope. |
| [0012](0012-dual-publish-to-nuget-org.md) | §6 | [0013](0013-trusted-publishing-oidc-for-nuget-org.md) | Auth is Trusted Publishing (OIDC), not a long-lived key. |
| [0012](0012-dual-publish-to-nuget-org.md) | §1 | [0039](0039-nuget-org-is-the-read-path.md) | The **read path** moved to nuget.org; the org feed is no longer the coherence source of truth. |
| [0013](0013-trusted-publishing-oidc-for-nuget-org.md) | §5 | *(self, 2026-07-14)* | "Never a silent no-op" is scoped by `vars.NUGET_ORG_PUBLISH`: unset ⇒ the nuget.org leg is skipped silently. |
| [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) | D1 | [0017](0017-skill-registry-condition-aware-materialization.md) | The manifest entry grows `materializes-when` / `supplied-by`; `registry/skills.yml` becomes the org catalog. |
| [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) | D5 | [0065](0065-one-agent-skill-root-contract.md) | The universal root default applies to coordination-kit receivers as well as scaffolded products. |
| [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) | D5 | [0067](0067-resolve-dont-copy-one-skill-source-two-runtime-roots-a-generated-view.md) §5 | **EXECUTED 2026-07-28 ([#1636](https://github.com/FS-GG/.github/issues/1636)):** D5's declared root set is now two — `.claude/skills`, `.agents/skills`. `.codex/skills` is retired. D5's design stands and is what made it a one-line change; a `FsggKitRetiredSkillRoots` constant joined it to sweep receivers. D6 (no committed symlinks) is untouched and re-affirmed on a measurement. Amended by [#1703](https://github.com/FS-GG/.github/issues/1703), a day late — #1690 landed the flip and amended 0065 only. |
| [0015](0015-register-the-registry-schema-as-a-governed-contract.md) | §3 | [0037](0037-schema-growth-is-publish-before-flip.md) | The "same change" procedure **cannot exist** — no PR spans two repos. Two ordered PRs instead. §1–2 stand. |
| [0018](0018-transient-durable-sdd-artifact-taxonomy.md) | — | [0026](0026-committed-compact-ship-verdict.md) | One role-based exception; the pattern becomes `readiness/*/*` (git will not descend into an excluded directory). |
| [0019](0019-org-repo-roster-registry-and-coordination-kit.md) | kit row | [0034](0034-typed-coordination-engine.md) | The `kit:` row becomes a shim over a `dotnet tool`. |
| [0021](0021-parallel-intra-repo-work-claim-worktree-touchset.md) | §1 | [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) | The assignee lock was a **no-op** (N agents, one account). Comment-order CAS on `fsgg:claim` markers. Worktree + touch-set stand. |
| [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md) | D1 | [0033](0033-fixed-step-double-buffer-is-a-simulation-primitive.md) | The double-buffered loop is simulation surface, and belongs with the accumulator in `Game.Core`. |
| [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md) | open item | [0029](0029-game-owns-the-testspec-corpus.md) | FS.GG.Game owns the TestSpec corpus; `.github` keeps stubs. |
| [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md) | D4 | [0063](0063-scaffold-materializer-sources-skills-from-the-owner-repo.md) | The deferred `game` scaffold provider is **cancelled**; frozen `--profile game` copies are retired by the materializer sourcing FS.GG.Game's `product-skills/` directly. §4's live-re-source rejection stands. |
| [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) | impl | [0034](0034-typed-coordination-engine.md) | Implementation only — **the comment-order CAS stands**. |
| [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) | scheduler | [0038](0038-the-corpus-is-the-cut-over-gate.md) | **Blockers are checked before the touch-set** — a blocked item cannot start whatever its touch-set says. |
| [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) | §5 | [0051](0051-coordination-rooms-derive-from-referencing-items.md) | The channel gains a **derived** subject: `inbox` widens to "items I claim ∪ rooms those items reference" (a `Rooms: #R` line), for the deadlock/negotiation tail. The per-item channel stands. |
| [0031](0031-republished-package-is-a-named-failure.md) | **all** | [0032](0032-the-lock-hash-must-not-depend-on-the-machine.md) | **Withdrawn — premise false.** §1 (cold restore) and §3 (never hand-write a `contentHash`) survive, in 0032 §5 and §4. |
| [0034](0034-typed-coordination-engine.md) | §5 | [0038](0038-the-corpus-is-the-cut-over-gate.md) | The three-day shadow clock **could never tick**. The gate is the defect corpus; the shadow is telemetry. |
| [0034](0034-typed-coordination-engine.md) | §5 exit / §4.4 | [0040](0040-port-the-io-layer.md) | "Delete the bash implementation" was **not reachable** — the IO layer is ported, each write's precondition in its type; the shim's preconditions are named. |
| [0041](0041-the-chore-lock-is-the-item-cas-on-another-subject.md) | the config clause | [0042](0042-the-chore-lock-ref-is-embedded-beside-the-roster.md) | The lock's number is **not** in `registry/repos.yml` — the engine has no YAML reader, and must not: the shim ships to receivers without the roster. Embedded beside the roster instead. The substrate decision, and `absent ⇒ offer refuses`, stand. |
| [0065](0065-one-agent-skill-root-contract.md) | root set | [0067](0067-resolve-dont-copy-one-skill-source-two-runtime-roots-a-generated-view.md) §5 | **EXECUTED 2026-07-28 ([#1636](https://github.com/FS-GG/.github/issues/1636)):** the ordered set is now two — `.claude/skills`, `.agents/skills`. `.codex/skills` is retired; it had no runtime behind it. The transport contract is unchanged and still governs; 0065 §Retiring a root states how a root leaves the set without violating it. How far 0067 §6's generated view has shipped is **not restated here** — that stage has one home, [the retirement order](../coordination/skill-apparatus-retirement-order.md) §6's standing stage verdict ([#1709](https://github.com/FS-GG/.github/issues/1709): this cell said *"NOT landed"* for **~19 hours** after the 7-of-7 verdict was stamped at 2026-07-28 09:55Z, and ~26 hours after stage 1). |
| [0065](0065-one-agent-skill-root-contract.md) | *materialized* disposition, **receivers only** | [0070](0070-restore-is-a-precondition-no-committed-skill-content-in-a-receiver.md) §1, §5 | **NOT executed — a contract decision that retires nothing** ([#1837](https://github.com/FS-GG/.github/issues/1837), 2026-07-28). A materialized root stops implying a **tracked** path: the kit still writes real, content-addressed files, into a git-ignored root. The runtime root set is unchanged, so this is not a retirement. The transport contract, §Retiring a root, and the *view* and *retired* dispositions all still govern; product materialization for scaffolded workspaces is out of scope. |
| [0067](0067-resolve-dont-copy-one-skill-source-two-runtime-roots-a-generated-view.md) | §6 | [0070](0070-restore-is-a-precondition-no-committed-skill-content-in-a-receiver.md) §1 | **NOT executed — a contract decision that retires nothing** ([#1837](https://github.com/FS-GG/.github/issues/1837), 2026-07-28). §6's premise — a view resolved from a **committed** source root — no longer holds in a receiver, because `.claude/skills` is generated too. Which ADR-0065 disposition each root takes is left open. §6's rejection of a committed symlink is re-affirmed — nothing committed, nothing to degrade. §8 binds harder: it becomes the whole of the guarantee. **§7 is NOT amended and does not reach that decision** — it prices *resolvers*, and its own conclusion prefers producing content at each runtime's expected path. |
