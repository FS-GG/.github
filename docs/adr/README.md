# Architecture Decision Records (cross-repo)

ADRs for decisions that span more than one FS-GG repo. Per-repo decisions live in that
repo. Use [template.md](template.md) for a new record; number sequentially. Withdrawn
numbers are **retired, not reused** — a withdrawn ADR keeps a tombstone row so the
sequence stays gap-free to a reader.

| ADR | Title | Status |
|---|---|---|
| [0001](0001-cross-repo-coordination-via-issues.md) | Cross-repo coordination via GitHub issues + a registry | Accepted |
| [0002](0002-composition-by-scaffold-lifecycle-parameter-governance-populated.md) | Composition by scaffold, `lifecycle` template parameter, governance populated-by-default | Accepted |
| [0003](0003-rename-fs-skia-ui-version-machinery-to-fs-gg-ui.md) | Rename the `fs-skia-ui` version-coherence machinery to `fs-gg-ui` (clean break) | Accepted |
| [0004](0004-constitution-ownership-for-lifecycle-sdd-products.md) | SDD owns the `lifecycle=sdd` constitution, shipped at `.fsgg/constitution.md` | Accepted |
| [0005](0005-fsgg-slot-ownership-sdd-project-governance-governance.md) | `.fsgg/` slot ownership — SDD owns `project.yml`, Governance owns `governance.yml` | Accepted |
| [0006](0006-org-shared-dotnet-build-config-and-unified-restore-locked-mode-gate.md) | `.github` owns the org-shared .NET build config; `RestoreLockedMode` gates on `GITHUB_ACTIONS` | Accepted |
| [0007](0007-reference-gate-set-package-version-derivation.md) | `FS.GG.Governance.ReferenceGateSet` version-derivation rule | Accepted |
| [0008](0008-fsgg-sdd-cli-first-class-member-of-coherent-set.md) | The `fsgg-sdd` CLI is a first-class member of the coherent set (orchestrator axis) | Accepted |
| [0009](0009-cli-single-orchestrator-detect-and-remediate.md) | The `fsgg-sdd` CLI is the single orchestrator — detect-and-remediate, not silent auto-update | Accepted (creation-time scaffolding refined by [0030](0030-creation-time-scaffolding-self-updates-by-default.md)) |
| ~~0010~~ | *SDD-native scaffold (inline `--provider-source`, explicit currency, config-driven governance default)* — declined in favour of the clone-free `scripts/new-sdd-fullstack.sh` working through existing machinery ([#100](https://github.com/FS-GG/.github/pull/100)) | **Withdrawn** |
| [0011](0011-agent-skill-roots-full-union-orchestrator-owned-mirror.md) | Every agent-skill root carries the full skill union; `fsgg-sdd` owns the mirror (orchestrator fan-out) | Accepted (implementation superseded by [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md); invariants stand) |
| [0012](0012-dual-publish-to-nuget-org.md) | Dual-publish FS-GG packages to nuget.org (public) alongside the org GitHub Packages feed | Accepted (§6 auth superseded by 0013) |
| [0013](0013-trusted-publishing-oidc-for-nuget-org.md) | Publish to nuget.org via Trusted Publishing (OIDC), not a long-lived API key | Accepted |
| [0014](0014-skill-vendoring-one-manifest-one-materialize-verify.md) | Skill vendoring & mirroring — one manifest, one materialize-and-verify, content-addressed (extends 0011) | Accepted |
| [0015](0015-register-the-registry-schema-as-a-governed-contract.md) | Register the registry schema as a governed contract (`registry-schema`; schema growth = tracked contract-change) | Accepted (§3's "same change" procedure superseded by [0037](0037-schema-growth-is-publish-before-flip.md) — that change cannot exist; §1–2 stand) |
| [0016](0016-retire-templates-local-new-fullstack-single-scaffolder.md) | Retire the Templates-local `new-fullstack.sh`; one sole scaffolder (completes 0010's withdrawal; reimplemented 2026-07-04 as a dotnet tool, then renamed `new-sdd-fullstack` → **`new-sdd-workspace`** / `FS.GG.NewSddWorkspace` to match the ADR-0020 vocabulary once `--profile` made "fullstack" inaccurate) | Accepted |
| [0017](0017-skill-registry-condition-aware-materialization.md) | Org skill registry + condition-aware `materializes-when` on the manifest; the union gate tells intentional off-profile absence from a genuine drop (extends 0014) | Proposed |
| [0018](0018-transient-durable-sdd-artifact-taxonomy.md) | Transient vs durable SDD artifact taxonomy; regenerable process output is gitignored by role (SDD ships the fragment), not committed per-feature | Proposed |
| [0019](0019-org-repo-roster-registry-and-coordination-kit.md) | Org repo roster registry (`registry/repos.yml`) — the single authoritative framework-repo list every fabric reads via `receives` capabilities; `.github` mirrors a coordination kit (skill + `fsgg-coord`) to participants (parallels 0014/0017) | Proposed |
| [0020](0020-platform-workspace-component-vocabulary.md) | Name the two products precisely — **platform** (the org), **component** (one repo), **workspace** (what you scaffold); "product" was overloaded | Accepted |
| [0021](0021-parallel-intra-repo-work-claim-worktree-touchset.md) | Parallel intra-repo work — the inner-repo sibling of 0001: a **claim** lock (assignee + `Status: In progress`), one **git worktree** per item, and a declared **`Paths:` touch-set** with an overlap check; reuses the Coordination board, done-stamp, and `fsgg-coord` (adds `claim`/`release`/`overlap`) | Accepted (§1 assignee-lock superseded by [0027](0027-worker-keyed-claim-lock-and-worker-channel.md); worktree + touch-set stand) |
| [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md) | Extract **FS.GG.Game** as the platform's sixth component (BCL-only `Game.Core` new bottom layer + `Game.Render` Scene adapter); `Scene.Geometry` **Option D** (module → `Game.Core`, forced major on Canvas **and** Scene, no `FS.GG.Math` leaf); develop it with `fsgg-sdd` **coexisting** with Spec Kit; defer the consumer provider and **freeze** `--profile game` | Proposed |
| [0023](0023-onboard-fs-gg-audio-as-an-sdd-driven-component.md) | Onboard **FS.GG.Audio** as the platform's **seventh** component — a standalone render-independent audio component (Core `AudioEffect` vocabulary / Host `IAudioBackend` seam / Engine buses·fades·ducking·3D / Elmish `Cmd` bridge), transferred into the org (`EHotwagner/FS.GG.Audio-component` → `FS-GG/FS.GG.Audio`) + first release **0.1.0-preview.1**; roster row + `fs-gg-audio` contract + architecture map 6→7. Depends on no FS-GG component. Prerequisite for 0024 — see epic [#234](https://github.com/FS-GG/.github/issues/234). | Proposed |
| [0024](0024-wire-fs-gg-audio-into-the-game-scaffold-profile.md) | Wire **FS.GG.Audio** into the `game`/`sample-pack` scaffold profile — pin all four packages (`Core`/`Host`/`Engine`/`Elmish`) from the Rendering `fs-gg-ui` template on its own **`$(FsGgAudioVersion)`** axis (mirrors 0022 P5); **complete the extraction** (retire `FS.GG.UI.Canvas.Audio`, re-point skill+template to `FS.GG.Audio.Core` — Option **(a)**, breaking Rendering change); keep the shared `fs-gg-audio` skill/package id as one capability at two layers | Proposed |
| [0025](0025-first-class-shipped-surface-mutation-event.md) | First-class **shipped-surface mutation** — treat a changed committed `.fsi` baseline of a *published* surface as a governed event: **detect** (sdd, on #240's baseline) → **classify** additive/breaking → reconcile **version** (publishing), **registry+projection+ADR** (.github, existing gates), and a **consumer-impact flag** (coordination); `.github` ships `scripts/fsgg-surface-impact` to enumerate the consumers to flag. Design slice of epic [#235](https://github.com/FS-GG/.github/issues/235)/[#236](https://github.com/FS-GG/.github/issues/236) | Proposed |
| [0026](0026-committed-compact-ship-verdict.md) | **Committed compact ship verdict** — the merge-boundary answer survives in git history (extends [0018](0018-transient-durable-sdd-artifact-taxonomy.md)). `ship.json` stays regenerable+ignored; `fsgg-sdd ship` also emits a ≤20-line `readiness/<id>/ship-verdict.json` (verdict + `sourcesDigest`), committed via **one role-based** gitignore exception — not 0018's banned per-feature re-inclusion list. Requires `readiness/*/` → `readiness/*/*` (git cannot re-include through an excluded directory). Design slice of epic [#235](https://github.com/FS-GG/.github/issues/235) | Proposed |
| [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) | **The parallel-work lock is keyed on the worker, not the account** (amends [0021](0021-parallel-intra-repo-work-claim-worktree-touchset.md) §1). N agents share one GitHub account, so the assignee lock was a **no-op** — a sibling's claim read back as "assigned to me" and both workers proceeded. Replaces it with a **comment-order CAS** on `fsgg:claim` markers (GitHub comment ids are a server-side total order → exactly one winner), plus **worker ids** (`whoami`, `git config fsgg.worker`, `FSGG-Worker:` trailer), a **lease** + `reap`, `who` (incl. `UNCLAIMED` detection), the `batch`/`take` scheduler that reads the marker not the board, a worker **channel** (`say`/`inbox`, `widen` notifies on collision), and advisory `verify-paths` drift | Accepted |
| [0028](0028-keyboard-input-config-mechanism-policy-boundary.md) | **Keyboard input-config boundary — mechanism (Rendering) vs policy (Game)**: the keymap *mechanism* (keymap-set type, resolve, conflict detection, serialization, config-screen widget, live-dispatch wiring) is **Rendering-owned** and command-id-agnostic — the command id is an **opaque token**; the command *vocabulary* (`FS.GG.Game.Core`) and *default keymap* (`FS.GG.Game.Render`) are **Game-owned**. Game hands Rendering a keymap as **data** over a `Game.Render → FS.GG.UI.KeyboardInput` edge (downstream → upstream, per [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md)); Rendering never learns a game action. Ratifies the boundary the mechanism (Rendering P1) + policy (Game P6) children build against, before the registry edge (contract child [#365](https://github.com/FS-GG/.github/issues/365)) lands. Epic [Rendering#330](https://github.com/FS-GG/FS.GG.Rendering/issues/330) | Accepted |
| [0029](0029-game-owns-the-testspec-corpus.md) | **The game TestSpec corpus is FS.GG.Game-owned; .github keeps pointer stubs** — FS.GG.Game becomes the single source of truth for the 15 `docs/TestSpecs/Games/*` specs + `docs/TestSpecTutorial.md`; `.github` retires its canonical copies to redirect stubs, re-points the consumer/profile references, and supersedes the ADR-0022 open-item recommendation | Accepted |
| [0030](0030-creation-time-scaffolding-self-updates-by-default.md) | **Creation-time scaffolding self-updates the CLI by default** — a bounded carve-out to [0009](0009-cli-single-orchestrator-detect-and-remediate.md). `new-sdd-workspace` self-updates `fsgg-sdd` to the newest coherent set **before** scaffolding (new step 2), so a fresh workspace is always built by current tooling; best-effort/non-blocking, `--pinned` (+ `--ref <tag>`) opts back into a reproducible pinned scaffold. Scope is only the create-a-new-workspace step — ADR-0009's "no silent auto-update" stands for every in-project `fsgg-sdd` verb | Accepted |
| [0031](0031-republished-package-is-a-named-failure.md) | **A silently re-published package is a NAMED failure** — ⚠️ **PREMISE FALSE; superseded in part by [0032](0032-the-lock-hash-must-not-depend-on-the-machine.md).** `FSharp.Core 10.1.301` was **never re-published**; the SDK bundles a *different* `.nupkg` than nuget.org serves at the same id+version, so a lock's `contentHash` depends on **which source** resolved it, not on when ([#471](https://github.com/FS-GG/.github/issues/471)). **What stands:** (1) any restore that **writes** (`--force-evaluate`) or **enforces** (`--locked-mode`) a lock file must be **cold** — a warm folder makes both the generator and the enforcer compare a record to a record (demonstrated against a real restore by [#460](https://github.com/FS-GG/.github/issues/460)'s fixture); (3) a `contentHash` is **never** hand-written (the catalog `packageHash` is a *different* hash and pasting it re-breaks the build). **What does not:** (2)/(4) — detecting re-publication via the catalog `packageHash` would detect **nothing**, and "cold" is not "hermetic". Epic [#266](https://github.com/FS-GG/.github/issues/266), item [#429](https://github.com/FS-GG/.github/issues/429) | **Superseded in part** |
| [0032](0032-the-lock-hash-must-not-depend-on-the-machine.md) | **The lock file's `contentHash` must not depend on the machine** — `FSharp.Core 10.1.301` was never re-published: there have always been **two** `.nupkg` files at that id+version, the SDK's bundled copy (`…/library-packs/`, 3,051,664 B) and nuget.org's (3,066,660 B), and `contentHash` is a function of **which source served the package**. CI resolves the SDK's; a dev box that excludes `library-packs` resolves nuget.org's. Ratifies: (1) a regenerated `packages.lock.json` must be **byte-identical on any machine**; (2) `FSharp.Core` resolves from **nuget.org everywhere** — the SDK's `library-packs` copy is excluded, so `contentHash` is a function of `(id, version)` alone and not of the SDK patch level; (3) the resulting re-pin is a **coherent set**, generated after the exclusion lands, or a re-pinned repo meets a CI that still resolves `library-packs`; (4) a hash mismatch must **name the source**. Supersedes [0031](0031-republished-package-is-a-named-failure.md) §Context + §Decision 2; its cold-restore decision stands. [#471](https://github.com/FS-GG/.github/issues/471), epic [#266](https://github.com/FS-GG/.github/issues/266) | Accepted |
| [0033](0033-fixed-step-double-buffer-is-a-simulation-primitive.md) | **The fixed-step double buffer is a SIMULATION primitive, owned by `FS.GG.Game.Core`** (amends [0022](0022-extract-fs-gg-game-as-an-sdd-driven-component.md) §Decision 1). ADR-0022's `Game.Core` inventory said `fixed-step` and was **silent on the double-buffered loop built on it** — so `FixedStep.drain` moved down while `Loop`/`StepState` stayed up in Canvas, splitting **one accumulator across two repos**. They then diverged on the thing that matters: `drain` was hardened against non-finite input, `Canvas.Loop.advance` propagated `NaN` and froze the sim permanently ([Rendering#266](https://github.com/FS-GG/FS.GG.Rendering/issues/266)) — **the hardened copy was not the one products used**. Ratifies: (1) the buffer (`StepState`/`init`/`advance`/`alpha`) is **simulation** surface, superseding the "render Loop" `.fsproj` comment; (2) **one accumulator in the org** — a second implementation is a defect; (3) the **double-buffered loop is the DEFAULT** for continuously-moving simulations, departures MUST be recorded in the spec (*interpolate when the world moves between ticks; buffer when you interpolate*); (4) never feed `alpha` back into the sim, never step with a variable `dt`, never read a wall clock below the effect interpreter; (5) a **cross-repo boundary is decided in an ORG ADR** — a repo-local ADR may execute one and must cite it. **Late ratification: already executed** — [Rendering ADR-0104](https://github.com/FS-GG/FS.GG.Rendering/blob/main/docs/product/decisions/0104-canvas-loop-is-a-simulation-primitive.md) (#269) made the call repo-locally and retired `Canvas.Loop` at framework `0.6.0`; no code changes fall out. [#313](https://github.com/FS-GG/.github/issues/313) | Accepted |
| [0034](0034-typed-coordination-engine.md) | **The coordination engine is a typed core; the tool is the model, and the docs are its projection** (amends [0019](0019-org-repo-roster-registry-and-coordination-kit.md) kit-row shape; [0027](0027-worker-keyed-claim-lock-and-worker-channel.md) implementation only — the comment-order CAS **stands**). `scripts/fsgg-coord` is 4,024 lines of bash modelling a concurrent, transactional, budget-constrained domain in jq regexes over prose — so *"is this item startable?"* is **computed in five places and agrees in none** ([#485](https://github.com/FS-GG/.github/issues/485); 34 issues, and #440's fix reintroduced #440's defect in its own else-branch, [#488](https://github.com/FS-GG/.github/issues/488)), an empty answer is indistinguishable from an error (epic [#266](https://github.com/FS-GG/.github/issues/266), 51 children), and a rule lives in **54 vendored copies** whose propagation edge is a second issue and a second PR every time (#309→#502, #481→#531). Decides: (1) a pure typed `FS.GG.Coord.Core` — **one** schedulability function returning a DU, every check `Green | Red | NoVerdict` with `NoVerdict` **non-zero**; (2) the `kit:` row becomes a **shim** exec'ing a `dotnet tool` resolved from the already-distributed `.config/dotnet-tools.json`, so every gate and path reference survives and the publish cycle breaks by asymmetry (**.github builds from source**) — a **contract-change** under [0015](0015-register-the-registry-schema-as-a-governed-contract.md), `repos.yml` `schemaVersion` bump; (3) **the tool is the sole GraphQL principal**, gated (6 violators at HEAD — the cause of #528/#538), which is what makes aliased writes ([#448](https://github.com/FS-GG/.github/issues/448), a measured ~6×) buildable at all; (4) `parallel-work.md` + the four `SKILL.md` become **generated projections** of the model, retiring the drift class by construction; (5) migration is **shadow-mode** — bash stays authoritative until divergence is zero for three days. Retires ~22 of 40 open `.github` issues; explicitly **not** a fix for the build/publish/pin substrate. **Phase 0 delivered 2026-07-12** — the done-stamp ([#558](https://github.com/FS-GG/.github/issues/558)/[#543](https://github.com/FS-GG/.github/issues/543)/[#583](https://github.com/FS-GG/.github/issues/583)), one queueing board write ([#510](https://github.com/FS-GG/.github/issues/510)), the kit-digest obligation made observable ([#588](https://github.com/FS-GG/.github/issues/588)/[#563](https://github.com/FS-GG/.github/issues/563)), and the GraphQL monopoly + `fsgg-coord add` ([#587](https://github.com/FS-GG/.github/issues/587)). [design doc](../design/coordination-engine.md) | Accepted |
| [0035](0035-observed-run-receipts.md) | **A test obligation is satisfied by a run SDD *read*, not by a `pass` an agent *typed*** — `evidence --from-tests` parses a runner-produced TRX/JUnit report into an `observedRun` receipt (bytes hashed, counts recorded), and `verify` refuses to mark a test obligation `satisfied` without one (`unobserved` instead). SDD **never runs a test** — no toolchain knowledge enters generic SDD. Boundary: SDD owns the receipt + the disposition; **Governance** owns freshness and whether `unobserved` may cross a merge boundary. Instance (j) of [#266](https://github.com/FS-GG/.github/issues/266); breaking, staged disclose → record → fail-closed | Accepted |
| [0036](0036-the-build-config-drift-check-pins-its-source.md) | **The shared-build-config drift check compares against a PIN, not against `main`** (amends [0006](0006-org-shared-dotnet-build-config-and-unified-restore-locked-mode-gate.md)). Every receiver's `gate.yml` checked `.github` out at `ref: main` and required byte-identity with `dist/dotnet/`, so the verdict of a **required** check was a function of *another repo's moving branch at CI time*: a receiver could not make it green from its own PR, and any edit here red-lit **every open PR in every adopting repo**. It fired twice — [#499](https://github.com/FS-GG/.github/issues/499) froze FS.GG.SDD for hours (SDD#379), and [#536](https://github.com/FS-GG/.github/issues/536) then red-lit a green in-flight PR by editing an **XML comment** (SDD#380). The push arm ([#626](https://github.com/FS-GG/.github/issues/626)) shortened the freeze but left it *indefinitely reachable* — a sync PR whose own `gate` goes red never auto-merges. Decides: the receiver commits `.config/fsgg-build-config.sha` recording the `.github` commit it synced from, and `--check` diffs against **that commit's** `dist/dotnet/`. The verdict becomes a pure function of the receiver's own tree — **behind is GREEN** (bot-remediated, loudly noticed), **hand-edited is RED** (fixable from the author's branch). A deliberate inversion for a *required* check: stale-until-someone-merges over merge-freeze-by-default. Needs **no receiver change** (the script is distributed too); an absent pin means legacy mode, so the rollout cannot freeze anyone. `shared-build-config` 1.0.0→1.1.0 | Accepted |
| [0037](0037-schema-growth-is-publish-before-flip.md) | **Schema growth is publish-before-flip — two ordered PRs, and the validator gates on the declared version** (amends [0015](0015-register-the-registry-schema-as-a-governed-contract.md) §3). ADR-0015 §3 obliged the `schemaVersion` bump + the validator-pin advance "in the **same change**" that teaches `Fsgg.Registry`. **That change cannot exist:** for both governed contracts the schema *document* is in `.github` (`registry/dependencies.yml`, `registry/skills.yml`) and the *validator* is in **FS.GG.SDD** (`Fsgg.Registry`), and no PR spans two repos — nor can a pin advance to a CLI that is not published yet. It never bit only because ADR-0015 held `schemaVersion` at `1` and the procedure was **never executed**; `mirrored:` ([#658](https://github.com/FS-GG/.github/issues/658)) is the first real schema growth since, and it landed **unbumped** ([#686](https://github.com/FS-GG/.github/issues/686)). Decides: (1) **two ordered PRs**, publish-before-flip (FR-007) — SDD teaches + **publishes** a CLI, `.github` **then** bumps both scalars and advances the pin; (2) the invariant is "**the pin advances with the schema**", not "one commit does both"; (3) a new field/rule is enforced **only when the document declares the new `schemaVersion`** — because `pin-coherence.yml` hard-fails when a pin is behind feed-newest, a CLI release reds `.github@main` (via the daily cron — a release touches no file here) and the **pin advances ahead of the bump, on Renovate's own PR**, so the window is **opened by the org's anti-freeze machinery**, not merely tolerated, and a validator that rejected the un-bumped document would red the very gate step 2 must pass; (4) an **OPTIONAL, ADDITIVE field IS schema growth** (M5 *was* that defect — "no consumer breaks" is its **mechanism**, not an exemption). The ordering is **derived, not chosen**: a validator ahead of the document is safe (every rule the document is subject to is one the gate can check), while a document ahead of the validator *is* the H2/M5 defect — and it does **not** announce itself: under **additive tolerance** the stale validator *passes* the grown document and `pin-coherence` is green too, so flip-then-publish is **silently green over a schema nothing checks**. It is also the only revertible order (unpublishing a package is not a thing this org does). ADR-0015 §1–2 stand | Accepted |
| [0038](0038-the-corpus-is-the-cut-over-gate.md) | **The defect corpus is the cut-over gate — the shadow clock could never tick, and the corpus found what the clock was built to classify as noise** (amends [0034](0034-typed-coordination-engine.md) §5). Phase 3b was gated on `divergence --fleet` going green: three covered days, ≥2 distinct workers. **That clock could not tick** — workers run in per-item worktrees, worktree workers resolved no engine ([#728](https://github.com/FS-GG/.github/issues/728)), and a worker who banks no evidence can never be one of the two the criterion needs; any engine republish also restarts the window, so the engine could not be improved while waiting for the clock that waited on the engine. Meanwhile **49 of 61 open items org-wide lived in `.github`** and arrivals ran level with closures. Decides: (1) the gate is the **defect corpus** — one case per historical defect, run against **both** engines in CI — and the shadow is demoted from gate to **telemetry**; (2) **blockers are checked before the touch-set** (the ordering decision the design doc deferred to the flip): a blocked item cannot start whatever its touch-set says, and blockers are free **board** facts while a touch-set costs a **body read** on the budget that dies first ([#418](https://github.com/FS-GG/.github/issues/418)); (3) an unread body is `Unreadable`, **not** `Undeclared`. Sweeping the corpus under `--engine=fs` produced **15 failures the shadow classifies as expected noise**, three of them real defects the flip would have shipped: a **prose blocker vanished** and its item became schedulable (`jq capture` yields *empty* on no match, so the blockers array collapsed to `[]` — [#266](https://github.com/FS-GG/.github/issues/266)'s shape, arriving through the door built to prove #266 was over); a **swept item whose body could not be read silently ceased to exist** (neither offered nor passed over with a reason); and the **lease window and holder were dropped** from every collision line, collapsing *live claim* and *batch member* — same verdict, opposite instructions ([#428](https://github.com/FS-GG/.github/issues/428)). `--engine=fs` is open; `--engine=bash` stays as a byte-identical escape hatch; every failure in `fs` is **fatal**, because falling back to bash after asking for the typed core is a silent engine substitution | Accepted |
