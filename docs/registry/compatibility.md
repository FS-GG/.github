# Contract & compatibility registry

Human projection of [`registry/dependencies.yml`](../../registry/dependencies.yml) —
the cross-repo source of truth for who depends on whom and which contract versions are
coherent. Update **both** when a versioned cross-repo contract changes (a
`contract-change` issue must do this as part of its resolution).

## Dependency graph

```text
FS.GG.Templates ──fs-gg-ui-template@0.1.55-preview.1 composed via scaffold (lifecycle=sdd); no vendored payload; root-buildable; productName-enabled; org-public; game-profile-released; symbology-skill-delivered──▶ FS.GG.Rendering
        │  ──scaffold-provider@1 + SDD skeleton──▶ FS.GG.SDD
        │  ──policy@1 / capabilities@2 / tooling@1 (populated overlay)──▶ FS.GG.Governance
        │  ──governance-reference-gate-set@1.2.1.1 (content pkg; overlay drift-gate source, Templates#14)──▶ FS.GG.Governance
FS.GG.SDD ──governance-handoff@1 (OPTIONAL)──▶ FS.GG.Governance
FS.GG.Rendering ──(depends on no FS-GG product; never depends on Governance)
```

## Versioned contracts

| Contract | Version | Owner | Surface | Consumers |
|---|---|---|---|---|
| `scaffold-provider` | 1.0.0 | SDD | `.fsgg/providers.yml` + invocation protocol (canonical product-name param `name`, ADR-0005) | Templates, Rendering |
| `fsgg-contracts` | 1.1.0 (source == pkg published) | SDD | `FS.GG.Contracts` BCL-only F# package (H2, [epic #16](https://github.com/FS-GG/.github/issues/16) Pillar 2) — `Fsgg.Schemas` (typed `.fsgg` schema records + version constants) / `Fsgg.Provider` (extended `ProviderDescriptor` + `Build/Test/Run/Verify` + canonical `NameParameter`) / `Fsgg.Registry` (`dependencies.yml` types + validator) / `Fsgg.ContractVersion`. **1.1.0**: additive typed registry validator over the real `dependencies.yml` ([SDD#26](https://github.com/FS-GG/FS.GG.SDD/pull/26), feature 042) — the source version the contract-coherence gate tracks. **Release lag closed (2026-06-28):** 1.1.0 is now PUBLISHED on the org feed ([SDD#27](https://github.com/FS-GG/FS.GG.SDD/issues/27), feature 043; feed serves 1.1.0 + 1.0.1), so `package-version` advanced `1.0.1 → 1.1.0` ([.github#47](https://github.com/FS-GG/.github/issues/47)) and consumers now resolve 1.1.0 — `package-version == version == feed == SDD source == 1.1.0`. Consumers re-type next: [SDD#9](https://github.com/FS-GG/FS.GG.SDD/issues/9), [Governance#14](https://github.com/FS-GG/FS.GG.Governance/issues/14), [Templates#13](https://github.com/FS-GG/FS.GG.Templates/issues/13). | SDD, Governance, Templates |
| `scaffold-provenance` | 1.0.0 | SDD | `.fsgg/scaffold-provenance.json` | SDD |
| `governance-handoff` | 1.0.0 (`1.x`) | SDD | `readiness/<id>/governance-handoff.json` | Governance |
| `governance-policy` | 1 | Governance | `.fsgg/policy.yml` | Templates |
| `governance-capabilities` | 2 | Governance | `.fsgg/capabilities.yml` | Templates |
| `governance-tooling` | 1 | Governance | `.fsgg/tooling.yml` | Templates |
| `governance-descriptor` | 1 | Governance | `.fsgg/governance.yml` (project descriptor / `ProjectFacts`; renamed from `project.yml`, ADR-0005) | Governance |
| `governance-reference-gate-set` | 1.2.1.1 | Governance | `FS.GG.Governance.ReferenceGateSet` **content-only** NuGet package ([Governance#15](https://github.com/FS-GG/FS.GG.Governance/issues/15), H3, [epic #16](https://github.com/FS-GG/.github/issues/16) Pillar 3) — `contentFiles/any/any/.fsgg/{governance,capabilities,policy,tooling}.yml`, byte-identical to `samples/sdd-reference-gate-set/.fsgg/` (single source, no copy); no assembly / no dependency group. Version `{governance}.{capabilities}.{policy}.{tooling}` derived from the contained `schemaVersion`s (= `1.2.1.1`; deterministic + distinguishable, [ADR-0007](../adr/0007-reference-gate-set-package-version-derivation.md)); production **gated** on the G1–G7 reference-set guard (shipped == tested). Bundles `governance-policy@1`/`capabilities@2`/`tooling@1`/`descriptor@1` as one pinnable set. Local/CI `dotnet pack` now; org-feed push deferred ([.github#21](https://github.com/FS-GG/.github/issues/21)). | Templates ([#14](https://github.com/FS-GG/FS.GG.Templates/issues/14)) |
| `fs-gg-ui-template` | 0.1.55-preview.1 (pkg published; tag [`fs-gg-ui-template/v0.1.55-preview.1`](https://github.com/FS-GG/FS.GG.Rendering/releases/tag/fs-gg-ui-template/v0.1.55-preview.1)); **root-buildable**; **org-public**; **`game` profile released**; **symbology product skill delivered** | Rendering | `dotnet new fs-gg-ui` + `FS.GG.UI.*` packages (stock `restore`/`build`/`test`/`run` at the product root). **+ additive `game` profile RELEASED (Feature 222, [#33](https://github.com/FS-GG/FS.GG.Rendering/issues/33)):** `dotnet new fs-gg-ui --profile game` selectable from the feed @ `0.1.54-preview.1` — minimal Pong-style starter, builds 0/0 + governance 26/26 zero-edit, non-game profiles byte-identical (publish-before-flip, FR-007). **+ symbology product skill delivered (Feature 223, [#35](https://github.com/FS-GG/FS.GG.Rendering/issues/35)):** `fs-gg-symbology` is now sourced into the ship list for the `fs-gg-scene` profile set (app, headless-scene, governed, sample-pack, game) on both `.agents`/`.claude` surfaces + the `fs-gg-product-symbology` consumer wrapper — `dotnet new fs-gg-ui --profile game`/`app` emits the real ~12.7 KB skill under every lifecycle; the skill-parity blind spot is closed and the Feature 219 "not-vendored" record reversed (GV-3 stays `diff-vs-today=none`). **RELEASED @ `0.1.55-preview.1`** (publish-before-flip, FR-007). **+ additive `--productName` (Feature 217, [#27](https://github.com/FS-GG/FS.GG.Rendering/issues/27)):** accepts the SDD scaffold-provider's `productName` convention as an alias of the canonical `name` (ADR-0005) — `dotnet new fs-gg-ui --productName Acme` (no `-n`) instantiates instead of exit-127; backward-compatible (absent ⇒ byte-identical, SC-003; `--productName Acme` ≡ `-n Acme`, SC-004). **RELEASED on the feed in `0.1.53-preview.1`** (Feature 218, [#29](https://github.com/FS-GG/FS.GG.Rendering/issues/29)) — verified live (clean feed install + `--productName` exit 0). **Whole set flipped private→public** (Feature 218, [#26](https://github.com/FS-GG/FS.GG.Rendering/issues/26)) so consumer-CI installs+restores without exit-103 (`internal` unavailable on the free org). Cross-ref [SDD#35](https://github.com/FS-GG/FS.GG.SDD/issues/35). | Templates, SDD |
| `shared-build-config` | 1.0.0 | .github | [`dist/dotnet/`](../../dist/dotnet/) `Directory.Build.props` + `Directory.Packages.props` + `.config/dotnet-tools.json`, synced by [`scripts/sync-build-config.sh`](../../scripts/sync-build-config.sh) ([ADR-0006](../adr/0006-org-shared-dotnet-build-config-and-unified-restore-locked-mode-gate.md), [guide](../build/README.md)). Now also carries the opt-in [`api-breaking-change-gate`](../build/README.md#the-api-breaking-change-gate-opt-in-advisory--required) (PublicApiAnalyzers + Package Validation; [.github#20](https://github.com/FS-GG/.github/issues/20)) — off by default, surface/version unchanged. | Rendering, SDD, Governance, Templates |

## `.fsgg/` slot ownership

`.fsgg/` is a **shared namespace**; a scaffolded product carries both SDD and Governance
artifacts in one directory. Ownership is an explicit contract
([ADR-0005](../adr/0005-fsgg-slot-ownership-sdd-project-governance-governance.md)) — a new
product writing into `.fsgg/` checks this map before claiming a filename, so the
`project.yml` collision class cannot silently recur.

| Slot | Owner | Schema |
|---|---|---|
| `project.yml` | SDD | lifecycle project descriptor |
| `sdd.yml`, `agents.yml`, `constitution.md` | SDD | lifecycle skeleton (constitution per ADR-0004) |
| `providers.yml`, `scaffold-provenance.json` | SDD | scaffold contracts |
| `governance.yml` | Governance | governance project descriptor (`ProjectFacts`; was `project.yml`) |
| `policy.yml`, `capabilities.yml`, `tooling.yml` | Governance | gate set config |

## Coherence state

| Id | Coherent? | Owner | Summary |
|---|---|---|---|
| `fs-gg-ui-version` | ✅ yes | Rendering | Resolved 2026-06-27: the `fs-gg-ui` template pins `FsGgUiVersion=0.1.50-preview.1` (the complete coherent 16-package `FS.GG.UI.*` set) behind the immutable tag [`fs-gg-ui/v0.1.50-preview.1`](https://github.com/FS-GG/FS.GG.Rendering/releases/tag/fs-gg-ui/v0.1.50-preview.1) (commit `57be86c`). All four profiles (app, headless-scene, governed, sample-pack) generate→restore→build→evidence→governance green; the phantom `FS.GG.UI.Color`/`FS.GG.UI.SkillSupport` pins were removed; restores are byte-reproducible (`RestorePackagesWithLockFile`). **Tracking:** [FS.GG.Rendering#1](https://github.com/FS-GG/FS.GG.Rendering/issues/1) (resolved). |
| `fs-gg-ui-bom` | ✅ yes | Rendering | Resolved 2026-06-27 (Feature 207): an **optional full-set BOM / metapackage `FS.GG.UI`** is published at **0.1.51-preview.1** alongside the 16 coherent `FS.GG.UI.*` members, behind the immutable tag [`fs-gg-ui/v0.1.51-preview.1`](https://github.com/FS-GG/FS.GG.Rendering/releases/tag/fs-gg-ui/v0.1.51-preview.1) (commit `d9f4c81`). One `FS.GG.UI@0.1.51-preview.1` reference pins the whole set (live-verified: all 16 members at V, clean build, byte-reproducible across two cache-cleared restores, preview channel). Dependencies-only (exact `[$version$]` member deps, no assembly); membership locked to the packable `FS.GG.UI.*` set by an always-on parity test. Deviation is detected both ways — `NU1605` (downgrade) / `NU1608` (upgrade) — a hard restore/build failure under `WarningsAsErrors=NU1605;NU1608`/`TreatWarningsAsErrors` (repo + governed-template default), warnings by default. **Additive/optional**: the `fs-gg-ui` template default pin stays `FsGgUiVersion=0.1.50-preview.1` (not migrated). **Tracking:** feature 207 (no separate issue). |
| `fs-gg-ui-template` | ✅ yes | Rendering | Resolved 2026-06-27 (Feature 206): the `FS.GG.UI.Template` package is **published at 0.1.50-preview.1** (was 0.1.17-preview.1), carrying the ADR-0002 `lifecycle` choice symbol (`spec-kit\|sdd\|none`) and the Feature 205 side-effect-free `initGit` opt-in (`skipGitInit` removed), over the coherent `FS.GG.UI.* 0.1.50-preview.1` set. Snapshotted behind the immutable tag [`fs-gg-ui-template/v0.1.50-preview.1`](https://github.com/FS-GG/FS.GG.Rendering/releases/tag/fs-gg-ui-template/v0.1.50-preview.1) (commit `2862caf`). Verified against the **installed** package: resolvable > 0.1.17; manifest carries `lifecycle`+`initGit`; `spec-kit` byte-identical for all four profiles; default generation side-effect-free (no `.git`); all four profiles restore→build green (0 NU1101 / 0 conflict); double-restore byte-reproducible; from-tag repack reproduces the package. **Advanced 2026-06-28 (Feature 212/215, [FS.GG.Rendering#9](https://github.com/FS-GG/FS.GG.Rendering/issues/9)):** re-released as the coherent set **0.1.52-preview.1** (tag [`fs-gg-ui-template/v0.1.52-preview.1`](https://github.com/FS-GG/FS.GG.Rendering/releases/tag/fs-gg-ui-template/v0.1.52-preview.1) over `fs-gg-ui/v0.1.52-preview.1`); the template now **guarantees a root-buildable generated product** — root `<Name>.slnx` + `net10.0` `global.json` pin + `build.sh`/`build.cmd` verb wrapper; stock `dotnet restore`/`build`/`test`/`run` at the product root, zero FAKE. Proven on the **real release** by the `release.yml` `template-product-tests` gate (load-bearing #9 evidence). **Advanced 2026-06-30 (Feature 218, [FS.GG.Rendering#29](https://github.com/FS-GG/FS.GG.Rendering/issues/29) + [#26](https://github.com/FS-GG/FS.GG.Rendering/issues/26)):** re-released as the coherent set **0.1.53-preview.1** (tags `v0.1.53-preview.1` + `fs-gg-ui-template/v0.1.53-preview.1` + `fs-gg-ui/v0.1.53-preview.1`, [release run 28404668485](https://github.com/FS-GG/FS.GG.Rendering/actions/runs/28404668485)) carrying Feature 217 `--productName`, **and** the whole set (template + 16 `FS.GG.UI.*` libs) flipped **private→public** so an ordinary consumer-CI token installs+restores+builds without exit-103. Both gates proven live for the **same** version (no half-landing, FR-004): clean feed install + `dotnet new fs-gg-ui --productName Acme` exit 0 (no 127); scaffolded product restores the `FS.GG.UI.*` set @0.1.53 from the feed and builds 0 err. `internal` was unavailable (`FS-GG` is a free/non-enterprise org) so **public** matches the already-public `FS.GG.*.Cli`. Template-released dispatch fired → [FS.GG.Templates#33](https://github.com/FS-GG/FS.GG.Templates/pull/33) pin-bump PR opened. Closes #29 + #26. **Advanced 2026-06-30 (Feature 219, [FS.GG.Rendering#30](https://github.com/FS-GG/FS.GG.Rendering/issues/30)):** the framework product-usage skills under `.agents/skills/fs-gg-*` / `.claude/skills/fs-gg-*` now **follow the product profile, not the lifecycle** — they emit under **every** lifecycle (`spec-kit\|sdd\|none`), so the recommended SDD path and the full-stack composition carry the profile-appropriate `fs-gg-*` skills (the `sdd`/`none` paths previously emitted **zero**). Only the lifecycle **workspace** (`.specify/`, constitution, agent-context tree, blanket authoring-skills copy + `speckit-*` command skills, `.template.config/generated/`, and the full-registry `docs/skillist-reference.md` catalog) stays `spec-kit`-gated; the catalog is now suppressed under `sdd`/`none` so it never dangles. **Additive / parameter-surface-neutral** (no parameter add/remove/rename): `spec-kit` is **byte-identical** for all four profiles (proven live — `diff-vs-today=none`), `sdd`/`none` gain skills with no file removed. `fs-gg-symbology` is now **vendored** — delivered to consumers in Feature 223 ([#35](https://github.com/FS-GG/FS.GG.Rendering/issues/35), @ `0.1.55-preview.1`); the earlier "not byte-equal / would change spec-kit output" note was a GV-3 misread (an ungated source is byte-identical under explicit `spec-kit` vs the no-flag default). **Advanced 2026-06-30 (Feature 220, [FS.GG.Rendering#31](https://github.com/FS-GG/FS.GG.Rendering/issues/31)):** the template gains the **additive `game` profile** (`dotnet new fs-gg-ui --profile game`) — a minimal, replaceable Pong-style starter intended as the game/rendering **default**, turning the latent game-family branch into a real path. Backward-compatible: `app` stays the explicit controls-showcase option (byte-identical), `headless-scene`/`governed`/`sample-pack` **byte-identical** (FR-007, diff-verified); the durable governance entrypoint assertion is relaxed to family-agnostic (`game → Viewer.runApp viewerOptions generatedHost`, no `-- pong` flag). **RELEASED on the feed in `0.1.54-preview.1`** (Feature 222, [#33](https://github.com/FS-GG/FS.GG.Rendering/issues/33), [release run 28468936061](https://github.com/FS-GG/FS.GG.Rendering/actions/runs/28468936061)) — verified live (publish-before-flip, FR-007): feed serves the coherent set @ `0.1.54-preview.1` (FR-001), `b78e72a` is an ancestor of the tag (FR-002), consumer `dotnet new install` exit 0 / no exit-103 (FR-003), `dotnet new fs-gg-ui --profile game` accepted → build 0/0 → governance 26/26 with zero `GovernanceTests` edits (FR-004), the three non-game profiles byte-identical to the F220 baseline (FR-005). The `app → game` **default-selection flip** is owned by the SDD scaffold-provider ([SDD#44](https://github.com/FS-GG/FS.GG.SDD/issues/44) enumerate+flip, [Templates#36](https://github.com/FS-GG/FS.GG.Templates/issues/36) governance expectations; sibling [FS.GG.Rendering#32](https://github.com/FS-GG/FS.GG.Rendering/issues/32)). ADR [`Rendering/0010-fs-gg-ui-template-default-starter`](https://github.com/FS-GG/FS.GG.Rendering/blob/main/docs/product/decisions/0010-fs-gg-ui-template-default-starter.md) (Accepted). **Advanced 2026-06-30 (Feature 223, [FS.GG.Rendering#35](https://github.com/FS-GG/FS.GG.Rendering/issues/35)):** re-released as the coherent set **0.1.55-preview.1** (tags `v0.1.55-preview.1` + `fs-gg-ui-template/v0.1.55-preview.1` + `fs-gg-ui/v0.1.55-preview.1`, [release run 28473123639](https://github.com/FS-GG/FS.GG.Rendering/actions/runs/28473123639)) **delivering the `fs-gg-symbology` product skill to consumers** (was authored-but-stranded). Proven live for the **same** version (publish-before-flip, FR-007): feed serves the coherent set @ `0.1.55-preview.1` (FR-001); the `release.yml` `template-product-tests` gate built+tested+ran the generated product green; `dotnet new fs-gg-ui --profile game`/`app` emits the real ~12.7 KB `fs-gg-symbology/SKILL.md` to **both** `.agents/skills/` and `.claude/skills/` under `spec-kit`/`sdd`/`none` (not the 506 B stub), with the `fs-gg-product-symbology` wrapper; GV-3 stays `diff-vs-today=none` every profile (Feature 219 "not-vendored" reversed); skill-parity blind spot closed (a product skill is satisfied only by its `fs-gg-product-*` alias). Closes #35. **Tracking:** [FS.GG.Rendering#9](https://github.com/FS-GG/FS.GG.Rendering/issues/9) (root-buildable), [FS.GG.SDD#1](https://github.com/FS-GG/FS.GG.SDD/issues/1) (dependent scaffold-path request — responded). |
| `governance-overlay-populated` | ✅ yes | Governance | Resolved 2026-06-28 (ADR-0002; P3 Governance + P4 Templates): Templates' `fs-gg-governance` overlay no longer ships **empty** — it carries **real gates**. Governance (P3) published the populated reference `.fsgg` gate set at `samples/sdd-reference-gate-set/.fsgg/`; Templates (P4) populated `templates/fs-gg-governance/.fsgg/` from it — `capabilities.yml` has non-empty `checks:` (build/test/evidence, each wired to a `tooling` command), `tooling.yml` has non-empty `commands:` (`dotnet-build`, `dotnet-test`, `build-evidence`). The advertised `governance-policy@1`/`capabilities@2`/`tooling@1` surfaces are now **enforced**, not just declared. **Resolved by:** [Governance#9](https://github.com/FS-GG/FS.GG.Governance/issues/9) (gate set), [Templates#9](https://github.com/FS-GG/FS.GG.Templates/issues/9) (overlay), [Templates#10](https://github.com/FS-GG/FS.GG.Templates/issues/10) (deleted `fs-gg-fullstack/` + `sync-from-rendering.sh`). **Tracking:** [.github#14](https://github.com/FS-GG/.github/issues/14). |
| `reference-gate-set-published` | ❌ no | Governance | **Producer side delivered** 2026-06-28 ([Governance#15](https://github.com/FS-GG/FS.GG.Governance/issues/15), H3, [epic #16](https://github.com/FS-GG/.github/issues/16) Pillar 3; [ADR-0007](../adr/0007-reference-gate-set-package-version-derivation.md)): Governance publishes the validated reference `.fsgg` set as the content-only package `FS.GG.Governance.ReferenceGateSet@1.2.1.1` (contract `governance-reference-gate-set`) — packs the four `samples/sdd-reference-gate-set/.fsgg/` files **in place** (single source, byte-identical), no assembly / no deps, version **derived** from the contained `schemaVersion`s (`1.2.1.1`; deterministic + distinguishable), **gated** on the G1–G7 guard so shipped == tested. A guard asserts byte-identity + content-only + derived version on the real packed `.nupkg` (CI job `reference-gate-set-pack`); the installed copy re-validates `Valid` through the real pipeline. `coherent: ❌` is a **standing request for the consumer side**: [Templates#14](https://github.com/FS-GG/FS.GG.Templates/issues/14) wires the overlay drift gate (`git diff --exit-code`) against the materialized `contentFiles/any/any/.fsgg/`. **Org-feed push deferred** (admin-blocked, [.github#21](https://github.com/FS-GG/.github/issues/21)); local/CI `dotnet pack` to `~/.local/share/nuget-local/` is the done-definition. **Tracking:** [Templates#14](https://github.com/FS-GG/FS.GG.Templates/issues/14). |
| `lockfile-restore-enforcement` | ✅ yes | Rendering | Resolved 2026-06-28 (P5 — [.github#7](https://github.com/FS-GG/.github/issues/7), epic [#5](https://github.com/FS-GG/.github/issues/5)): the `FsGgUiVersion` staleness bug class is **structurally impossible** in every consumer repo — committed `packages.lock.json` + CI `--locked-mode` + `NU1603`/`NU1608`→error — not just convention-enforced. Rendering is the reference impl (Feature 211). Others adopt the same posture gating `RestoreLockedMode` on `GITHUB_ACTIONS` (SDD forces `ContinuousIntegrationBuild=true` unconditionally, so a CIB gate would fail-closed on a fresh clone); SDD & Governance each gained their first/only per-PR CI gate. Templates' packaging project has no `PackageReference`s → empty lockfile (forward guardrail only; load-bearing `FS.GG.UI.*` enforcement for scaffolded apps stays upstream). **Resolved by:** [SDD#6](https://github.com/FS-GG/FS.GG.SDD/pull/6) (gate green), [Governance#12](https://github.com/FS-GG/FS.GG.Governance/pull/12) (gate green, full 165-project build), [Templates#11](https://github.com/FS-GG/FS.GG.Templates/pull/11) (composition green). **Unified 2026-06-28 ([ADR-0006](../adr/0006-org-shared-dotnet-build-config-and-unified-restore-locked-mode-gate.md), [.github#19](https://github.com/FS-GG/.github/issues/19)):** the gate is canonicalized to `GITHUB_ACTIONS` (superseding Rendering's lone `ContinuousIntegrationBuild` gate) and published in the org-shared [`shared-build-config`](#versioned-contracts); per-repo re-sync is sequenced as H3 follow-ups, drift caught by `sync-build-config.sh --check` (wired by the reusable contract-coherence gate, [.github#18](https://github.com/FS-GG/.github/issues/18) — **landed**). |
| `apicompat-publicapi-gate` | ✅ yes | .github | **Resolved 2026-06-28 ([#20](https://github.com/FS-GG/.github/issues/20), epic #16 Pillar 5).** A public-API breaking-change gate now **enforces** on both SDD/Rendering contract-package repos: a removed/changed public member fails CI and must force a SemVer major (which the registry ranges enforce). **Mechanism** (Governance spec 088 D1): the packages are **F#**, so the operative detector is the language-agnostic SDK **ApiCompat / Package Validation** — *not* `Microsoft.CodeAnalysis.PublicApiAnalyzers` (a Roslyn/C# analyzer that doesn't see F#; it ships in [`shared-build-config`](#versioned-contracts) under the opt-in `FsggApiGate` knob but only bites on C# packables). The committed `.fsi` (+ reflective `surface/*.surface.txt`) baselines stay the source-level record; ApiCompat runs at the I/O edge (pack vs the published feed baseline) so the pure leaves gain no new dependency. **Adoption:** [FS.GG.Rendering](https://github.com/FS-GG/FS.GG.Rendering) enforces the 17 `FS.GG.UI.*` packables vs `0.1.52-preview.1` ([#22](https://github.com/FS-GG/FS.GG.Rendering/pull/22) adopt + [#25](https://github.com/FS-GG/FS.GG.Rendering/pull/25) ratchet); [FS.GG.SDD](https://github.com/FS-GG/FS.GG.SDD) enforces `FS.GG.Contracts` vs `1.0.1` ([#28](https://github.com/FS-GG/FS.GG.SDD/pull/28) adopt + [#29](https://github.com/FS-GG/FS.GG.SDD/pull/29) ratchet). Each runs a dedicated `api-compatibility-gate` job that exits non-zero **only on a genuine break** (`NoBaselineYet`/`Indeterminate` never fail it), with `EnablePackageValidation` OFF in the normal build. Enforcement is **workflow-level** (these repos have no branch protection — same model as [`lockfile-restore-enforcement`](#coherence-state); add the job to branch-protection required checks to also hard-block the merge button). [FS.GG.Governance](https://github.com/FS-GG/FS.GG.Governance) keeps its own spec-088 maturity track (advisory → BlockOnRelease on SC-005); its packages are unpublished (`NoBaselineYet`) and out of this id's scope. **Tracking:** [.github#20](https://github.com/FS-GG/.github/issues/20). |
| `cross-repo-auto-update` | ❌ no | .github | H4 / [.github#22](https://github.com/FS-GG/.github/issues/22) (epic #16 Pillar 4): the org-owned cross-repo auto-update fabric is **authored** — both halves live in `.github`. **Push:** a reusable [`dispatch-sender.yml`](../../.github/workflows/dispatch-sender.yml) (`workflow_call`) mints a GitHub App token scoped to the target repo and POSTs `repository_dispatch {event_type, client_payload:{version,…}}` (a `GITHUB_TOKEN` can't dispatch cross-repo — research C4), so a producer release fires the consumer's auto-update workflow. **Pull:** an org-shared Renovate preset [`default.json`](../../default.json) (`extends: ["github>FS-GG/.github"]`) adds custom managers for every embedded pin the standard `nuget` manager misses — the `FsGgUiVersion` MSBuild property (→ `FS.GG.UI.Template`) and an annotation-driven manager (`# renovate: datasource=.. depName=..`, for the `FS.GG.Governance` gate-set + any other non-standard literal) — over the org GitHub Packages feed, grouping `FS.GG.UI.*` and allowing `-preview`. Provisioning ([#21](https://github.com/FS-GG/.github/issues/21)) is **done + verified**: dispatch smoke-tested end-to-end, Renovate installed + resolving the preset, `read:packages` feed-auth working (HTTP 200, Mend secret `FSGG_PACKAGES_READ_TOKEN`). NB the feed `hostRules` token lives in each repo's **own** `renovate.json` (Renovate won't substitute `{{ secrets }}` inside an `extends` preset). **Both producer halves now fire:** FS.GG.SDD release `v1.0.1` pushed `FS.GG.Contracts` 1.0.1, and FS.GG.Rendering's release (`fs-gg-ui-template/v0.1.52-preview.1`) pushed the full coherent set — `FS.GG.UI.Template` + the `FS.GG.UI.*` packages — so the **feed-presence blocker is resolved**: 19 `FS.GG.*` packages now resolve on the feed (`FS.GG.Contracts` 1.0.1 + the `FS.GG.UI.*` set incl. `FS.GG.UI.Template` at 0.1.52-preview.1); none is a 404 `no-result`. **Still ❌ for one remaining reason:** no Renovate sweep has yet resolved an `FS.GG.*` feed lookup green end-to-end and opened the auto-PR (only third-party Renovate PRs observed so far). Flips ✅ on the first green `FS.GG.*` Renovate sweep. **Guide:** [auto-update fabric](../coordination/auto-update-fabric.md). **Tracking:** [.github#22](https://github.com/FS-GG/.github/issues/22). |
| `registry-validator-typed` | ✅ yes | SDD | **Resolved 2026-06-29 ([.github#49](https://github.com/FS-GG/.github/issues/49)).** The reusable contract-coherence gate ([.github#18](https://github.com/FS-GG/.github/issues/18)) now validates this registry with the **typed** `Fsgg.Registry` validator — `fsgg-sdd registry validate registry/dependencies.yml` — instead of the Python stand-in. **Path:** SDD#26 gave `Fsgg.Registry` a real `RegistryDocument.load` (YAML) + `validateDocument` + the `fsgg-sdd registry validate` CLI (shipped in `FS.GG.Contracts 1.1.0`), [SDD#27](https://github.com/FS-GG/FS.GG.SDD/issues/27)/[SDD#31](https://github.com/FS-GG/FS.GG.SDD/issues/31) published it to the feed, and the one divergence found on first wiring — the typed grammar rejected the legitimate 4-segment `1.2.1.1` (`governance-reference-gate-set`, [ADR-0007](../adr/0007-reference-gate-set-package-version-derivation.md)-derived) that the stand-in accepted — was fixed in [SDD#32](https://github.com/FS-GG/FS.GG.SDD/issues/32) (`semVerRegex` widened to the optional 4th segment; CLI republished `0.2.1`, Contracts `1.1.1`), restoring byte-for-byte parity (verified: typed CLI → valid/exit 0 over this file). #49 pinned `FS.GG.SDD.Cli 0.2.1` in [`contract-coherence.yml`](../../.github/workflows/contract-coherence.yml), swapped the schema step onto the typed CLI, and **retired** `scripts/validate-registry.py`; the typed `fsgg-contracts` version-coupling assertion stays a dedicated step. **New requirement:** the gate restores the validator from the org feed, so every caller must grant **`packages: read`** (the `.github` self-gate is updated; the four product-repo callers adopt the same grant as a #49 follow-up). **Tracking:** [.github#49](https://github.com/FS-GG/.github/issues/49) (resolved). |

This row was exactly the failure that a cross-repo coordination mechanism exists to make
visible: a downstream consumer (Templates) discovered an upstream (Rendering)
template↔framework incoherence that had no release tag and no notification path. It was
resolved 2026-06-27 by pinning the template to a tagged, reproducible `FS.GG.UI.*` snapshot
(`fs-gg-ui/v0.1.50-preview.1`) and closing [FS.GG.Rendering#1](https://github.com/FS-GG/FS.GG.Rendering/issues/1).

## Behavioral breaks

| Contract | Break | Consumer action |
|---|---|---|
| `fs-gg-ui-template` | **Feature 205 (2026-06-27): side-effect-free generation.** The `fs-gg-ui` template no longer auto-runs git-init/chmod post-actions at generation time. `skipGitInit` (opt-out) is **removed**; `initGit` (opt-in, bool, default `false`) is **added**; default generation spawns no process, creates no repo, and never hangs in CI/IDE hosts. No emitted-file changes. Contract: [`fs-gg-ui-template-generation.md`](https://github.com/FS-GG/FS.GG.Rendering/blob/main/specs/205-scaffold-git-init-chmod/contracts/fs-gg-ui-template-generation.md) (Accepted). | **SDD scaffold path** must own repo-init + chmod as explicit post-instantiation steps (contract §5 S1–S3) and stop relying on template auto-init. Direct callers: drop `--skipGitInit true`; pass `--initGit true` (plus `--allow-scripts yes` non-interactively) to reproduce the old auto-init. |
