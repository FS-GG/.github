# UTEL-REL-02 — Native completed-item closure patch release

Owner: `.github` telemetry producer and coherent-set release. Main/SystemAdmin owns installed Host adoption.

PR #3583 merged the completion reducer fix at `75ffcdd3a4a6e0ac811a124512c3bd7956f461e4`. Its required `mutation-sweep` check passed. The immutable 0.91.0 set predates that source. This patch advances the three coherent package identities together to 0.91.1. The current public dashboard remains empty until a promoted 0.91.1 manifest is adopted by the Host and an exact eligible item receives an approved public alias.

The candidate workflow binds current `main`, exact 0.91.1 package version, the promoted 0.91.0 predecessor channel and source `85c356b2c44cfea4b83659b14b6946d64bbd9a22`, unused tags and both-feed uniqueness. It packs one three-package set and retains the exact archive. The publisher accepts only that version and independently verifies candidate run, artifact digest, current authority and uniqueness before effects. A new protected journal ref, `refs/heads/fsgg/v2/journal/release/utel-rel-02`, isolates the operation from the completed 0.91.0 journal. The authority repository's active `v2-journal-integrity` and `v2-journal-writer` rulesets include this ref pattern; only the ordinary ledger App bypasses creation/update restrictions. The existing Trusted Publishing registration is bound to the unchanged `release-successor-publish.yml` filename, release environment and three package IDs. The five legacy writers remain disabled.

Qualification sequence: source PR checks; candidate run from the final main SHA; read-only publisher preflight against that retained candidate; admitted publisher run; exact release, journal and both-feed readback; public-feed CLI installation. Main then selects only the promoted manifest, adopts the installed CLI on the protected Host and reports private completed and public eligible counts. Public label approval remains a separate user decision based on one concrete eligible item.
