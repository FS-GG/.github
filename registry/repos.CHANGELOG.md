# Repo roster changelog

Reverse-chronological log of changes to [`repos.yml`](repos.yml) — the FS-GG org repo roster (the
single authoritative list of framework repos the org fabrics iterate, ADR-0019). Its human
projection is [`../docs/registry/repos.md`](../docs/registry/repos.md).

**Protocol.** Mirrors [`CHANGELOG.md`](CHANGELOG.md) (the `dependencies.yml` log) and
[`skills.CHANGELOG.md`](skills.CHANGELOG.md): every change to `repos.yml` **prepends one dated entry**
at the top of the Entries list below (newest first) and sets the file's `updated:` date to match.
Entries follow the same loose `HEADER (owner; refs): body` grammar — name the repo id(s) and/or the
`receives` capability touched, the owner, and the issue/ADR refs. Adding or retiring a repo, or
changing a repo's `receives`, is a changelog-worthy roster change; bumping the kit `sha256` after a
skill/client edit is too.

`repos.yml` is validated by [`../scripts/repos.sh validate`](../scripts/repos.sh) (schema, the
single-authority invariant, the `receives` vocabulary, and content-addressed kit digests).

## Entries

- **2026-07-10** — Coordination-kit `fsgg-coord` sha256 bumped (@.github; [#353](https://github.com/FS-GG/.github/issues/353), [#266](https://github.com/FS-GG/.github/issues/266), ADR-0021, ADR-0027): **`widen` and `overlap --active` re-checked a touch-set against *every repo's* in-flight claims, so they invented collisions and posted false "sequence behind me" notices onto strangers' items.** `Paths:` tokens are repo-relative, but both commands asked `active_claims` with **no repo** — where `batch` scopes it to one (`active_claims "$repo"`). Comparing an item's `scripts/fsgg-coord` against another repo's `scripts/fsgg-coord` compares two strings that name files in **two different repositories**, and `conflicts_between` — which cannot see a repo — reports the shared prefix as an overlap. Reproduced live twice while widening [#331](https://github.com/FS-GG/.github/issues/331): a `.github` widen "collided" with `FS.GG.Rendering#303` on `.claude/skills`, stopping a worker who had nothing to stop for and pinning two false coordination comments onto an innocent worker's item. The failure is **fail-closed** — it never hands two workers one file — but it stops honest work, poisons the channel workers use to talk, and is **incoherent with the scheduler**: `batch` would happily run in parallel the very pair `widen` insists collides. **Fixed by scoping every comparer to one repo** — `widen` passes `"$so/$sr"` and `overlap --active` passes `"$ao/$ar"` to `active_claims`, and the pairwise `overlap a b` short-circuits to DISJOINT when its two refs resolve to different repos (the third caller that could hand `conflicts_between` cross-repo tokens) — so a cross-repo claim, which can never share a (repo-relative) file, is never in the compared set; every same-repo check is unchanged. `conflicts_between` now documents its same-repo contract so the next caller does not re-introduce the trap. Tests: a cross-repo fixture (`#401` SDD probed against `#402` Rendering declaring the same bare token, plus `#403` SDD as a real same-repo neighbour) proves `widen`/`overlap --active`/`overlap a b` report DISJOINT and leave the bystander uncommented, while every same-repo overlap still fires and still exits non-zero — 13 new assertions, 398 total green; reverting each fix reddens exactly its cross-repo assertions and none of the same-repo controls. `fsgg-coord` re-digested `9c5ca17b…` → `39af3c3d…`; no `.agents/` twin — `fsgg-coord` is `kind: client`, and only skills are mirrored. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-10** — Coordination-kit `intra-repo-parallel-work` sha256 bumped (@.github; [#360](https://github.com/FS-GG/.github/issues/360), [#346](https://github.com/FS-GG/.github/issues/346), [#345](https://github.com/FS-GG/.github/issues/345), [#266](https://github.com/FS-GG/.github/issues/266), ADR-0021, ADR-0027): **the skill taught a worker to write an epic body's child list without the rules the matcher reads it by.** [#346](https://github.com/FS-GG/.github/issues/346) taught `EPIC-UNLINKED-CHILD`/`epic_rollup` the first-ref/bare-`#n`/PR-ref rules — which token on a `- [ ]`/`- [x]` line is the declared child, how a bare `#n` resolves, and that a PR ref is not a child — and [`../docs/coordination/parallel-work.md`](../docs/coordination/parallel-work.md) §"What the body counts as a declaration" carries them for a reader of the *doc*. But `intra-repo-parallel-work/SKILL.md` §"If you filed an issue, LINK it" said only that the gate reads "`- [ ]` / `- [x]` lines naming an issue", so a worker reading the **skill** — which is most of them — could still write a body the gate reads differently than they meant: a trailing aside outranking the child, a `SDD#109` silently resolving to `.github#109`, or a `(PR #243)` reported as an unlinked child forever. This is the **third strand** of #346, deferred there (as #345 deferred it) because the SKILL.md edit is a *different* kit source — its own digest and its `.agents/` twin — so widening #346 across it would have made that PR unreviewable; filed as #360 and worked here rather than dropped. **Fixed by mirroring the three rules** into the skill as a compact subsection beneath the agreement paragraph, at the skill's altitude and pointing to `docs/coordination/parallel-work.md` for the canonical text and the live incidents. Documentation only — no protocol change, no gate change; the gate already enforces exactly what the skill now teaches. `intra-repo-parallel-work` re-digested `840ae4ba…` → `aa74ea00…`, with its `.agents/` twin re-mirrored byte-identically. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-10** — Coordination-kit `fsgg-coord` sha256 bumped (@.github; [#346](https://github.com/FS-GG/.github/issues/346), [#325](https://github.com/FS-GG/.github/issues/325), [#266](https://github.com/FS-GG/.github/issues/266), ADR-0021, ADR-0027): **`EPIC-UNLINKED-CHILD` read the PR that closed a checklist line as a forever-unlinked child, so a genuinely-complete epic went permanently red.** The [#325](https://github.com/FS-GG/.github/issues/325) gate (`lint`'s `EPIC-UNLINKED-CHILD` and the matching `epic_rollup` refusal) compares an epic's **body** task-list refs against its **sub-issue graph** and flags any body-declared ref the graph lacks. But GitHub **refuses to link a pull request as a sub-issue**, so a `- [x] … (PR #243)` line — the single most natural way to record which PR closed a checklist item — declares a ref the graph can *never* hold. The condition then has **no repair short of rewriting the body**: `coord child` 422s on a PR, and `done --flip` refuses to roll the epic up forever. Not hypothetical — of the six unlinked refs #345 had to repair, **four were PRs**; the shipped workaround was a convention (cite the `/pull/` URL, which the matcher's `/issues/`-anchored alternative already ignores) held in a human's head and enforced by nothing, so the next `(PR #243)` reddens a complete epic — and a gate red on correct work is one people learn to ignore, which is #266 arriving from the other side. **Fixed by making the gate's subject the thing it means — an *issue* ref.** A new `ref_is_pr`/`drop_pr_refs`/`prune_pr_unlinked` re-resolves each **otherwise-unlinked** ref over REST (the separate 5,000/hr budget) and drops the ones GitHub reports as pull requests (`.pull_request`); the probe hits only the already-unlinked set — typically empty — so a healthy board pays nothing, and both consumers (`lint` and `epic_rollup`) share the one classifier rather than re-deriving it. The discipline is #266's, applied one layer down to the probe itself: a ref that will **not resolve** (404, network, rate-limit) is **kept**, never dropped — *"I could not check"* is not *"it is a PR"* — so an unreachable subject still fails the gate closed. `EPIC-UNLINKED-CHILD` is left **not** scoped to open epics (the docstring now says why, answering #346's open question): unlike `EPIC-NO-CHILDREN`, a **closed** epic whose body declares an unlinked child that is *itself still open* is the one blind spot `EPIC-DONE-OPEN-CHILD` cannot cover, because that child is absent from the graph it reads — so scoping to open would fail open on the sharpest case, while the PR-skip already removes the #345 false-red class (those were all PR refs). Honest scope: the third strand of #346 — mirroring the first-ref/bare-`#n`/PR-ref rules from `docs/coordination/parallel-work.md` into the `intra-repo-parallel-work` SKILL.md — touches a **different** kit source (its own digest, its `.agents/` twin) and is filed separately rather than widened across a repo-boundary touch-set. Tests: #409's fixture now cites `PR #418` beside its genuine unlinked issue #414, proving the PR is dropped while #414 still fires and still names **only** #414; a `GH_FAIL_ISSUE_GET` leg proves the fail-closed keep; the internal `unlinked` scratch field is asserted **absent** from `--json`; and a new rollup chain (#52 → epic #55, body citing `PR #920`) proves the mirror case — an epic whose graph is complete and whose only extra body ref is a PR now **flips** where it once refused. Seven new assertions, 383 total green. `fsgg-coord` re-digested `59959d78…` → `9c5ca17b…`; no `.agents/` twin — `fsgg-coord` is `kind: client`, and only skills are mirrored. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-10** — Coordination-kit `fsgg-coord` + `intra-repo-parallel-work` sha256 bumped (@.github; [#331](https://github.com/FS-GG/.github/issues/331), [#266](https://github.com/FS-GG/.github/issues/266), ADR-0021, ADR-0027): **`release` reverted the `Status: Blocked` the protocol had just told the worker to set.** `release_cmd` defaulted `status="Ready"` and wrote it unconditionally, so the documented blocked-item sequence — `set-field <i> 'Blocked by' <ref>`, `set-field <i> Status Blocked`, `release <i>` — could not produce its own documented end state: the third line silently undid the second. The board was then left asserting `Status: Ready` on a row whose `Blocked by` named an **open** issue. Reproduced live on #325. **Honest scope**, because it is narrower than it looks: this is *not* a scheduler fail-open. `batch` derives blockedness from the `Blocked by` refs' issue state and never consults `Status`, so a `Ready`-but-blocked item was still filtered out of `batch`/`take`/`next` and no worker was ever dispatched onto blocked work. What broke is the board **as the record humans read** — a self-contradicting row that `/check-board` exists to reconcile and that `release` silently regenerated every time a worker followed the protocol, plus a `ready --status Ready` listing that nobody could start. The defect is a **conflation**: *"drop the lease"* and *"this item is startable"* are two different claims, and `release` asserted the second whenever it was asked for the first. Fixed by making the default **derived, not constant**, and by fixing it at the depth the defect actually lives: `release` is not the only way a claim goes away. **`reap` forced `Ready` just as hard** (`reap_cmd`), so a worker whose lease expired on an item it had marked `Blocked` had that column reset on its way out — the reaper collects a *lease*, and knows nothing about whether the item became startable. Both verbs now ask one shared question, `unclaim_status`: undoing a claim resets the `In progress` that `claim` itself wrote — and only that. Any other column was chosen deliberately (`Blocked`, `Backlog`, `In review`, `Done`) and is **preserved**, with no `Status` write at all rather than a redundant matching one; `--status S` still wins outright, as the caller stating the end state instead of `release` inferring it. The `[--status S]` flag ADR-0021 §84 already specified was never mentioned by any skill, which is why the docs prescribed the broken sequence in the first place. Deriving the default required reading the item's **current** `Status`, so a new `item_status` helper does exactly that in one narrow query — deliberately *not* folded into `item_id`, which caches its answer to a file, and a stale `Status` is worse than none. It also refuses to guess: **a `Status` we could not read is not a `Status` we may overwrite**, so a failed read leaves the column untouched and says so on stderr, naming the repair. That is #266's rule (*a coherence gate must fail closed when its subject is absent, stale, or unreachable*) applied one layer down, to a **writer** rather than a gate — and the trap is live in the implementation: `gql` calls `die`, but `exit` only unwinds the `$( )` it runs in, leaving `out` empty, and an empty payload yields no board node, which is byte-identical to *"not on this board"*. The read is therefore checked **explicitly** rather than left to `set -e`, or the fix would have re-enacted the bug it repairs. The lease is dropped **before** any of this: the marker is the lock, so a board that cannot be read or written never strands a claim. `intra-repo-parallel-work` §3 gains the `--status` form and states the lease/startable distinction outright, for `reap` as well as `release`. The literal `In progress` is now the named constant `CLAIM_STATUS`, so the column `claim` writes and the column undoing it resets cannot drift apart. Tests gain a per-issue `Status` read on the `gh` stub (`$STORE/itemstatus-<num>`, `GH_FAIL_ITEM_STATUS`, `GH_OFFBOARD_ITEM` — the last answering *both* the item-id and item-status queries, so the fixture cannot have them disagree about whether an item is on the board), a `Blocked` option on the fixture's `Status` field (the real board has one; the fixture did not), and twenty-two assertions covering reset, preserve-`Blocked`, preserve-`Done`, explicit-`--status`-overrides, unreadable-column, no-`Status`-set, and off-board — plus the `reap` half, whose stale marker is seeded *after* the existing reap block so it cannot perturb it — each clearing the `gh` log *after* the `claim` that writes `In progress`, so a "wrote no `Status`" assertion cannot pass for free. Every new leg was verified to actually fire by running the new fixture against the pre-fix `fsgg-coord` and watching exactly those eleven assertions go red. `pnext-item` §4's own text is **not** corrected here — that skill is introduced by the unmerged [#310](https://github.com/FS-GG/.github/pull/310) and does not exist on `main` — but with this fix its prescribed sequence now *works as written*; the wording simplification rides along with [#337](https://github.com/FS-GG/.github/issues/337). `docs/coordination/parallel-work.md:92` still says `(Status=Ready)` and is now stale; it was held by a concurrent claim ([#345](https://github.com/FS-GG/.github/issues/345)) and is tracked separately rather than fixed across a live touch-set. `fsgg-coord` re-digested `15eaa986…` → `59959d78…`; `intra-repo-parallel-work` `31f6cbb7…` → `840ae4ba…`, with its `.agents/` twin re-mirrored byte-identically. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-10** — Coordination-kit `fsgg-coord` + `intra-repo-parallel-work` sha256 bumped (@.github; [#325](https://github.com/FS-GG/.github/issues/325), [#266](https://github.com/FS-GG/.github/issues/266), ADR-0021, ADR-0027): **an epic rolled up over a child nobody had linked.** `done --flip` completes a parent epic from its **native sub-issue graph** (`subIssues`) and from nothing else — but the protocol never told a worker to create that edge. `pnext-item` §4 and this skill both prescribe `gh issue create` + `project item-add` + three `set-field` calls for a finding filed against an epic, and *not one of them links it*. A `(j) child of #266` title, a `Child of #266` comment, and a checklist line in the epic's body all **look** like a link; none is one. The failure needs only that a worker file an epic-child finding while working the epic's *last open child* — precisely when they are most likely to notice a residual instance of the epic's own pattern, because they are standing in it. It happened live: while closing #318 (child (i) of epic #266) a worker filed #322 as child (j) per §4, and `done .github#318 --flip` stamped `✓✓ FSGG-DONE .github#266 (epic) all 10 children Done + closed` **thirty minutes later**, over an open child. Fixed on both halves. (1) **A verb**: `fsgg-coord child <parent> <issue>` creates the sub-issue edge, and is the one place the endpoint's two traps now live — it keys on the child's REST **`id`**, never its issue number, and `gh api -f sub_issue_id=…` sends that id as a JSON string and is rejected **422**, so it must be `-F`. It is idempotent by id (not by number: two repos can each hold an issue #7), so a worker re-running a close-out never has to reason about whether the edge exists. The protocol now says to run it **when the issue is filed**, not at close-out — the whole failure mode is a worker who moved on. (2) **A gate**: `epic_rollup` now reads the epic's **body** as its second, human-legible record of its children — the `- [ ]`/`- [x]` task-list lines, first ref per line, so `- [x] (b) #268 — … (cf. #100)` declares #268 and not #100 — and **refuses to roll up**, naming them, when the body declares a child the graph does not contain. "All children are Done" must never be a claim about a set already known to be short; that is #266's rule (*a coherence gate must fail closed when its subject is absent, stale, or unreachable*) applied to the roll-up's own subject, the child set. The graph stays **authoritative** for what rolls up; the body is only the cross-check that it is not missing anyone. `lint` reports the standing condition as **`EPIC-UNLINKED-CHILD`**, scoped to epics whose child list is *not* truncated — where the scan cannot see every child, "unlinked" and "unseen" are indistinguishable, and a gate that guesses is the defect, not the fix. Both consumers share **one** jq `body_child_refs` definition rather than two hand-copied regexes in two engines, the drift surface `fsgg-coord` already warns about. Honest scope: the body-vs-graph gate would *not* have caught the original #322 incident, whose child was in neither record — the `child` verb is the primary fix and the gate is defence in depth against the next worker who updates the checklist and forgets the link. `pnext-item`'s own §4 text is **not** corrected here: that skill is introduced by the unmerged [#310](https://github.com/FS-GG/.github/pull/310) and does not exist on `main`; tracked in [#337](https://github.com/FS-GG/.github/issues/337). Tests gain a mutable `sub_issues` store on the `gh` stub that reproduces both API traps — it **422s** a `-f` string id, and the seeders now give every issue an `id` that is deliberately *not* its number, so a `child` that POSTed the wrong field could not pass — plus a rollup chain (#46 → epic #302) whose graph says 1/1 children done while its body declares an unlinked #47, a truncated-epic negative proving `EPIC-UNLINKED-CHILD` stays silent when it cannot know, and a prose-mention negative proving a bare `#415` outside a task-list line declares nothing. Each new failure leg was verified to actually fire by reverting the fix and watching the fixture go red — the vacuous-failure-leg check the epic's own SDD#299 finding calls for. `fsgg-coord` re-digested `34e955e5…` → `15eaa986…`; `intra-repo-parallel-work` `1ffde4dd…` → `31f6cbb7…`, with its `.agents/` twin re-mirrored byte-identically. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-10** — Coordination-kit `fsgg-coord` sha256 bumped (@.github; [#322](https://github.com/FS-GG/.github/issues/322), [#266](https://github.com/FS-GG/.github/issues/266), ADR-0021, ADR-0027): `verify-paths` **invented a `SKIP` verdict from an unanswered query**. Resolving which issue a PR implements starts at the head branch, and that read swallowed its exit status — `head="$(gh api repos/$o/$r/pulls/$pr --jq .head.ref 2>/dev/null || echo "")"`. The `|| echo ""` is there so the closing-reference fallback can run when the branch is not `item/<n>-…`, but it makes **"the API did not answer"** byte-identical to **"the branch is not an item branch"**. On a transient 502 the fallback then answers *healthily* that the PR closes no issue — the normal case, since ADR-0021's convention is the branch name, not a `Closes:` line — and `verify-paths` printed `FSGG-PATHS SKIP` and returned **0**. [`../.github/workflows/touch-set-drift.yml`](../.github/workflows/touch-set-drift.yml) classified that as `skip`, passed **green**, and *deleted* the sticky drift comment. So across a busy merge window a rate limit left the scheduler's disjointness guarantee unenforced on every concurrent PR, with nothing red and nothing commented to say so. This is child **(j)** of epic #266 — *a coherence gate must fail closed when its subject is absent, stale, or unreachable* — where the subject is *which issue the PR implements* and the failure mode is **unreachable**. It is precisely the scenario [#318](https://github.com/FS-GG/.github/issues/318) describes and **survives #318's fix**: #318 made the gate fail closed on a non-zero rc or a missing `FSGG-PATHS *` marker, which catches a *crashing* `verify-paths` but not one that reaches a *wrong verdict* with rc=0. The asymmetry was visible in the file: the changed-files call one screen down is a plain assignment, so `set -e` already fired and it *did* fail closed. **Fixed by capturing the rc at the call site** rather than `|| echo ""`-ing it away, and `die`ing when the head ref cannot be read. `die` is right **even under `--warn`**: `--warn` downgrades the *drift verdict* to advisory (ADR-0021: the touch-set is "a declaration, not an enforced boundary"), and *"I could not check"* is not a verdict — `SKIP` must mean *"I asked, and there is nothing to check."* No workflow change was needed: `touch-set-drift.yml` already keeps this rc instead of `|| true`-ing it, and now fails closed on it, which is the whole reason #318 kept it. The assignment is deliberately split from its `local` (`local head rc=0` then `head="$(…)" || rc=$?`), because `local head="$(…)"` would mask the substitution's exit status behind `local`'s own — the same swallow one layer down. Tests gain a `GH_FAIL_PR_GET=<n>` fault injection on the stub (matching the existing `GH_FAIL_ISSUE_GET` convention) and assert the reachable-vs-unreachable distinction on **both** paths: an unreachable head ref now reaches **no** `FSGG-PATHS` verdict at all and fails closed with and without `--warn`, while `--issue` still bypasses the head-ref read entirely — the fix guards the **call**, not the resolution step. Verified against the epic's own rule by reproducing the 502 end-to-end before and after (`rc=0` + `SKIP` → `rc=1` + no verdict) and replaying `touch-set-drift.yml`'s classifier over the result. `fsgg-coord` re-digested `01aefa9e…` → `34e955e5…`; no `.agents/` twin — `fsgg-coord` is `kind: client`, and only skills are mirrored. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-10** — Coordination-kit `fsgg-coord` + `intra-repo-parallel-work` sha256 bumped (@.github; [#319](https://github.com/FS-GG/.github/issues/319), ADR-0021, ADR-0027): the **isolation command the protocol prints and documents named no base ref**. `claim` emitted `git worktree add ../<repo>-<n> -b item/<n>-<slug>`, and `git worktree add -b <new>` with no commit-ish branches from the **shared checkout's `HEAD`**. That is not an edge case here: this protocol's entire premise is that N workers pass through one shared checkout, so its `HEAD` is routinely the *last* worker's unmerged branch rather than `main` — the failure gets **more likely the better the protocol works**. A worker following the printed command verbatim built its item on that branch, and `gh pr create --base main` opened a PR carrying both its own commit and every commit the branch held; [#317](https://github.com/FS-GG/.github/pull/317) landed that way with 10 files, 8 of them a sibling's. Nothing on the path warns, and the silence is the point: the item's declared `Paths:` were *honest*, the **branch base** was not, so every touch-set check the org built was looking in the wrong place. `verify-paths` is the only thing that sees the result, it is advisory by decision (ADR-0021: the touch-set is "a declaration, not an enforced boundary"), and it sees it **only while the sibling branch stays unmerged** — once that branch lands, GitHub recomputes the PR diff against the new base and the foreign commits vanish from it entirely. [#318](https://github.com/FS-GG/.github/issues/318) compounds this: a crashed `verify-paths` currently reads as `skip` and passes green. Fixed at every site the command is emitted or documented — `claim`'s `isolate:` line, the `intra-repo-parallel-work` skill (§3, plus the loop synopsis), and [`../docs/coordination/parallel-work.md`](../docs/coordination/parallel-work.md) (§2 and the fan-out recipe) — each carrying the *why*, because an invisible failure needs its reason to travel with the command. `origin/main` is safe org-wide: no repo declares a default branch, and `coordination-propagate.yml` already hardcodes `--base main` for every receiver. A stale `origin/main` merely bases the branch on an older ancestor of `main`, which rebases cleanly — strictly the lesser evil. **The test was never a guard**: `assert_contains "… -b item/70-scene-graph"` is a *prefix* of the base-less command, so it passed for exactly as long as the bug existed. The base ref now gets its own assertion, on the trailing commit-ish rather than on the branch name, and mutation-checked — reintroducing the bug turns the fixture red while the old assertion stays green. Skills re-digested `5b4c1ca6…` → `1ffde4dd…` and `fsgg-coord` `fefa8882…` → `01aefa9e…`; `.agents/` twin mirrored byte-identical. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate. Not fixed here: `.claude/skills/pnext-item/SKILL.md` documents the same base-less command, but it exists only on the unmerged [#310](https://github.com/FS-GG/.github/pull/310) and is outside this item's touch-set — flagged on that PR so it lands fixed rather than reintroducing this on merge.

- **2026-07-10** — Coordination-kit `cross-repo-coordination` sha256 bumped (@.github; #303): the board grew a **`P6 Game`** and a **`P7 Audio`** `Phase` option, and the skill's field table was **three ways wrong** about the board it documents. `Repo Scope` was called `Repo` — a name that collides with Projects' own built-in `Repository` column, so the reader who went looking for the field found the wrong one. `Phase` still listed six options; `Repo Scope` had gained `game` and `audio` but `Phase` had not, which made *"set `Phase` … on **every** item"* unsatisfiable for the two newest repos: FS.GG.Game#31–#35 were filed with `Phase` deliberately unset, and `P1 Rendering` sat there as the tempting wrong answer for a `Game.Core` item. `P6` was already load-bearing in prose — [`dependencies.yml`](dependencies.yml) names *"the P6 game scaffold provider"* and *"retired at P6"* — so the option set now matches the text that assumed it. The skill's frontmatter `description` also named only four repos, so a Game or Audio agent reading the skill index would conclude the protocol did not govern its repo — the same blind spot, one layer up, at discovery rather than at the field; it now names all six product repos. Skill re-digested `d75b804c…` → `00adaed7…`; `.agents/` twin mirrored byte-identical. Also **backfilled `Phase` on all 12 unphased game/audio board items** (11 × `P6 Game`, 1 × `P7 Audio`), which was every unphased item in those two repos, not just the five the report named. The skill gains a warning earned in the doing: `updateProjectV2Field` is the only scriptable way to add a single-select option, its option input carries **no id**, and it therefore **recreates every option and clears the field on every item** — it blanked `Phase` on all **374** items that had it, restored from a pre-mutation snapshot and verified equal. Roster unchanged (7 repos, 3 kit items). Propagates to `coordination-kit` receivers via the `coordination-coherence` gate. The report's fourth ask — FS.GG.Game (**and** FS.GG.Audio, which it missed) lacking a `roadmap` label, so the board's auto-add cannot pick them up — is **not** handled here: `roadmap` is absent from the shared set in [`../scripts/apply-labels.sh`](../scripts/apply-labels.sh), making it a change to the `labels` capability across all 7 rostered repos, on a different touch-set. Split to [#305](https://github.com/FS-GG/.github/issues/305), which also records that the `label:roadmap` scoping of the board's auto-add is **asserted, not verified** — Projects v2 does not expose a built-in workflow's filter query via GraphQL.

- **2026-07-09** — Coordination-kit `cross-repo-coordination` sha256 bumped (@.github; #283, #279): the **publish-before-flip runbook understated the registry-flip touch-set**. Step 2 named three files — `registry/dependencies.yml`, `registry/CHANGELOG.md`, `docs/registry/compatibility.md` — then closed with *"validate with the typed validator — the SAME one the gate runs."* That reads as **sufficient**. It is not: `registry/dependencies.yml` is precisely the [`../.github/workflows/architecture-map.yml`](../.github/workflows/architecture-map.yml) reconcile trigger, so a flip must also touch `docs/architecture.md` or carry the `architecture-map: unaffected` opt-out. And neither `fsgg-sdd registry validate` nor `check-feed-coherence` inspects the map — or the projection; both are CI-only gates, so a green validator is **not** a green PR. Following the recipe exactly is what reddened #279's first push while flipping `fs-gg-audio` to stable (`::error::docs/architecture.md was not reconciled.`), the fourth file discovered from a gate error. A papercut anywhere else; here it lands on the artifact people follow while doing a rare, ordered, partly-irreversible operation — the moment you least want an undocumented step. Step 2 now names four files, explains why the trigger fires on a registry flip, points routine version bumps at the opt-out, and states the validators' limits outright; [`../docs/coordination/README.md`](../docs/coordination/README.md) gains the same four-file list, cross-referencing the *System overview → the architecture map* section it already carried rather than restating the gate twice. Documentation only — no protocol change, no gate change. Re-digested skill `436357f7…` → `d75b804c…`; `.agents/` twin mirrored byte-identical. Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-09** — Coordination-kit `fsgg-coord` + `intra-repo-parallel-work` sha256 bumped (@.github; #277, ADR-0021, ADR-0027): a **quoted `Paths:` line was read as a declaration**. `paths_from_body` grepped `^[[:space:]]*[Pp]aths:` across the *entire* issue body, **fenced code blocks included**. So an issue that merely *quotes* a touch-set — in a reproduction, in prose, in a suggested `widen` command — silently acquired it as its own; and because every match is unioned, a body carrying a real declaration *and* a quoted one reserved both. (The union is not the defect and is kept, deliberately — see below; reading the *quotation* is.) This is the sibling of #273, one turn further in, and strictly the more dangerous of the two: an **unmatchable** token (`**/foo`) reserves *nothing* and is detectable because no token can match, which is why #273 could be refused by naming it; a **fabricated** token reserves the **wrong files**, confidently, and passes every check #276 added, because `invalid_paths` sees only well-formed exact paths. #261 is the live instance — its only `Paths:` line sits inside a fence quoting `FS.GG.Rendering#186`, so it reserved *Rendering's* files and **none of the file it edits** (`scripts/fsgg-coord`); #276 rejects it only *by accident*, via a stray `**/packages.lock.json` in the quote. It is a bug that cannot be written up without triggering it: #273 infected itself with its own reproduction.

  **Fixed by stripping code blocks before scanning** — fenced (` ``` ` and `~~~`, honouring the fence character, its length, and markdown's ≤3-space indent bound) and indented, since markdown's own rule settles that case: a `Paths:` line indented 4+ spaces *or a tab* is a code block, so a declaration is one indented at most 3 (a list-nested declaration keeps working). That last part is a **behaviour change** — the old `^[[:space:]]*` matched a tab-indented line — and it fails *closed*: such an item now declares nothing and is loudly refused as undeclared rather than mis-scheduled. A body whose only `Paths:` line is fenced likewise declares **nothing**, the fail-**closed** direction epic #266 requires: unschedulable beats mis-scheduled.

  **What survives the strip is UNIONED, not `head -1`ed — deliberately against #277's suggested fix**, and this is the substantive judgement in the change. Both stop a *fenced* quote from widening a real declaration, but only because the strip already deleted it; they differ on a quote that **escapes** the strip — a bare one at column 0, which #277 itself records as how "#273 infected itself with its own reproduction". Take the first line and you reserve the **quotation** and silently **drop the real declaration beneath it**: *under*-reserving, so two workers are told `DISJOINT` on a file they both edit. That is ADR-0021's own failure mode — precisely what this file exists to prevent, and what a fix for #277 must not reintroduce. `main` unions today and therefore over-reserves, which is safe; `head -1` would have made this PR a net regression on the one axis that matters. Unioning keeps the **superset**: the cost is a false `OVERLAP` — loud, investigable, spending only parallelism. Over-reserve on ambiguity; never under-reserve. All three tests #277 asked for pass under the union (its own quotations are fenced), so the deviation is invisible to the issue's acceptance criteria and visible only in the failure mode. `widen` **collapses** the duplicates to one, keeping reader and writer coherent — replacing only the first would leave the rest reserving their old tokens, i.e. a widen that did not widen to what it printed.

  **The writer had the same bug, found in review and fixed here.** `widen` rewrote the first `/^[ \t]*[Pp]aths:/` line *anywhere* in the body. Had only the reader learned about fences, `widen` would have patched a quotation inside a code block, left the real declaration standing, and printed a confident `widened … → Paths: …` for a touch-set the reader would never see — and on a body whose only `Paths:` line is fenced (the #261 shape, i.e. exactly the item a worker is trying to *repair*), it would overwrite the quote and leave the item still declaring nothing. Reader and writer now share one `FENCE_AWK` tracker and one `^ {0,3}[Pp]aths:` bound, so `widen` patches precisely the line `paths_from_body` reads, and **appends** one when no declaration exists.

  **Two more the guard nearly shipped**, both in that new writer, both now pinned by an assertion that fails without its fix. **(i)** An **unclosed fence** swallows the rest of the body, so there is no declaration to patch and `widen` appends — *into the still-open fence*, where the reader cannot see it. It printed `widened …` and `DISJOINT` over an item that declared nothing, on precisely the body shape `widen` exists to repair. The fence is now closed before the declaration is appended. **(ii)** `awk -v repl=…` applies **escape processing** to its value, so a token holding a backslash was PATCHed as something other than the string `die_on_invalid_paths` validated and `widen` echoed back: `src/a\tb` became a literal tab, which the reader's `tr` then split into two tokens. `repl` now reaches awk through the environment (`ENVIRON`), which does not escape.

  Grammar and the declaration rule stated in `SKILL.md` §1, both skill roots, and [`../docs/coordination/parallel-work.md`](../docs/coordination/parallel-work.md) §3, including the indent bound and the advice to quote in a **fence**, never bare. Fixture `tests/fsgg-coord/run.sh` grew a `#277` section (**26 assertions, 324 total**), pinning the three cases the issue named — a fenced line is not a declaration, a real-plus-quoted body takes the real one, an all-valid-token quotation reserves nothing (the fail-open asserted directly: `overlap` must never answer `DISJOINT`) — plus `~~~` fences, the 4-space bound and its ≤3-space complement, the bare-quote-above-the-declaration case that `head -1` would have under-reserved, all four halves of the `widen` writer (patch, append, collapse, close-the-fence), and `batch` passing a fenced-only candidate over with its reason. [`../.github/workflows/touch-set-drift.yml`](../.github/workflows/touch-set-drift.yml) needed no change: it shells out to `verify-paths` rather than carrying a second `Paths:` regex, so it inherits the fix — the drift surface #276 consolidated away. **Housekeeping:** #261 still declares another repo's files and must re-declare its touch-set before anything is scheduled against it. Re-digested client `4a20e239…` → `fefa8882…` and skill `a5a345e4…` → `5b4c1ca6…`. Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-09** — Coordination-kit `fsgg-coord` + `intra-repo-parallel-work` sha256 bumped (@.github; #273, ADR-0021, ADR-0027): the **`Paths:` touch-set failed OPEN**. The skill documented the line as "comma- or space-separated **globs**"; `paths_from_body` strips a *trailing* `/**` or `/*` and nothing else, and `conflicts_between` compares tokens only by equality and subtree containment. There is no glob matcher anywhere on that path — so a leading `**/`, or any interior `*`, survived normalization as a literal path segment and matched **no file, ever**. And a token that matches nothing **conflicts with nothing**: `overlap` printed `DISJOINT`, `widen` reported it had cleared every in-flight claim, and `batch`/`take` handed two workers items whose real files overlapped completely. Observed on `FS.GG.Audio#4`, where `Paths: **/packages.lock.json` was simultaneously "disjoint from everything" and "covers none of the seven lockfiles it names" (`verify-paths --pr 5` → DRIFT). This is **ADR-0021's own defect one level down** — a lock that *succeeds* under exactly the conditions it exists to prevent — in the touch-set rather than the claim, and it is the same class as .github#257 (in-flight work invisible to the scheduler).

  **Fixed by failing closed, not by growing a glob engine.** An unmatchable token is now treated as an **unknown** touch-set — the state `paths_of` already refuses to schedule against when a body read fails — rather than as an empty one. `claim` refuses the lock (holding an item that reserves nothing makes your files invisible to every peer's overlap check); `widen` rejects **before** the PATCH, so the old, still-reserving declaration survives a rejected widen; `overlap` exits **2** (undeclared) and never prints `DISJOINT`; `batch` passes an unmatchable *candidate* over with its reason, and **refuses outright** when an item already *held* declares one, since that holder's real surface is unknowable; `verify-paths` gained a fourth verdict, `FSGG-PATHS INVALID`, because under a broken touch-set every changed file is trivially "outside" it. Items with **no** `Paths:` at all stay claimable and merely unschedulable, exactly as before — declaring nothing differs from claiming to have declared. Option (b) of #273 (implement the documented grammar) is deliberately **not** taken: a real pattern-vs-pattern overlap test is the hard half, and the fail-open hole closes without it.

  **Three fail-opens the guard itself nearly shipped**, caught in review, each now pinned by a test that fails without its fix. **(i)** `claim` validated via `die_on_invalid_paths "$(paths_of …)"` — but `paths_of` fails closed by calling `die`, and an `exit` inside `$( )` kills only the substitution subshell. A transient body-read failure therefore printed *"refusing to schedule against an unknown touch-set"* on stderr and then claimed the item anyway, exit 0. Assign first, then validate; `overlap` (`pa="$(paths_for_ref …)"`) already had this right. **(ii)** `widen` screened its **own** new tokens but not the in-flight claims it re-checks *against*, so a **legacy** holder declaring `**/…` still cleared it — the identical fail-open, surviving on the one command whose entire job is to shout about collisions. It now refuses to answer (exit 2) while still letting the re-declaration land, because widening is how a bad declaration gets **fixed**, including on a held item. **(iii)** [`../.github/workflows/touch-set-drift.yml`](../.github/workflows/touch-set-drift.yml) classified only DRIFT/OK/skip: `FSGG-PATHS INVALID` fell through to `else → skip`, which **deletes** the sticky comment and passes green, and the comment step would then have rendered it with the `✅ stays inside` branch. Fourth verdict added, matched *before* the fallback, with its own ⛔ comment.

  Grammar now stated honestly in `SKILL.md` §1, both skill roots, and [`../docs/coordination/parallel-work.md`](../docs/coordination/parallel-work.md) §3: exact paths and directory prefixes, trailing `/**` / `/*` only. Fixture `tests/fsgg-coord/run.sh` grew a `#273` section (**40 assertions, 298 total**); against the pre-fix client 15 of them fail, including "`#300` vs `#301` is NEVER reported DISJOINT", and each of the three review fixes above is likewise covered by an assertion that fails when its guard is removed. The unmatchable-token predicate lived in **four** hand-copied forms across two engines (one `grep -E` and three inline `jq` regexes); the jq copies are now a single `claims_with_unmatchable_paths` filter, since a later widening of one char class that the copies did not follow would silently reopen the fail-open on exactly the in-flight-claim guards that need it most. Consolidating surfaced that **two of those three guards had no test at all** (the `batch` assertion was reaching the per-candidate `grep` path instead) — 7 assertions now go red when the shared filter is neutered. Re-digested client `447a330c…` → `4a20e239…` and skill `79c54b3d…` → `a5a345e4…`. Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-09** — **`schemaVersion` 1 → 2**: new top-level **`outside-fabric:`** opt-out list, and a gate that makes the roster's closed-world assumption checkable (owner github; refs #269, epic #266, ADR-0019). The roster is the list every org fabric iterates, so a repo absent from it receives nothing — no labels, no coordination kit, no audit — and *nothing could say so*: `coordination-sync --check --repo <full>` exits **0** for a repo that does not `receives: coordination-kit`, which is correct for a repo deliberately outside the fabric and catastrophic for one accidentally outside it. The two were indistinguishable to every gate we had, which is why `FS.GG.Audio` (the entry below) sat outside the roster for weeks while `repos-audit` — which only iterates repos *in* the roster — reported green. New [`../scripts/check-roster-closure.py`](../scripts/check-roster-closure.py) asserts closure from both directions: **(A)** every `dependencies.yml` `repos:` participant has a row here (offline, and the cheap invariant Audio violated), and **(B)** every repo live in the GitHub org (`GET /orgs/FS-GG/repos`) is either rostered or carries an explicit `outside-fabric:` row. Per epic #266's rule it **fails closed**: an errored, empty, or too-narrow org listing is an error, never a skip — in particular a listing that omits a repo we already roster proves the caller cannot see the whole org, so "not in the listing" cannot mean "does not exist". Exemptions are reviewed claims, not mute buttons: `reason` is required, a repo may not be both rostered and exempt, an exemption naming a repo that no longer exists fails as stale, and nothing is auto-exempt (archiving a repo must never be a way out of the gate). `outside-fabric:` ships **empty** — the org's seven live repos are all rostered. Wired into `coherence.yml` (every PR + push to `main`), fixture `tests/roster-closure/run.sh` (15 assertions); `scripts/repos.sh validate` grew the offline shape checks. Projection [`../docs/registry/repos.md`](../docs/registry/repos.md) updated in the same change.

- **2026-07-09** — ADD repo **`audio`** (`FS-GG/FS.GG.Audio`, role `framework`, `receives: [labels, coordination-kit]`) — the org's **seventh repo, never rostered** (owner github; refs ADR-0023, ADR-0019). `FS.GG.Audio` was transferred into the org and registered in [`dependencies.yml`](dependencies.yml) as a contract owner (`fs-gg-audio`, consumed by rendering) when it cut `0.1.0-preview.1`, but nobody added it to **this** roster. Consequences, all silent: `coordination-propagate` never opened a kit PR against it, `apply-labels.sh` never gave it the shared labels, and — because the coherence gate's `--repo` roster check exits 0 for a non-receiver — **no gate could ever report the absence.** Its kit is *missing*, not drifted: no `scripts/fsgg-coord`, no `cross-repo-coordination` or `intra-repo-parallel-work` under either skill root. Found while reconciling kit drift across the org after the SDD `v0.9.0` release; the other five receivers had merely drifted (their propagate PRs sat open and unmerged) and were re-synced by merging those. **No `schemaVersion` bump** — a row, not a shape change. `scripts/repos.sh validate` → green; projection [`../docs/registry/repos.md`](../docs/registry/repos.md) updated in the same change. Audio still needs its own thin `coordination-coherence.yml` caller before the pull gate applies to it — `coordination-sync` manages the skills + client, not workflows — and `coordination-propagate` fires only on pushes touching kit paths, so this roster row does **not** self-trigger it; dispatch the workflow, or seed the kit with `scripts/coordination-sync <checkout>`.

- **2026-07-09** — Coordination-kit `fsgg-coord` + `cross-repo-coordination` sha256 bumped (@.github; #259, #254): `lint`'s `EPIC-NO-CHILDREN` invariant is now scoped to **open** epics. It had fired on any `[epic]` card with zero sub-issues regardless of state, which on the live board meant three errors against three **closed** epics (`.github#234`, `FS.GG.SDD#159`, `FS.GG.SDD#160`) — keeping `lint` permanently red and burying its one genuine finding (`EPIC-DONE-OPEN-CHILD .github#235`). Neither harm the rule names survives closure: rollup is how an epic *reaches* `Done`, and `next` hands out only open `Ready`/`Backlog` cards. `EPIC-DONE-OPEN-CHILD` is deliberately left unscoped — a closed epic over an open child is the sharpest form of *that* bug. Re-digested client `220e8822…` → `447a330c…` and skill `2da47871…` → `436357f7…`. Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.

- **2026-07-09** — Coordination-kit `fsgg-coord` sha256 bumped (@.github; #257, ADR-0021, ADR-0027): in-flight work is now found by its **claim marker**, not the board's `In progress` column. `claim` writes that column strictly best-effort — a Projects v2 5xx is swallowed, and an item never added to the board has no column at all — so `who`/`reap`/`inbox` went blind to real claims, and `batch` never RESERVED an off-board claim's `Paths:` touch-set, which let the scheduler double-book overlapping work. `active_claims` now unions the column (the only source that can report `UNCLAIMED`) with every open issue carrying a live marker, found via a paginated, uncached issue scan pruned soundly on `comments > 0`. `reap` also stopped reporting a board reset it never performed. Re-digested client `4844a2ca…` → `220e8822…`. Propagates to `coordination-kit` receivers via the `coordination-coherence` gate.
- **2026-07-06** — New framework repo `game` added to the roster (@.github; ADR-0022): seed the roster row `{ id: game, full: FS-GG/FS.GG.Game, role: framework, receives: [labels, coordination-kit] }` for the sixth platform component — the extracted game subsystem (`FS.GG.Game.Core` BCL-only sim + `FS.GG.Game.Render` Scene adapter). `game` participates in `labels` and the `coordination-kit` distribution like the other framework repos. The repo itself is created in epic phase P2; the row lands ahead of it per the publish-before-flip sequence. Phased plan: `docs/reports/2026-07-06-extract-fs-gg-game-component-sdd-driven.md`.
- **2026-07-06** — Coordination-kit gains skill `intra-repo-parallel-work`; `fsgg-coord` sha256 bumped (@.github; ADR-0021): added the **inner-repo sibling** of `cross-repo-coordination` — a protocol for running multiple workers in parallel on different items *inside one repo* via a **claim** lock (assignee + `Status: In progress`), one **git worktree** per item, and a declared **`Paths:` touch-set** with an overlap check. New kit skill row `intra-repo-parallel-work` (`a952f74a…`); `fsgg-coord` grew `claim`/`release`/`overlap` subcommands, re-digested `b49eebc5…` → `a0c44984…`. `coordination-sync` now materializes every `kind: skill` kit row (was cross-repo-only). Propagates to all `coordination-kit` receivers via the `coordination-coherence` gate.
- **2026-07-05** — Coordination-kit `cross-repo-coordination` + `fsgg-coord` sha256 bumped (@.github; ADR-0019): added an **earned done-stamp** — a `fsgg-coord done <issue> [--pr N] [--flip]` subcommand that verifies an item is finished (closing PR merged **and** board `Status: Done`) in one thrifty query and prints a greppable green `FSGG-DONE` / red `FSGG-NOT-DONE` two-line stamp. `--flip` sets `Status: Done` after re-confirming the merge and **rolls the completion up the parent-epic chain**, flipping+stamping each epic whose children are now all `Done` (transitive, bounded). Plus a *Signal an item is finished* skill section wiring it into the release dance's "Land + record" step. Re-digested skill `30946d20…` → `dcb8dfd3…` and client `3ac2eb42…` → `b49eebc5…`. Propagates to `coordination-kit` receivers.
- **2026-07-04** — Coordination-kit `cross-repo-coordination` sha256 bumped (@.github; ADR-0003, ADR-0019): the kit `SKILL.md` carried the pre-rename contract id `fs-skia-ui-version`; corrected to `fs-gg-ui-version` (ADR-0003) in both skill roots, re-digesting the kit `460542a2…` → `9ff9ed86…`. Propagates to `coordination-kit` receivers; unblocks Governance's ADR-0003 rename guard (Governance#90).
- **2026-07-04** — Roster registry established (@.github; ADR-0019, slice 1): seed `repos.yml` with
  the five framework repos (`.github` authority + `sdd`/`rendering`/`governance`/`templates`), the
  `labels` + `coordination-kit` capabilities populated, and the coordination kit
  (`cross-repo-coordination` skill + `fsgg-coord` client) declared content-addressed. Migrated
  `scripts/apply-labels.sh` off its hardcoded array to `repos.sh list --receives labels`. The
  `build-config` / `lockfile-sync` / `contract-coherence` capabilities are reserved vocabulary,
  populated when each fabric is migrated in a follow-up PR. Kit sync + coherence gate land in slice 2.
