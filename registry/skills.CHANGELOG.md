# Skill registry changelog

Reverse-chronological log of changes to [`skills.yml`](skills.yml) — the FS-GG org skill
registry (the authoritative catalog of process + product skills, ADR-0017). Its human projection
is [`../docs/registry/compatibility.md`](../docs/registry/compatibility.md) (§Versioned contracts →
`skill-registry`, §Coherence state → `skill-registry-published`).

**Protocol.** Mirrors [`CHANGELOG.md`](CHANGELOG.md) (the `dependencies.yml` log): every change to
`skills.yml` **prepends one dated entry** at the top of the Entries list below (newest first) and
sets the file's `updated:` date to match. Entries follow the same loose
`HEADER (owner; refs): body` grammar — name the skill id(s)/scope touched, the owner, and the
issue/ADR refs. `skills.yml` is **generated/reconciled from the producer skill-manifests**, so an
entry should say which producer manifest (and version/PR) a row was reconciled from.

`skills.yml` is a governed contract (`skill-registry`, ADR-0015): schema growth (a new field or a
tightened rule) is a tracked `contract-change` — bump `skills.yml` `schemaVersion` + the
`skill-registry.version` in `dependencies.yml`, and advance the gate's `FS.GG.SDD.Cli` pin.

**An OPTIONAL, ADDITIVE field IS schema growth** (decided [.github#686](https://github.com/FS-GG/.github/issues/686)):
ADR-0015's motivating defect was additive growth under "additive tolerance", so *"no reader breaks"*
is the **mechanism** of the defect, not an exemption from it. The test is *does the on-disk schema now
carry a field or rule the pinned validator does not know*.

ADR-0015 §3 asks for that bump **in the same PR that teaches `Fsgg.Registry`**. **That PR cannot
exist** — this document lives in `.github` and `Fsgg.Registry` lives in **FS.GG.SDD**, and no PR spans
two repos ([.github#689](https://github.com/FS-GG/.github/issues/689) amends the ADR). The real
sequence is **publish-before-flip** (FR-007), two ordered PRs: **(1)** FS.GG.SDD teaches
`Fsgg.Registry` and **publishes** a CLI carrying it; **(2)** `.github` **then** bumps both scalars and
advances the pin to that CLI.

## Entries

<!-- Prepend new entries here, newest first:
- **YYYY-MM-DD** — HEADER (owner; refs): body
-->

- **2026-07-31** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s) and 0 materializes-when value(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-31** — BOUND board review at three rounds and roadmap critique at ten; stage `FS.GG.Kit` 0.26.0 + `FS.GG.Drivers` 0.12.0 (owner `.github`; tags `kit/v0.26.0`, `drivers/v0.12.0`): `pnext-item`, `work-board`, and `drive-board` permit at most three numbered same-critic repair-and-confirmation rounds with an ordered URL/SHA chain. A third confirmation that still carries material findings produces a durable escalation marker, adds the machine-readable `Blocked on: human/action` sentinel plus provenance, parks the item `Blocked`, releases the claim, and forbids a fourth automated round or merge until a human explicitly retires the park. `work-roadmap` separately permits ten exact-SHA milestone repair/confirmation rounds; a failed tenth records terminal human escalation, leaves the milestone unchecked and unmerged, and forbids round eleven. Its critique artifact advances to schema v2 with an ordered reviewed-commit chain. The normal/best routing variants inherit the complete canonical board workflows unchanged. Both authored roots and producer manifests are re-digested; registry = manifest = bytes.

- **2026-07-31** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s) and 0 materializes-when value(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-31** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s) and 0 materializes-when value(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-31** — RELEASE independent item critique in `work-board` and the shared `pnext-item` contract as `FS.GG.Kit` 0.25.0 + `FS.GG.Drivers` 0.11.0 (owner `.github`; [PR #2062](https://github.com/FS-GG/.github/pull/2062)): every board item now pauses on an exact-SHA review by a fresh non-editing critic, retains the implementer for one normal repair round plus blocker-only recovery, and requires same-critic confirmation. Review-discovered root-cause search, dedupe, and filing belong exclusively to the critic. A new item is permitted only for evidence-backed material impact to acceptance, correctness, compatibility, security, releaseability, architecture, or measured recurring operational cost; style, preference, speculation, optional cleanup, and already-repaired observations cannot create issues, board rows, blocker edges, or private follow-ups. `drive-board` and `work-board` reserve critic capacity and verify the durable PR marker, exact SHAs, independence, dispositions, and filed rows against GitHub. Tags `kit/v0.25.0` and `drivers/v0.11.0` both resolve to merge `e2c67535b6eea5025ed44db1ee1db1ee6a36713e`; release runs [30620700927](https://github.com/FS-GG/.github/actions/runs/30620700927) and [30620701624](https://github.com/FS-GG/.github/actions/runs/30620701624) completed green and created GitHub Packages records `1085227111` and `1085226535`. Independently downloaded GitHub Packages / nuget.org archives have byte-identical normalized payloads after excluding only nuget.org's `.signature.p7s`: Kit has 38 entries (archive SHA-256 `f44dcec8c8ed04506d1571dc3aa2a67839624ae5d83f858cffd870e713435ec1` / `e8f391d1ad9c6d8469ff13d95c439d23157eac87b68d2eec23d20261770eee1`) and Drivers has 30 (`e1fb24cc5c95765d87220e0da80bd0018d2d8fc78627048db6b9009fbc34d993` / `a63f6e585d8cfa6df1083bd261039482bba245d95dbeb6319423bd3645579fd7`); the new review contract and updated `work-board` skill are present. Both authored roots and producer manifests are re-digested; registry = manifest = bytes.

- **2026-07-31** — ADD bounded independent critique to `work-roadmap` + stage `FS.GG.Drivers` 0.10.0 (owner `.github`; `drivers/v0.10.0`): every roadmap milestone now receives one fresh artifact-only critic after its first green implementation/test/evidence loop. The implementation worker must resolve blocker/major findings, route minor follow-ups durably, and obtain same-critic confirmation; repair is capped at one normal round and one blocker-only recovery round. A schema-v1 JSON artifact binds scope, findings, dispositions, repair count, and reviewed commits, and the new `validate-critique-state.py` makes worker and host acceptance fail closed on missing, malformed, wrong-cycle, unevidenced, over-repaired, or unresolved state. Feedback remains a separate development-system channel. Both authored roots are byte-identical, the complete driver tree is re-digested in `registry/driver-skill-manifest.json`, and the additive driver release advances the stable package 0.9.0 → 0.10.0 for dual-feed publication and subsequent SDD pin adoption. registry = manifest = bytes.

- **2026-07-31** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s) and 0 materializes-when value(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-30** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s) and 0 materializes-when value(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-29** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-29** — RETIRE ADR-0022 §6 frozen-mirror classification (owner `.github`; [.github#1862](https://github.com/FS-GG/.github/issues/1862), [Rendering#1147](https://github.com/FS-GG/FS.GG.Rendering/issues/1147)): Rendering removed the four second copies, so the registry and Game producer rows no longer carry `mirrored`; the checker, autofix classification, workflows, tests, and live documentation no longer maintain a mirror-verdict or byte-comparison finding family. The Game bodies retain their existing authoritative owner, source, digest, and predicate. Removing the optional field does not change schema version 3 or its active scope vocabulary. registry = manifest = bytes.

- **2026-07-29** — ADD `lane-steward` and `publishing-and-deployment` operator skills (owner `.github`; [.github#1531](https://github.com/FS-GG/.github/issues/1531)): the `.github` producer manifest now declares both never-materialized operator skills (`materializes-when: "false"`), and the registry rows were reconciled from that manifest. The generator also closes its directory-classification set, with `spectre-console` retained as the explicit repo-native exemption. registry = manifest = bytes.

- **2026-07-29** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-28** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-27** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 4 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — **`fs-gg-playtest` production-journey digest and Skills 0.5.0 publication** (owner `fs-gg-game`; refs [Game#507](https://github.com/FS-GG/FS.GG.Game/issues/507)/[PR#508](https://github.com/FS-GG/FS.GG.Game/pull/508), [.github#1484](https://github.com/FS-GG/.github/issues/1484)). The Game producer manifest at merge `71d5fc53302cdac28d02e6c885f5e14e8b690da2` declares `fs-gg-playtest` digest `7696eaf6020f47606828a65942e5b5f7b74dd970bfe7261e022b21879f4918a6`, already reconciled mechanically by [.github#1485](https://github.com/FS-GG/.github/pull/1485). Tag `skills/v0.5.0` and green release run [30208622225](https://github.com/FS-GG/FS.GG.Game/actions/runs/30208622225) published `FS.GG.Game.Skills` 0.5.0 to both feeds; independently downloaded archives (`98638fcd6cbd…` org / `81ee9c2493fa…` public) have byte-identical extracted payloads after excluding only nuget.org's signature. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — ADD `padd-item` product-workspace driver + stage `FS.GG.Drivers` 0.8.0 (owner `.github`; [.github#1473](https://github.com/FS-GG/.github/issues/1473)): explicit `$padd-item <request>` uses the current workspace's configured organization, named-user, or viewer-owned Projects v2 board. It proves wiring without fallback, runs workspace-scoped reconcile plus exhaustive Backlog triage, deduplicates and files in the current repository, routes other-repo ownership through cross-repo coordination, adds/fields idempotently, and verifies fresh board truth with zero queued writes. Added byte-identically to all three authored roots with `scope: driver` / `materializes-when: always`; the additive driver grows the stable package 0.7.0→0.8.0 for dual-feed publication and subsequent SDD pin adoption. Catalog ceilings grow by one measured skill (descriptions 2500→2700, per-root authored 3000→3200, effective Codex 6000→6400) while preserving bounded headroom. registry = manifest = bytes.

- **2026-07-26** — ADD `p-add` operator skill (owner `.github`; [.github#1471](https://github.com/FS-GG/.github/issues/1471)): explicit `$p-add <request>` first reconciles the Coordination board and exhaustively triages Backlog, then infers the owning repository, deduplicates live issues, writes evidence-backed acceptance criteria and `Paths:`, adds the issue idempotently, chooses a supported initial status, and verifies the fresh board with zero queued writes. Added byte-identically to all three authored roots with `scope: operator` / `materializes-when: false`; registry = manifest = bytes. The same reconcile refreshed four mechanically stale Rendering producer digests discovered by the required pre-filing cleanup.

- **2026-07-26** — RECONCILE (auto; .github#1451): `fsgg-skill-registry-check --write` reconciled the `work-board` and `work-roadmap` driver digests after adding their performance-first worker setup contract. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 4 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-26** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-25** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-25** — RENAME `workRoadmap` → `work-roadmap`, `workBoard` → `work-board`; re-digest all `.github` drivers/operators (owner `.github`; [.github#1411](https://github.com/FS-GG/.github/issues/1411), epic [.github#1409](https://github.com/FS-GG/.github/issues/1409)): migrated both driver identifiers atomically through the three authored roots, producer manifest, registry rows, package/scaffold references and links, with no compatibility aliases. All 11 routing descriptions are now trigger-first and 120–220 characters; `scripts/generate-driver-manifest --catalog-report` gates lower-kebab-case directory/name identity, scalar non-placeholder descriptions, byte-identical roots, five routing fixtures per skill, and explicit catalog ceilings. The current authored description cost is **2,132 / 2,500 characters per root**; names + descriptions + paths cost **2,736 / 3,000** in `.claude`, **2,725 / 3,000** in `.codex`, and **2,736 / 3,000** in `.agents`; effective Codex exposure across its two discoverable roots is **5,461 / 6,000**, preserving measured headroom for future names and paths. Reconciled the four changed `.github` rows (`work-roadmap`, `work-board`, `drive-board`, `cut-nuget-release`) from `registry/driver-skill-manifest.json`; registry = manifest = bytes. No schema or row-count change.

- **2026-07-25** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-25** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 4 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-23** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. The feedback-report v2 change also updated both product drivers to capture and aggregate its checkpoints. registry = manifest = bytes.

- **2026-07-23** — RETIRE `fs-gg-feedback-capture`; preserve report-only feedback flow (owner fs-gg-rendering; [.github#1393](https://github.com/FS-GG/.github/issues/1393), [FS.GG.Rendering#951](https://github.com/FS-GG/FS.GG.Rendering/issues/951), [PR #952](https://github.com/FS-GG/FS.GG.Rendering/pull/952), [ADR-0056](../docs/adr/0056-sdd-is-the-default-lifecycle-spec-kit-is-legacy-and-scheduled-for-removal.md) D3): removed the stale catalog row after Rendering intentionally deleted `template/feedback/skill/SKILL.md` and its producer-manifest entry in merge `e41bb36d`. This is the accepted producer removal reaching the central registry, not restoration of a missing source. The product set decreases **29 → 28** and the Rendering-owned product set **19 → 18**; total catalog rows decrease **49 → 48** (16 process + 28 product + 2 driver + 2 operator), with no schema change (`schemaVersion` remains 3). `fs-gg-feedback-report` remains reconciled from Rendering's current manifest with `materializes-when: always`, and `workRoadmap` remains an `always` driver whose step 4 is report-only, so the cross-lifecycle report flow survives without the retired capture skill. Verified against current Rendering, Game and SDD producer manifests with `fsgg-skill-registry-check`; registry = manifest = bytes.

- **2026-07-23** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-23** — RENAME `fs-gg-mapgen` → `fs-gg-mapcraft` (owner fs-gg-game; [.github#1355](https://github.com/FS-GG/.github/issues/1355), [FS.GG.Game#475](https://github.com/FS-GG/FS.GG.Game/pull/475)): removed the retired `fs-gg-mapgen` row after FS.GG.Game renamed the skill around its broader map-construction contract. The replacement `fs-gg-mapcraft` row remains reconciled verbatim from Game's current producer manifest (`registry = manifest = bytes`), with no Rendering counterpart and `mirrored: false`. Product rows decrease by one because the two ids represented one renamed skill, not two independently shipped skills; no schema change.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 4 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 5 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — ADD `cut-nuget-release` operator skill (owner github; [.github#1362](https://github.com/FS-GG/.github/issues/1362), ADR-0057): reconciled the new `.github`-authored release-train skill from `registry/driver-skill-manifest.json` into the operator block. It audits every rostered producer for package-affecting commits since its last successful release, sequences coherent-set publications, verifies GitHub Packages plus nuget.org, and reconciles the dependency registry. Like `drive-board`, it requires sibling repository checkouts and is therefore `scope: operator` with `materializes-when: false`; it is available byte-identically in this operator checkout's `.claude`, `.codex`, and `.agents` roots but is intentionally absent from product workspaces. No schema change; operator rows increase 1→2.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 0 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 1 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 5 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 2 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 4 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-22** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-21** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 2 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-21** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 8 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-21** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 0 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 1 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-21** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 0 sha256 row(s), 0 materializes-when value(s) and 4 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-21** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 3 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-20** — RE-HOME the two `--write`-appended rows (`fs-gg-playtest`, `fs-gg-symbol-design`) from the file's tail into their blocks (owner github; [.github#299](https://github.com/FS-GG/.github/issues/299), [#1194](https://github.com/FS-GG/.github/issues/1194); ADR-0017, ADR-0022 §6). **Move only — not a single value byte changes, and NO count change**: the two rows are already counted in the current **45 total** (16 process + 27 product + 1 driver + 1 operator) the reconciles below settled, so this entry only relocates them out of the appended tail (the "re-home and annotate these" note `fsgg-skill-registry-check --write` left, .github#299) into the blocks they belong to. `fs-gg-playtest` moves beside its game-originated siblings (ballistics/ai/effects/physics) carrying its manifest bytes verbatim, `mirrored: false` included; `fs-gg-symbol-design` moves into the Rendering-owned game-sim block (collision/grids/line-drawing/visibility), with NO `mirrored:` field — Rendering owns it, so the obligation question does not arise ("absent is not classified"). **REFUSED, and recorded because it was asked:** [FS.GG.Rendering#923](https://github.com/FS-GG/FS.GG.Rendering/issues/923) via cross-repo [#1194](https://github.com/FS-GG/.github/issues/1194) requested `fs-gg-playtest` as `mirrored: true`. It is not flipped — `mirrored:` is the OWNER's asserted obligation, reconciled verbatim from FS.GG.Game's manifest, which declares `false`; hand-forging `true` here to satisfy a consumer's frozen copy is exactly the [FS.GG.Rendering#505](https://github.com/FS-GG/FS.GG.Rendering/issues/505) / [.github#658](https://github.com/FS-GG/.github/issues/658) trap. Rendering#923's `--freshness` lane goes green by declaring the skill `NoCounterpart` in `scripts/FrozenMirrorVerdict.fs` (as it already does for ballistics/ai/effects/physics), or FS.GG.Game must first declare the obligation in its manifest — neither is a `.github` edit. **No `schemaVersion` change** — re-homing rows does not grow the schema (stays **3**).

- **2026-07-20** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 0 sha256 row(s), 1 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-20** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-20** — **`workRoadmap` predicate collapses `feedback == true and lifecycle == spec-kit` → `always`; body re-digested (step 4 → report-only)** (owner `.github`; refs [.github#1247](https://github.com/FS-GG/.github/issues/1247), [ADR-0056](../docs/adr/0056-sdd-is-the-default-lifecycle-spec-kit-is-legacy-and-scheduled-for-removal.md), [ADR-0053](../docs/adr/0053-roadmap-driven-milestone-loop-disposable-sdd-subagents.md) §6, [ADR-0054](../docs/adr/0054-workroadmap-delivery-fabric-a-github-authored-product-materialized-driver.md); epic [.github#1245](https://github.com/FS-GG/.github/issues/1245)). **Consequence of ADR-0056** (`sdd` default, `spec-kit` legacy + scheduled for removal), not a separate decision. The `workRoadmap` driver's `materializes-when` was the COMPOSED AND-of-producers — `fs-gg-sdd-*` (all `always`) AND the `fs-gg-feedback-*` pair — whose one binding clause was `fs-gg-feedback-capture`'s `feedback == true and lifecycle == spec-kit`. ADR-0056 D3 freezes capture and schedules its removal (Rendering#951), so the driver can no longer AND against a skill that is going away: **step 4 becomes report-only** (the driver invokes `fs-gg-feedback-report` alone; capture records on a legacy `spec-kit` tree are still *read* by report, never *invoked* by the driver), the composition **collapses to `fs-gg-feedback-report`'s `always`**, and the driver finally materializes on the **`sdd` lane** it was built for (the Rouge1 symptom ADR-0056 Context names). **NOT schema growth** — no new field, no tightened rule — so `schemaVersion` stays **3**, `skill-registry.version` and the `FS.GG.SDD.Cli` pin are untouched; this is a value reconcile of an existing row (a predicate flip + re-digest), like the digest reconciles below. **Row `workRoadmap` reconciled**: `materializes-when` `feedback == true and lifecycle == spec-kit` → `always`, `sha256 197462de… → 2b9313bf…` (the canonical-body digest of the edited `.claude`/`.agents` `SKILL.md`, both roots byte-identical per `skill-roots-selfcheck`). Predicate + digest are the driver manifest's bytes verbatim: `scripts/generate-driver-manifest`'s `workRoadmap` declaration flipped to `always`, `registry/driver-skill-manifest.json` regenerated (`--check` green), reconciled into this catalog (`registry = manifest = bytes`). Count unchanged at **45** (16 process + 27 product + 1 driver + 1 operator). ADR-0053 §6 + ADR-0054 carry reciprocal `Amended by: ADR-0056` markers. No count change ⇒ no `docs/registry/compatibility.md` edit.
- **2026-07-20** — **FOURTH SKILL CLASS: a `.github`-authored, NEVER-materialized `scope: operator` — `drive-board` is the first row; `schemaVersion` 2 → 3** (owner `.github`; refs [ADR-0057](../docs/adr/0057-operator-scope-a-github-authored-never-materialized-skill-class.md), [ADR-0054](../docs/adr/0054-workroadmap-delivery-fabric-a-github-authored-product-materialized-driver.md), [ADR-0053](../docs/adr/0053-roadmap-driven-milestone-loop-disposable-sdd-subagents.md), [ADR-0037](../docs/adr/0037-schema-growth-is-publish-before-flip.md); FS.GG.SDD.Cli 0.18.x). **Step 2 of ADR-0057's publish-before-flip** (SDD publishes step 1 — the `operator` vocabulary in `Fsgg.Registry` — first; **this flip is BLOCKED on that publish**). **`scope` grows to `process | product | driver | operator`.** `operator` is the mirror image of `driver`: also reconciled from `.github`'s OWN driver manifest (`scripts/generate-driver-manifest` gains a `drive-board` declaration), but delivered to NO tree. **Added row `drive-board`** (`scope: operator, owner: .github, source: .github/.claude/skills/drive-board/SKILL.md, sha256 28edb56f…, materializes-when: "false"`) — the count grows **44 → 45** (16 process + 27 product + 1 driver + **1 operator**). `drive-board` is the cross-repo sibling of `workRoadmap` (ADR-0053): it burns the org Coordination board down across every repo, fanning `/pnext-item` workers across sibling checkouts and re-planning each wave via `check-board`. It runs ONLY from an operator checkout where every repo is a sibling, so it materializes nowhere — the never-true `materializes-when: "false"` (ADR-0017 grammar) makes the scope-agnostic union gate (`skill-union-assert.sh`) read it as a justified downstream absence, **no gate change**. **Schema growth:** `schemaVersion` 2 → 3 here + `skill-registry.version` 2 → 3 in `dependencies.yml` + the `contract-coherence.yml` `FS.GG.SDD.Cli` pin 0.17.0 → **0.18.x** (the CLI that teaches `Fsgg.Registry` the `operator` token — 0.17.0 REJECTS `scope: operator` exactly as 0.15.0 rejected `driver`). Per ADR-0037 §3 the step-1 CLI still accepts skills.yml as it stood (schemaVersion 2; the `operator` token growth is monotone — it rejects nothing the live document contains). `docs/registry/compatibility.md` skill-registry-counts + contract-versions regions regenerated with `scripts/generate-projections`; its hand-authored `skill-registry` surface cell + ASCII edge updated to name the fourth class.
- **2026-07-19** — **THIRD SKILL CLASS: a `.github`-authored, product-materialized `scope: driver` — `workRoadmap` is the first row; `schemaVersion` 1 → 2** (owner `.github`; refs [.github#1230](https://github.com/FS-GG/.github/issues/1230), [#1224](https://github.com/FS-GG/.github/issues/1224), [ADR-0054](../docs/adr/0054-workroadmap-delivery-fabric-a-github-authored-product-materialized-driver.md), [ADR-0053](../docs/adr/0053-roadmap-driven-milestone-loop-disposable-sdd-subagents.md), [ADR-0037](../docs/adr/0037-schema-growth-is-publish-before-flip.md); [FS.GG.SDD#595](https://github.com/FS-GG/FS.GG.SDD/issues/595)). **Step 2 of ADR-0054's publish-before-flip** (SDD published step 1 first). **`scope` grows to `process | product | driver`** and `.github` becomes a producer of its OWN authored drivers: the new emitter `scripts/generate-driver-manifest` writes `registry/driver-skill-manifest.json`, reconciled into this file the way SDD's/Rendering's/Game's manifests are (`registry = manifest = bytes`). **Added row `workRoadmap`** (`scope: driver, owner: .github, source: .github/.claude/skills/workRoadmap/SKILL.md, sha256 197462de…`) — the count grows **43 → 44** (16 process + 27 product + **1 driver**). Its `materializes-when: "feedback == true and lifecycle == spec-kit"` is the COMPOSED (AND-of-producers) predicate ADR-0053 §6 requires: `fs-gg-sdd-*` (all `always`) AND the `fs-gg-feedback-*` pair, whose binding constraint is `fs-gg-feedback-capture`'s predicate (report is `always`) — refuse-if-incomplete expressed structurally. **Schema growth PAID:** `schemaVersion` 1 → 2 here + `skill-registry.version` 1 → 2 in `dependencies.yml` + the `contract-coherence.yml` `FS.GG.SDD.Cli` pin 0.13.0 → **0.17.0** (SDD#595, the CLI that teaches `Fsgg.Registry` the driver vocabulary — 0.15.0 REJECTS `scope: driver`, 0.17.0 ACCEPTS it; measured). This ALSO pays the OWED `mirrored` 1 → 2 debt (#658/#686) — the same scalar, now covered by a validator that reads the file. Per ADR-0037 §3 the step-1 CLI still accepts skills.yml as it STOOD (schemaVersion 1 + `mirrored`; valid, 0 diagnostics — verified before flip). Reconcile discovers `.github` via `fsgg-skill-registry-check`'s third `MANIFEST_CANDIDATES` path and an `owner_of` `.github` special-case (the `-github` dot→dash would match no row); `skill-registry-coherence.yml`/`-autofix.yml` reach `.github`'s own checkout by symlink, not a network clone. The **union gate `skill-union-assert.sh` needed no change** — it is already scope/owner-agnostic (reads `id`/`sha256`/`materializes-when`), correcting ADR-0054 step 2's framing (the real producer-repo assumption lived in the reconcile + coherence workflow, both fixed here). `docs/registry/compatibility.md` skill-registry-counts + contract-versions regions regenerated with `scripts/generate-projections`.

- **2026-07-19** — RECONCILE (auto; .github#299/#1200): `fsgg-skill-registry-check --write` reconciled 1 sha256 row(s), 0 materializes-when value(s) and 0 mirrored verdict(s); homed 0 and appended 0 row(s) from the producer manifests. registry = manifest = bytes.

- **2026-07-14** — DECISION `schemaVersion` stays **1**, and a bump to **2** is now an **OWED DEBT** — **an OPTIONAL, ADDITIVE field IS schema growth, so the entry below's §(e) reasoning is OVERTURNED** (owner github; [.github#686](https://github.com/FS-GG/.github/issues/686), [#689](https://github.com/FS-GG/.github/issues/689), [FS.GG.SDD#420](https://github.com/FS-GG/FS.GG.SDD/issues/420); ADR-0015, ADR-0017). **No row, digest, predicate, owner, source or count changes — 41 rows** (16 process + 25 product); this entry records a **decision** and a **debt**, and edits only comments in `skills.yml`. **(a) CORRECTION to §(e) of the entry below, which this decision falsifies.** That entry declined the `schemaVersion` bump and argued it was safe because "the field is optional and additive: every reader ignores unknown keys […] nothing couples the two version numbers […] the producer shipped this same field into `skill-manifest` v1 without a bump." **Every one of those statements is true, and together they are the wrong conclusion** — they are the reasoning ADR-0015 exists to refuse. Its motivating defect (M5) *was* additive growth: the registry "grew one-off fields" (`minimum-fsgg-sdd`, `package-tag`, `profiles`) and "because the typed validator treats most fields as optional (**additive tolerance**), a stale validator silently degrades toward a YAML-parses check." Those fields were all optional, additive and back-compatible. **"No reader breaks" is not an exemption from schema growth — it is the MECHANISM of the defect**: it is exactly why the drift is *silent*, and therefore exactly why it must be *tracked*. ADR-0015's Consequences say so outright, making back-compat the ambient condition rather than the test: *"unknown fields are still tolerated."* The real test is **does the on-disk schema now carry a field or rule the pinned validator does not know** — and `mirrored` does. This governs every future additive field on this contract, not just this one. **(b) The bump is OWED and deliberately UNPAID.** `schemaVersion` 1 → 2 + `skill-registry.version` 1 → 2 is now a recorded debt, not a closed question — but paying it *here* would be worse than owing it. `Fsgg.Registry` does not assert over this file **at all** yet (the enforcing flip still waits on publishing that assertion), so a bump today would version a schema **no validator reads** and advance the gate's pin to a CLI carrying **nothing**: a claim with nothing behind it — the [#416](https://github.com/FS-GG/.github/issues/416) ornament shape, inside the contract registry itself. An unpaid debt that is *recorded* is honest; one that *looks paid* is not. Read the `schemaVersion: 1` scalar as a debt, not as "no growth" — the file now says so at the scalar. **(c) The bump PROCEDURE was unrunnable, and is corrected.** The rule quoted in this file's own header — bump "in the **SAME PR** that teaches `Fsgg.Registry`" — **is unsatisfiable, and not only from `.github`**: for *both* contracts ADR-0015 governs, the schema **document** lives in `.github` (`skills.yml`, `dependencies.yml`) and the **validator** lives in `FS.GG.SDD`. No PR spans two repos. It has never bitten only because **no `schemaVersion` has ever been bumped** — `mirrored` is the first real schema growth since ADR-0015, and it exposed the procedure cannot be executed as written. Replaced by the org's own **publish-before-flip** (FR-007), two ordered PRs: **1.** FS.GG.SDD#420 teaches `Fsgg.Registry` the field and **publishes** a CLI carrying it — modelling `mirrored` as an OPTIONAL boolean whose **ABSENT is a third state ("not classified") and must NEVER coerce to `false`**, since that coercion is precisely the fail-open [#658](https://github.com/FS-GG/.github/issues/658) was written to close; **2.** `.github` **then** bumps both scalars and advances the `contract-coherence.yml` `FS.GG.SDD.Cli` pin to that published CLI. Amending ADR-0015 §3 itself is [#689](https://github.com/FS-GG/.github/issues/689) — this repo amends an Accepted ADR with a **new** one (cf. ADR-0027 amending ADR-0021 §1), never in place, so it is its own change. **(d)** `dependencies.yml`'s `skill-registry` `surface:` now names `mirrored`, which it had under-described; the entry below deliberately left that alone, because editing the surface while declining the bump would have made half of this decision silently.

- **2026-07-14** — SCHEMA `mirrored:` carried onto the 8 `fs-gg-game`-owned rows — **the ADR-0022 §6 mirror obligation stops being three unlinked guesses and becomes one declared fact** (owner fs-gg-game; [.github#658](https://github.com/FS-GG/.github/issues/658), epic [#266](https://github.com/FS-GG/.github/issues/266); ADR-0017, ADR-0022 §6): there were **THREE readings** of "which skills are frozen mirrors" and nothing asserted they agree — FS.GG.Game's manifest (a declared `mirrored` boolean, [FS.GG.Game#280](https://github.com/FS-GG/FS.GG.Game/pull/280)), FS.GG.Rendering's `check-frozen-mirrors.fsx` (derived from this file's `owner:`), and its `check-skill-refs.sh` (a hardcoded list, [FS.GG.Rendering#722](https://github.com/FS-GG/FS.GG.Rendering/issues/722)). All three said four. **Nothing made them, and the drift is fail-open in every one**: a body that becomes a mirror and is declared nowhere reads as "not mirrored" to all of them, so bare `[[refs]]` in it stay legal and it dangles in the mirror with **both repos' gates green** ([FS.GG.Game#273](https://github.com/FS-GG/FS.GG.Game/issues/273) / [FS.GG.Rendering#714](https://github.com/FS-GG/FS.GG.Rendering/issues/714) verbatim). **(a) Eight rows gain the field**, reconciled verbatim from FS.GG.Game's producer manifest (`template/skill-manifest/skill-manifest.json` @ `a5f5c77`): `mirrored: true` on the four ADR-0022 P4 migrations `fs-gg-game-core`/`fs-gg-audio`/`fs-gg-persistence`/`fs-gg-model-swap`, `mirrored: false` on the four Game-originated `fs-gg-ballistics`/`fs-gg-ai`/`fs-gg-effects`/`fs-gg-physics`. **No digest, predicate, owner, source or row count changes — 41 rows (16 process + 25 product; 17 Rendering-owned, 8 Game-owned)**, so the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) is **not** edited. **(b) The other 33 rows deliberately carry NO verdict, and that is not an omission.** Absent means *not classified*, never `false` — `select(.mirrored == true)` reads an absent field as false (`null == true` is false), which is how a manifest predating the field answers "not mirrored" for **every** body and leaves every real mirror unguarded (the trap [FS.GG.Game#282](https://github.com/FS-GG/FS.GG.Game/pull/282) nearly shipped). Those 33 are rows for which the question does not *arise*: FS.GG.Rendering holds no second copy of them. The moment it does, the new `mirror-verdict` arm goes **red** until the owner declares one. Do not "tidy" the file by adding `mirrored: false` everywhere. **(c) `fsgg-skill-registry-check` now classifies on the OBLIGATION, not on a name collision** — and that fixes a latent regression, not just a gap. The old `frozen-mirror` arm keyed on `owner == fs-gg-game AND Rendering holds a same-named file`; it gave the right answer only by the grace of an `owner:` assignment, because `fs-gg-collision`/`fs-gg-grids`/`fs-gg-line-drawing`/`fs-gg-visibility` **also** exist in both trees, deliberately diverge (rewritten against the `.fsi`), and happen to be homed `owner: fs-gg-rendering`. **Re-home one to its real owner and the old tool starts demanding byte-identity of two bodies nobody promises agree** — driven against the live producer trees, it does exactly that. Classify on the obligation and it is four; classify on the collision and it is eight, which is the wrong set [FS.GG.Rendering#541](https://github.com/FS-GG/FS.GG.Rendering/issues/541) is carrying. **(d) A SECOND fail-open closed, and it was green on `main` as this was written.** The old arm compared bytes only *if* Rendering held a file, so a live obligation whose copy had **vanished** — deleted, renamed, never vendored — matched nothing and reported **`coherent`, exit 0**. Verified: delete Rendering's `fs-gg-audio` copy against `main`'s tool and it still says coherent. That is a missing subject reporting green (#266) *inside the arm written to catch it*. A declared `mirrored: true` with no copy is now a `frozen-mirror` finding; a mirror is retired when the **owner** flips the verdict, not when a file goes missing behind its back. **(e) NO `schemaVersion` bump, and that is a decision, not an oversight.** The contract row's rule is "bump this `version` + skills.yml `schemaVersion` in the SAME PR that teaches the validator" — but `Fsgg.Registry` lives in **FS.GG.SDD**, cannot be taught from here, and (per this file's own header) does not assert over `skills.yml` **at all** yet. The field is optional and additive: every reader ignores unknown keys (`skill-union-assert.sh` selects `[.id, .sha256, ."materializes-when"]`; `skill-registry-autofix-body` indexes by `id`), nothing couples the two version numbers, and the producer shipped this same field into `skill-manifest` **v1** without a bump. Bumping a version whose validator cannot know the field would be a claim with nothing behind it. The bump + the typed validator + `dependencies.yml`'s `surface:` prose are filed together as [.github#686](https://github.com/FS-GG/.github/issues/686).

- **2026-07-13** — RE-DIGEST 14 rows + PREDICATE `fs-gg-audio` — **the table reaches ZERO findings for the first time; the two frozen-mirror blockers that capped its predecessors at 1 are gone** (owners fs-gg-rendering + fs-gg-game; [.github#486](https://github.com/FS-GG/.github/issues/486); ADR-0017, ADR-0022 §6): the gate reported **15 findings** on `main`; this leaves **0** — `registry = manifest = bytes`, absolutely, for all 41 rows. Reconciled by `scripts/fsgg-skill-registry-check --write --repos-root <dir>` against a **scratch** root holding the three producers cloned at `origin/main` — never a sibling-directory root, which reads in-flight `git worktree`s as extra producers. Every digest is the tool's, read from the producer manifests; **none was copied from an issue, from a prior changelog entry, or from the bot's standing PR** (#486's trap 1). **(a) Fourteen digests**, from producer bodies that moved in the last ~30 hours: `fs-gg-scene`, `fs-gg-symbology`, `fs-gg-elmish`, `fs-gg-skiaviewer`, `fs-gg-keyboard-input`, `fs-gg-ui-widgets`, `fs-gg-collision`, `fs-gg-testing`, `fs-gg-feedback-capture`, `fs-gg-feedback-report` (owner fs-gg-rendering — [FS.GG.Rendering#721](https://github.com/FS-GG/FS.GG.Rendering/pull/721) `99e11ee`, [#703](https://github.com/FS-GG/FS.GG.Rendering/pull/703) `5cd124c`, [#697](https://github.com/FS-GG/FS.GG.Rendering/pull/697) `237414c`, [#691](https://github.com/FS-GG/FS.GG.Rendering/pull/691) `a9e3850`, [#672](https://github.com/FS-GG/FS.GG.Rendering/pull/672) `1fddbd0`); `fs-gg-game-core`, `fs-gg-audio`, `fs-gg-persistence`, `fs-gg-model-swap` (owner fs-gg-game — [FS.GG.Game#282](https://github.com/FS-GG/FS.GG.Game/pull/282) `5f43827`, [#279](https://github.com/FS-GG/FS.GG.Game/pull/279) `96e0b49`, [#271](https://github.com/FS-GG/FS.GG.Game/pull/271) `5dbc83e`). **(b) One predicate.** `fs-gg-audio` `profile in [game, sample-pack]`→**`profile in [app, sample-pack, game]`** — `predicate-matches` ([.github#292](https://github.com/FS-GG/.github/issues/292)) asserts the registry predicate is the producer manifest's bytes *verbatim*; the row was half-drifted (digest **and** predicate), and re-digesting without it would have left it red on the very item that exists to fix it. Rewritten by `--write`, which reconciles `materializes-when` as well as `sha256`. **(c) Zero, not one — and that is the news.** Both predecessors below ended `1 finding` red on a `[frozen-mirror]` divergence they correctly refused to touch (`fs-gg-game-core`, then `fs-gg-persistence`) — a mirror lives in FS.GG.Rendering and `.github` cannot fix it. **Both mirrors have since adopted**: Rendering [#652](https://github.com/FS-GG/FS.GG.Rendering/pull/652) re-froze `fs-gg-audio`/`fs-gg-persistence`, [#721](https://github.com/FS-GG/FS.GG.Rendering/pull/721) re-synced all four, and Game [#282](https://github.com/FS-GG/FS.GG.Game/pull/282) made the mirrored-body guard read the manifest instead of a hardcoded list. The `frozen-mirror` arm now derives **no** finding. **Set/count does NOT change (41 rows; 16 process + 25 product — 17 `fs-gg-rendering`-owned, 8 `fs-gg-game`-owned)** — `--write` appended nothing, so the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) is **not** edited; its `25 = 17+8` was re-counted against the live registry and is correct. No `schemaVersion` bump (a pure digest+predicate reconcile). `scripts/skill-union-assert.sh` was re-checked for the twin assumption [#311](https://github.com/FS-GG/.github/issues/311) flagged and **has none** — it never reads this registry, so no change is owed; the claim is now verified twice. **(d) CORRECTION to the entry below, which this reconcile falsifies.** That entry closes by saying that post-[#514](https://github.com/FS-GG/.github/issues/514) *"a stale standing PR at least says so on its own checks instead of sitting there looking mergeable."* **It does not.** The bot's current standing PR, [#595](https://github.com/FS-GG/.github/pull/595), shows **7/7 green** and is **12 findings** from coherent against today's producers — measured, by running the checker over its branch's `skills.yml`. Merging it would **not** green `main`, and it declares digests that were never current (`fs-gg-elmish a14e6d90…` against an actual `4b1fbf4a…`). #514 made the PR's checks **run once**, by pushing with an App token; it did not make them **re-run**. A check result is frozen at its last execution, the nightly `schedule` run of `skill-registry-coherence` is not attached to any PR, and [#422](https://github.com/FS-GG/.github/issues/422)'s `--baseline-registry` (correctly) forgives findings already present in the merge base — so producer drift *after* the bot's last push is structurally invisible on the bot's own PR. **A green stale standing PR is worse than a red one**: red is a signal, green is an invitation to merge a regression. That is [#425](https://github.com/FS-GG/.github/issues/425) alive in a new form, and it sharpens the open [#642](https://github.com/FS-GG/.github/issues/642) (*"no auto-merge, so a green PR lands nothing"*): **auto-merge alone would make this worse, not better** — merged blind, today's green #595 would have reverted **12 of the 14 rows** in this entry (it agrees with the producers on only `fs-gg-skiaviewer` and `fs-gg-keyboard-input`). The bot must re-check freshness against the producers *at merge time*, not trust a check that passed hours ago. Measured and recorded on #642 rather than re-filed.

- **2026-07-11** — RE-DIGEST 5 rows — **the second reconcile of this table in three hours, which is the finding, not the footnote** (owners fs-gg-rendering + fs-gg-game; [.github#486](https://github.com/FS-GG/.github/issues/486); ADR-0017, ADR-0022 §6): the entry below took the gate from **12 findings to 1** at 15:54Z. By 17:56Z it was at **6** again — five producer bodies had moved in the intervening two hours, none of them anybody's fault, all of them invisible to `.github` until the nightly gate. This entry takes it back to **1**. Reconciled by `scripts/fsgg-skill-registry-check --write --repos-root <dir>` against a **scratch** root holding the producers cloned at `origin/main` — never a sibling-directory root, which would read in-flight `git worktree`s as extra producers. Every digest is the tool's, read from the producer manifests; none was copied from an issue or from the bot's PR. **(a) Five digests.** `fs-gg-elmish` `78db71a0…`→**`a4f1c07f…`** and `fs-gg-testing` `2b481203…`→**`e225abd4…`** (both [FS.GG.Rendering#508](https://github.com/FS-GG/FS.GG.Rendering/pull/508), `04f52d9`); `fs-gg-keyboard-input` `fbad9e80…`→**`848c935d…`** ([FS.GG.Rendering#523](https://github.com/FS-GG/FS.GG.Rendering/pull/523), `89f1fe7`); `fs-gg-feedback-capture` `5ef93a66…`→**`6c3822b5…`** ([FS.GG.Rendering#528](https://github.com/FS-GG/FS.GG.Rendering/pull/528), `cdc47a5`) (owner fs-gg-rendering); `fs-gg-physics` `0cf55208…`→**`cabeed06…`** ([FS.GG.Game#156](https://github.com/FS-GG/FS.GG.Game/pull/156), `219592a`) (owner fs-gg-game). **(b) One finding remains, and it is not `.github`'s to fix.** `[frozen-mirror] fs-gg-persistence` — FS.GG.Rendering's frozen copy (`e998befc…`) is not byte-identical to the Game-owned canonical body (`e9f471b0…`). `--write` refuses judgement cases by design (auto-resolving one would be the epic-#266 fail-open inside the fix for it), so it is filed against the owning repo and this table stays red until the mirror adopts. Note the blocker **rotated**: the previous entry's blocker, `[frozen-mirror] fs-gg-game-core` ([FS.GG.Rendering#497](https://github.com/FS-GG/FS.GG.Rendering/issues/497)), is now **closed and adopted** — a *different* mirror drifted in its place. **(c) Supersedes the bot's standing PR** [#521](https://github.com/FS-GG/.github/pull/521), which was cut at 17:24Z and was already stale by 17:56Z: it carried four of these five rows and never saw `fs-gg-feedback-capture`. That is [#425](https://github.com/FS-GG/.github/issues/425) demonstrated twice in one day — and now, post-[#514](https://github.com/FS-GG/.github/issues/514), a stale standing PR at least says so on its own checks instead of sitting there looking mergeable.

- **2026-07-11** — RE-DIGEST 7 rows + PREDICATE `fs-gg-symbology` + COMPLETENESS `fs-gg-ai`/`fs-gg-effects`/`fs-gg-physics` added (22→25 product) — **the whole table reconciled as ONE coherent set, because the per-row chains demonstrably lose the race** (owners fs-gg-rendering + fs-gg-game; [.github#486](https://github.com/FS-GG/.github/issues/486), consolidating [#311](https://github.com/FS-GG/.github/issues/311)/[#328](https://github.com/FS-GG/.github/issues/328)/[#330](https://github.com/FS-GG/.github/issues/330)/[#438](https://github.com/FS-GG/.github/issues/438)/[#477](https://github.com/FS-GG/.github/issues/477), and [#484](https://github.com/FS-GG/.github/issues/484) closed as a sixth duplicate during this work; ADR-0017, ADR-0022 §6, ADR-0015): the gate reported **12 findings** on `main`; this leaves **1**. Reconciled by `scripts/fsgg-skill-registry-check --write --repos-root <dir>` against a **scratch** root holding only the three producers cloned at `origin/main` — never a sibling-directory root, which reads in-flight `git worktree`s as extra producers. Every digest is the tool's, taken from the producer manifests; **none was copied from any issue's table**, per #486's trap 1 — and that mattered: of the five originals #486 consolidated, **three carried digests that were already wrong** when filed. **(a) Seven digests.** `fs-gg-symbology` `7e576bd7…`→**`966c4612…`**, `fs-gg-elmish` `4662d8ff…`→**`78db71a0…`**, `fs-gg-collision` `3d4788ba…`→**`0ae0d753…`**, `fs-gg-project` `3dc0b55b…`→**`ae893fd0…`** (owner fs-gg-rendering); `fs-gg-game-core` `16978c46…`→**`ffdbc63a…`** ([FS.GG.Game#139](https://github.com/FS-GG/FS.GG.Game/pull/139), `e3fdc76`), `fs-gg-audio` `a2941b2f…`→**`9401ccd9…`**, `fs-gg-ballistics` `44a1c7ae…`→**`01620e43…`** ([FS.GG.Game#143](https://github.com/FS-GG/FS.GG.Game/pull/143), `046b0da7`) (owner fs-gg-game). **(b) One predicate.** `fs-gg-symbology` `profile in [app, headless-scene, governed, sample-pack, game]`→**`profile in [app, sample-pack, game]`** — `predicate-matches` ([.github#292](https://github.com/FS-GG/.github/issues/292)) asserts the registry predicate is the producer manifest's bytes *verbatim*; the row was half-drifted (digest **and** predicate), and re-digesting without it would have left the row red on the very item that exists to fix it. **(c) Three appended rows.** `fs-gg-ai` **`612033dc…`**, `fs-gg-effects` **`8f4b369b…`**, `fs-gg-physics` **`0cf55208…`** — declared by FS.GG.Game's manifest and never flipped here at all; the `declared-completeness` arm ([.github#289](https://github.com/FS-GG/.github/issues/289)) had been reporting them for days. Appended by `--write` and re-homed into the game block per the tool's emitted comment; the rows' bytes are the tool's. **Set/count DOES change: 38 → 41 rows (16 process + 25 product; Rendering-owned 17, Game-owned 5 → 8)** — so, as with .github#289/#298 and unlike every pure re-digest entry, the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) **is** edited. It was found carrying **two mutually-inconsistent counts** (`22 = 17+5` under *Versioned contracts*, `21 = 17+4` under `skill-registry-published`) — neither correct, both hand-maintained, and `check-projection.py` covers only `dependencies.yml`, so nothing was ever going to catch that. Both are now `25 = 17+8`. No `schemaVersion` bump (a digest reconcile plus three appends — the #288/#289/#292 precedent). **The four GAME-ORIGINATED skills have no Rendering twin, and that is the design.** `fs-gg-ballistics`/`fs-gg-ai`/`fs-gg-effects`/`fs-gg-physics` were authored in FS.GG.Game and are not ADR-0022 migrations, so the `frozen-mirror` arm correctly derives none for them — verified against Rendering's live tree, whose `template/product-skills/` ships 17 skills and none of these four. **[FS.GG.Rendering#505](https://github.com/FS-GG/FS.GG.Rendering/issues/505) asked Rendering to "adopt the corrected bytes" into frozen copies of `fs-gg-ballistics` + `fs-gg-effects` that do not exist** — a request with no subject, which #484 had then sequenced its re-digest *behind*, parking two immediately-landable rows behind a PR nobody could write (the [silent no-op](https://github.com/FS-GG/.github/issues/416) shape, as a scheduling dependency). Said so on both; those two rows land here. `scripts/skill-union-assert.sh` was reviewed for the twin assumption #486 flagged and **has none** — it never reads this registry (it asserts the byte-identical union across a scaffold's skill roots), so no change was owed. **`skill-registry-coherence` stays RED on `main` after this lands, with exactly ONE finding, and that is intended:** `[frozen-mirror] fs-gg-game-core` — FS.GG.Rendering's frozen `--profile game` copy is still `16978c46…` against a canonical `ffdbc63a…`. **`.github` cannot fix it alone** (the row reconciles FROM the owner's manifest, so `--write` moves the row and the mirror finding survives; the arm compares the mirror to the **canonical body**, not to this row, so it was **already red before this change and is not made worse by it** — a point #486's own sequencing table got wrong, and the reason the dual-homed rows are landed here rather than held). Owned by [FS.GG.Rendering#497](https://github.com/FS-GG/FS.GG.Rendering/issues/497), still open with no PR; canonical + registry + mirror are one set ([.github#422](https://github.com/FS-GG/.github/issues/422) is the protocol finding). **#486's table also lists `fs-gg-audio` as gated on [FS.GG.Game#131](https://github.com/FS-GG/FS.GG.Game/issues/131); it is not** — that mirror has already adopted (`9401ccd9…` both sides, verified byte-for-byte), so the row lands here too. **The shape, again:** nothing propagates a producer skill-body merge into this file — [.github#299](https://github.com/FS-GG/.github/issues/299) closed detection→response with the standing autofix PR [#414](https://github.com/FS-GG/.github/pull/414), but response→landed is still open ([#425](https://github.com/FS-GG/.github/issues/425)): the bot never merges, and the PR rots while it waits. **That is what makes this entry a data fix and not a cure.** Six hand-filed chains in eight days, three of them stale on arrival, is the measurement; #425 is the fix.

- **2026-07-11** — RE-DIGEST 2 rows + PREDICATE `fs-gg-feedback-report` `feedback == true`→**`always`** — the feedback pair, whose bodies moved when a record was made to carry the toolchain version it was reproduced on (owner fs-gg-rendering; producers [FS.GG.Rendering#272](https://github.com/FS-GG/FS.GG.Rendering/issues/272) / PR [#455](https://github.com/FS-GG/FS.GG.Rendering/pull/455), `main` @ `9f6465f`, and [FS.GG.Rendering#434](https://github.com/FS-GG/FS.GG.Rendering/issues/434) for the predicate; requested as [.github#427](https://github.com/FS-GG/.github/issues/427); ADR-0017, ADR-0015): Rendering#455 rewrote both canonical bodies — a feedback record must now carry the toolchain version it was reproduced on, and a reporter must check a candidate defect against the latest tag before filing, which closes the *"a stale finding is indistinguishable from a live one"* hole that cost three consumers independent rediscovery of the same fixed defect. Reconciled: `fs-gg-feedback-capture` `e6c16f33…`→**`5ef93a66…`**, `fs-gg-feedback-report` `4093ccd3…`→**`68626e24…`**. **The request's table was stale on the second row and was NOT trusted**: it names `ba7cfe74…` as the report's old digest, but this file actually carried `4093ccd3…` (the row was re-reconciled by [.github#298](https://github.com/FS-GG/.github/issues/298) after #427 was filed). Both new digests were **re-derived from the merged producer bytes** (`sha256sum` over `template/feedback/skill/SKILL.md` and `template/feedback-report/skill/SKILL.md` at `FS.GG.Rendering@main`) and independently cross-checked against both the producer manifest `template/skill-manifest/skill-manifest.json` and what `scripts/fsgg-skill-registry-check` computes — all three agree on both rows. Run against a **scratch** repos-root holding only the three producers cloned at `origin/main`, never a sibling-directory root (which reads in-flight `git worktree`s as extra producers). **The `materializes-when` flip is not scope creep — it is the other half of this row's drift.** `predicate-matches` (.github#292) asserts the registry predicate is the producer manifest's bytes *verbatim*; Rendering#434 (merged 13:08Z) moved the report's predicate to `always`, and the row still declared `feedback == true`. A reporter must ride **every** lane: an instruction placed only in the `feedback`-gated `capture` never reaches a reporter on the `sdd` or `none` lane — the reason #427's own body gives for why *both* rows moved, not just the one Rendering#272 anticipated. Left red, the row would have been half-reconciled (digest green, predicate red) on the very row this item exists to fix. The dated 2026-07-10 provenance note in `skills.yml` is left as written — it was true when written; the row's own annotation, which describes the row as it now stands, is re-tensed. **Set/count does NOT change (38 rows; 16 process + 22 product)** — no row added or removed, so the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) is **not** edited (it carries no per-row digests) and there is no `schemaVersion` bump. **`skill-registry-coherence` stays RED on `main` after this lands, with 7 findings, and that is intended** — verified by running the gate on pristine `main` (**10** findings) and on this branch (**7**): the three it clears are exactly this item's two `[digest-matches]` and one `[predicate-matches]`, and it introduces none. The 7 that remain are red independently of this change and outside its declared touch-set: three `[digest-matches]` (`fs-gg-symbology`, `fs-gg-elmish`, `fs-gg-collision`), one `[predicate-matches]` (`fs-gg-symbology`), and three `[declared-completeness]` (`fs-gg-ai`/`fs-gg-effects`/`fs-gg-physics`, Game-owned) — the digests plus the three missing rows are covered by the standing autofix PR [#414](https://github.com/FS-GG/.github/pull/414), which **never merges** ([#425](https://github.com/FS-GG/.github/issues/425)) and whose snapshot predates `fs-gg-symbology`'s predicate drift, so merging it as it stands would not green `main` either. Folding them in would make a mechanical row-bump an unreviewable multi-producer reconcile (the #298/#289 precedent). **The shape, again:** nothing propagates a producer skill-body merge into this file — #427 was filed by hand *because* Rendering#272 asked to be pinged on merge, and the ping is a human. The missing edge is the producer-release → `.github` dispatch filed as [.github#299](https://github.com/FS-GG/.github/issues/299); this is now its Nth instance in three days, and the manual sweep still does not converge.

- **2026-07-11** — RE-DIGEST 10 rows — every `fs-gg-sdd-*` **stage** skill, whose bodies moved together when SDD stopped teaching `covers` as a magic token (owner fs-gg-sdd; producer [FS.GG.SDD#352](https://github.com/FS-GG/FS.GG.SDD/issues/352) / PR [#354](https://github.com/FS-GG/FS.GG.SDD/pull/354), `main` @ `4e57c73`; requested as [.github#426](https://github.com/FS-GG/.github/issues/426); ADR-0017, ADR-0015): SDD#354 (*"link the doctested canonical example from every stage skill; stop teaching `covers` as a magic token"*) rewrote all ten stage bodies in one commit and re-emitted SDD's own manifest in the same commit — so **the producer's drift-guard stayed green throughout and no producer decision was required**; the drift was entirely in this file, which still pinned the pre-move digests. `skill-registry-coherence` on `main` went from 11 findings to **21** the moment #354 merged. Reconciled: `fs-gg-sdd-analyze` `5e9e90ca…`→**`ae59ef0f…`**, `fs-gg-sdd-charter` `9855bd3c…`→**`8e1df19b…`**, `fs-gg-sdd-checklist` `c267d33f…`→**`04528366…`**, `fs-gg-sdd-clarify` `09d2ed97…`→**`2d5729af…`**, `fs-gg-sdd-evidence` `7576a7c5…`→**`46952485…`**, `fs-gg-sdd-plan` `51a95d10…`→**`62b6d9be…`**, `fs-gg-sdd-ship` `c98e3e86…`→**`5255a14d…`**, `fs-gg-sdd-specify` `55c65444…`→**`f4bd7db5…`**, `fs-gg-sdd-tasks` `c62b4624…`→**`ee821440…`**, `fs-gg-sdd-verify` `8ac403e4…`→**`ea8a770d…`**. Every digest **re-derived from the merged bytes** (`sha256sum FS.GG.SDD/.claude/skills/fs-gg-sdd-<id>/SKILL.md` at `4e57c73`) rather than copied from the request's table, and cross-checked against what `scripts/fsgg-skill-registry-check` computes — the two agree on all ten. Run against a **scratch** repos-root holding only the three producers detached at `origin/main`: the sibling directory currently carries five in-flight `git worktree`s (`FS.GG.SDD-352b`, `FS.GG.Rendering-435/-446/-453`, `FS.GG.SDD-349`), and a sibling-directory root reads each of them as an extra producer. **Set/count does NOT change (38 rows; 16 process + 22 product)** — a pure re-digest, so the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) is **not** edited (it carries no per-row digests) and there is no `schemaVersion` bump. No `owner`/`source`/`materializes-when` change to any row. **`skill-registry-coherence` stays RED on `main` after this lands, with 10 findings, and that is intended** — the request ([#426](https://github.com/FS-GG/.github/issues/426)) scopes this item to these ten rows and names the owners of the rest, and folding them in would make a mechanical row-bump an unreviewable multi-producer reconcile (the #298/#289 precedent). The 10 that remain, all red independently of this change and outside its declared touch-set: five `[digest-matches]` (`fs-gg-symbology`, `fs-gg-elmish`, `fs-gg-collision`, `fs-gg-feedback-capture`, `fs-gg-feedback-report`), two `[predicate-matches]` (`fs-gg-symbology`, `fs-gg-feedback-report`), and three `[declared-completeness]` (`fs-gg-ai`/`fs-gg-effects`/`fs-gg-physics`, Game-owned) — three of the digests plus the three missing rows are covered by the standing autofix PR [#414](https://github.com/FS-GG/.github/pull/414), which **never merges** ([#425](https://github.com/FS-GG/.github/issues/425)); the two frozen-mirror rows are [FS.GG.Rendering#450](https://github.com/FS-GG/FS.GG.Rendering/issues/450). **Note the shape, now on its Nth instance:** nothing propagates a producer skill-body merge into this file, so a re-digest buys hours — the missing edge is the producer-release → `.github` dispatch filed as [.github#299](https://github.com/FS-GG/.github/issues/299), and this entry is more evidence that the manual sweep does not converge.

- **2026-07-11** — RE-DIGEST 2 rows — `fs-gg-game-core` + `fs-gg-persistence`, whose canonical bodies moved when Game taught them the two traps they were teaching *into* (owner fs-gg-game; producer [FS.GG.Game#129](https://github.com/FS-GG/FS.GG.Game/issues/129) / PR [#130](https://github.com/FS-GG/FS.GG.Game/pull/130), `main` @ `8f13ae7`; requested as [.github#420](https://github.com/FS-GG/.github/issues/420); ADR-0022 §6, ADR-0017): `fs-gg-game-core` `db299500…`→**`16978c46…`** (its `SpatialGrid` example could not compile — it fed a `Geometry.Vec2` to `SpatialGrid.build`, which takes a `FS.GG.Game.Core.Point`; the old snippet fails `FS0001`), `fs-gg-persistence` `5aea4524…`→**`e9f471b0…`** (its *Common pitfalls* told the reader to *"assert on `PersistenceEvidence.Requested`, not on files on disk"* — sound for the record-only `interpret`/`record`, but a suite written to it **passes against a backend that writes nothing**, which would have walked an agent whose task was to *build* the file backend straight into the silent-no-op family, epic [.github#416](https://github.com/FS-GG/.github/issues/416); the body now carries an `## If you own the backend` branch). Reconciled with `scripts/fsgg-skill-registry-check --write --repos-root <dir>` against a **scratch** root holding only the three producers cloned at `origin/main` — never a sibling-directory root, which picks up in-flight `git worktree`s and reads them as extra producers. Digests re-derived from the merged bytes (`sha256sum`), not copied from the request. **Set/count does NOT change (38 rows; 16 process + 22 product)** — a pure re-digest, so the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) is **not** edited (it carries no per-row digests) and no `schemaVersion` bump. No `owner`/`source`/`materializes-when` change to any row. **`skill-registry-coherence` stays RED on `main` after this lands, and that is intended — this is one member of a three-repo coherent set, not a fix on its own.** (1) Two `[frozen-mirror]` findings on the very rows re-digested here: FS.GG.Rendering still ships frozen `--profile game` copies that must be byte-identical to the canonical body, and they now diverge. **`.github` cannot fix this alone** — the row reconciles FROM the owner's manifest (ADR-0017), so `--write` moves the row and the mirror finding survives; `--write` correctly refuses it as a judgement case. Owned by [FS.GG.Rendering#450](https://github.com/FS-GG/FS.GG.Rendering/issues/450); canonical + registry + mirror are **one set**, and landing any one of them alone only moves which arm is red (the sixth instance of this window — [.github#422](https://github.com/FS-GG/.github/issues/422) is the protocol finding, precedents #295/#304/#328/#330/#311). (2) Nine further findings — **six** `[digest-matches]`/`[predicate-matches]` across four skills (`fs-gg-symbology` ×2, `fs-gg-elmish`, `fs-gg-collision`, `fs-gg-feedback-report` ×2) plus three `[declared-completeness]` rows (`fs-gg-ai`/`fs-gg-effects`/`fs-gg-physics`) — all **red independently of this change and outside its touch-set**. (11 findings remain in total: these 9 + the 2 frozen-mirror above.) The standing autofix PR [#414](https://github.com/FS-GG/.github/pull/414) reconciles three of them — the **digests** of `fs-gg-symbology`/`fs-gg-elmish`/`fs-gg-collision` — and appends the three missing rows; but it was cut at 08:25Z and covers **none** of the drift that landed after it: `fs-gg-feedback-report`'s digest *and* predicate (`feedback == true`→`always`, [Rendering#434](https://github.com/FS-GG/FS.GG.Rendering/issues/434), merged 13:08Z) and `fs-gg-symbology`'s predicate (it leaves that row's `materializes-when` untouched). **So merging #414 as it stands would not green `main` either** — the standing PR's body is a snapshot that silently decays, filed as [.github#425](https://github.com/FS-GG/.github/issues/425). Deliberately NOT touched here (the .github#298/#289 precedent): folding them in would make this row-bump an unreviewable multi-producer reconcile and duplicate the bot's branch.

- **2026-07-10** — RE-DIGEST 4 rows + COMPLETENESS `fs-gg-ballistics` added (21→22 product) — the game skill bodies moved when their dangling refs were qualified (owner fs-gg-game; producer [FS.GG.Game#35](https://github.com/FS-GG/FS.GG.Game/issues/35) / PR [#63](https://github.com/FS-GG/FS.GG.Game/pull/63), `7183195`; ADR-0022 §6, ADR-0017, ADR-0014): **(a) The re-digest.** FS.GG.Game#63 qualified every `[[wiki-ref]]` in its published skills with the publishing repo's `owner` id (`[[fs-gg-scene]]` → `[[fs-gg-rendering:fs-gg-scene]]`), because a bare ref to a Rendering-owned skill resolves in Rendering and resolves to **nothing** in a scaffolded game product — 21 such refs across all five bodies. That edited four canonical bodies, so four rows are reconciled here: `fs-gg-game-core` `f9f732cd…`→**`db299500…`** (also absorbing the `Loop` section from [Game#44](https://github.com/FS-GG/FS.GG.Game/issues/44)), `fs-gg-audio` `3a757374…`→**`a2941b2f…`**, `fs-gg-persistence` `af107dec…`→**`5aea4524…`**, `fs-gg-model-swap` `a48df195…`→**`e6be58c8…`**. **(b) The completeness catch.** `fs-gg-ballistics` shipped in FS.GG.Game on 2026-07-10 ([Game#40](https://github.com/FS-GG/FS.GG.Game/issues/40), PR [#58](https://github.com/FS-GG/FS.GG.Game/pull/58)) and was never flipped here; the `declared-completeness` arm ([.github#289](https://github.com/FS-GG/.github/issues/289)) reported it. Unlike its four siblings it is **not** an ADR-0022 migration — it originates in Game and has no Rendering counterpart, so no frozen mirror exists and the `frozen-mirror` arm correctly derives none. Appended by `--write` and re-homed into the game block per the tool's emitted comment; the row's bytes are the tool's. **Set/count DOES change: 37 → 38 rows (16 process + 22 product; Rendering-owned 17, Game-owned 4 → 5)**, so — as with .github#289/#298 and unlike every pure re-digest entry — the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) **is** edited. Reconciled with `scripts/fsgg-skill-registry-check --write --repos-root <dir>` against a scratch root of the three producer repos cloned at `origin/main` (never a sibling-directory root: it picks up in-flight `git worktree`s such as `FS.GG.Game-35` and reads them as extra producers). No `schemaVersion` bump (a digest reconcile plus one row append — the #288/#289/#292 precedent). No `owner`/`source`/`materializes-when` change to any existing row. **`skill-registry-coherence` stays RED on `main` after this lands, with 5 findings, and that is intended.** (1) Four `[frozen-mirror]` findings on the very rows re-digested above: FS.GG.Rendering still ships frozen `--profile game` copies that must be byte-identical to the canonical body, and they now diverge. **`.github` cannot fix this alone** — the row is reconciled FROM the owner's manifest (ADR-0017), so `--write` moves the row and the mirror finding survives; this is the same two-step signature as the `fs-gg-audio` entry below, with the direction reversed (there the mirror moved and the canonical adopted; here the canonical moved and the mirror must). Filed as [FS.GG.Rendering#299](https://github.com/FS-GG/FS.GG.Rendering/issues/299), sequenced `Blocked by` Rendering#285 because it must edit `template/skill-manifest/skill-manifest.json`, which #285 holds. (2) One `[digest-matches]` on `fs-gg-symbology` (`7e576bd7…` vs `fe4bd347…`), from [Rendering#296](https://github.com/FS-GG/FS.GG.Rendering/pull/296) — **red independently of this change, outside its touch-set, and its body is still in flight under Rendering#285**, so re-digesting it here would be reconciled stale within the hour. Deliberately NOT touched (the .github#298 precedent). **Deliberately NOT done here:** the `owner`/`source` flip of `fs-gg-collision`/`fs-gg-grids`/`fs-gg-line-drawing`/`fs-gg-visibility` to `fs-gg-game`. FS.GG.Game#63 published rewritten versions of those four — they teach the `Geometry`/`Resolution`/`Grids`/`Los`/`Fov`/`Visibility` **package modules** that Game#32/#33/#34/#38 promoted, not the product-owned `Collision.fs`/`Grids.fs`/`LineDrawing.fs`/`Visibility.fs` **fragments** Rendering's scaffold emits. The two bodies therefore **cannot** be byte-identical, so flipping the rows would immediately red `frozen-mirror` on four more. Retiring the mirror is the real ADR-0022 §6 P6 work and changes what `--profile game` emits; it needs the deferred consumer scaffold provider. Until then those rows stay `owner: fs-gg-rendering` and remain digest-green against Rendering's unchanged copies — which is why this change adds **no** finding for them. Also **not** done: the `skill-registry-published` coherence flip ([.github#290](https://github.com/FS-GG/.github/issues/290)) — a green gate is its precondition, and the gate is red.

- **2026-07-10** — HEADER CORRECTION + RE-DIGEST 2 rows — the `BLOCKED-ON` note described a defect fixed six days earlier (owner github; .github#290, epic .github#266, producers FS.GG.SDD#321 / FS.GG.Rendering#268, ADR-0017): **(a) The header correction — the substance of #290.** This file's `BLOCKED-ON` block claimed the typed validator + the `skill-registry-published` flip were "blocked **ONLY** on the Rendering predicate-grammar alignment (FS.GG.Rendering#77)". That was **stale**, and the file already contradicted itself about it: the 2026-07-05 provenance note ~40 lines above records Rendering#77 as **closed 2026-07-04 (`cf93eabd`)** with both sides emitting the canonical grammar. Re-verified from the producer rather than from either note: `FS.GG.Rendering@main` (`4ba4491`) ships `template/skill-manifest/skill-manifest.json` with **0 of 21** entries containing `&&`, `||`, parens or quotes, and `scripts/generate-skill-manifest.fsx` carries an explicit `C-style → ADR-0017 canonical` normalizer (`normalizeCondition`) so `template.json` can keep the C-style form the `dotnet new` engine requires while the manifest emits the canonical one. `registry = manifest = evaluable` holds. The header now says so, and the two body comments that spoke of rows "**DO** ride on" / "do **NOT** depend on" the alignment are re-tensed — that alignment landed, so it no longer distinguishes the Rendering-owned rows from the Game-owned ones. **What actually remains** before flipping coherence `skill-registry-published` → `true` is `.github`-side only: publish the typed `Fsgg.Registry` validator assertion over this file, then flip. The two guards #290's body named as standing in the way — [#288](https://github.com/FS-GG/.github/issues/288) (stale digests) and [#289](https://github.com/FS-GG/.github/issues/289) (`manifest ⊆ registry`) — are **both closed**. **The flip is still NOT taken here**, per #290's own instruction and the #295 entry below: a green `skill-registry-coherence` is a *precondition* of it, not the flip itself. **(b) The re-digest — not the subject of #290, folded in only because `main` was red.** At `df8b69b` the gate reported two `[digest-matches]` findings on rows this change had no interest in: `fs-gg-sdd-evidence` (`581db14d…`→**`7576a7c5…`**) from **`532aaed`** ([FS.GG.SDD#321](https://github.com/FS-GG/FS.GG.SDD/pull/321), *"derive a render-and-look obligation when a workspace declares a visual surface"*) and `fs-gg-visibility` (`09ca2157…`→**`74f9d3fb…`**) from **`84601a6`** ([FS.GG.Rendering#268](https://github.com/FS-GG/FS.GG.Rendering/pull/268), *"cull visibility occluders by segment, not endpoints"*). Both producers were internally consistent — each moved body hashes to exactly what its own emitted manifest declares — so both producer drift-guards stayed green and the drift was purely in this file; **no producer decision was required**. Reconciled with `scripts/fsgg-skill-registry-check --write --repos-root <dir>` (run against a **scratch repos-root of the six canonical repos symlinked from `origin/main`** — a plain sibling-directory root also picks up in-flight `git worktree`s such as `FS.GG.Game-31` and reads them as extra producers, corrupting the findings). New digests independently confirmed byte-equal to `sha256sum` of the producer bodies. No `schemaVersion` bump (a digest reconcile plus comment text). **No set/count change: 37 rows (16 process + 21 product)** — so, as with every re-digest entry, [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) needed no *count* edit, though it **is** edited here for the coherence-note correction in (a). No `owner`/`source`/`materializes-when` change to any row. **The recurring shape, still unfixed, now on its third instance in two days:** nothing propagates a producer skill-body merge into this file, so a re-digest buys hours. The 2026-07-10 #297 entry below filed it as [.github#299](https://github.com/FS-GG/.github/issues/299) (a producer-release → `.github` dispatch that opens the re-digest PR); it remains **open**, and this entry is the evidence that the manual sweep does not converge. `skill-registry-coherence` should be **green on `main`** once this lands — these two findings were the only ones reported.

- **2026-07-10** — FROZEN-MIRROR — `fs-gg-audio` re-digested `4a469583…` → `3a757374…`; the mirror moved and the canonical adopted (owner fs-gg-game; .github#295, epic .github#266, producer FS.GG.Rendering#250 / adopting PR [FS.GG.Game#37](https://github.com/FS-GG/FS.GG.Game/pull/37), ADR-0022 §6, ADR-0024): `fs-gg-audio` has been **fs-gg-game-owned** since the ADR-0022 P4 migration (#213/#218), while FS.GG.Rendering still ships a **frozen copy** (the two-copies cost, retired at P6) that must stay byte-identical to the canonical body. [Rendering#250](https://github.com/FS-GG/FS.GG.Rendering/pull/250) (*"the audio host seam"*, `c5afb4f`, 2026-07-10 05:32Z) edited that frozen copy and regenerated **Rendering's own** manifest, so each producer's drift-guard stayed green — Rendering's manifest agreed with Rendering's bytes, Game's with Game's — while the two bodies disagreed. Only the cross-repo `frozen-mirror` arm can see that shape, and it did: the gate was **right**, and this is the check working, not a check bug. **Third instance of *the mirror moved and the canonical didn't*** (`fs-gg-game-core` 2026-07-08, `fs-gg-audio` at ADR-0024 step 4, and now). Resolved the same way as the first: Rendering's text is the **newer, correct** one — it teaches the seam #250 actually shipped, and every symbol and path it cites was verified present in the shipped surface (`OpenAlBackend.create : AssetResolver -> IAudioBackend`, `Audio.play : IAudioBackend -> AudioEffect list -> unit`, `Viewer.runAppWithAudio options sink host`, `GeneratedAppHost.audioRequests`, and the `Audio.Core`/`Audio.Host` `.fsi` surfaces) — so **FS.GG.Game adopted it byte-for-byte** and regenerated its manifest (Game#36 / PR #37, `ff54d28`), and this file then re-digested the row from that manifest. **`.github` could not have fixed this alone, and the ordering is load-bearing**: run before Game adopted, `--write` reconciled **0 rows** and the `frozen-mirror` finding survived, because the row is reconciled FROM the *owner's* manifest (ADR-0017) and Game's bytes were still `4a469583…`. Only once #37 merged did `--write` move the row — and the intermediate state (Game adopted, registry stale) surfaces as `[digest-matches]`, not `[frozen-mirror]`, which is the two-step signature of this repair. Reconciled by `scripts/fsgg-skill-registry-check --write --repos-root <dir>`; the row's bytes are the tool's — **registry = manifest = bytes** holds, and `sha256 3a757374d1c7bcf95db95a1c3eb9ffde54647fafd158ff73eac5d97c686d300c` is byte-equal to both `sha256sum FS.GG.Game/template/product-skills/fs-gg-audio/SKILL.md` (canonical) and Rendering's frozen copy. **Set/count does NOT change: 37 rows (16 process + 21 product)**, so — as with every re-digest entry, and unlike #289/#298 — the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) is **not** edited. No `schemaVersion` bump (a digest reconcile: no new field, no tightened rule). No `owner`/`source`/`materializes-when` change to any row. The header's `registry = manifest = bytes` claim, which #295 correctly recorded as **false as written** while the mirror was diverged, is **true again** as of this entry. **`skill-registry-coherence` is now GREEN on `main`** — this was the sole remaining finding after [#297](https://github.com/FS-GG/.github/issues/297) and [#298](https://github.com/FS-GG/.github/issues/298), as the #298 entry directly below predicted. **Deliberately NOT done here** (separately owned, outside this change's declared touch-set): the `skill-registry-published` coherence flip in `dependencies.yml` — [#290](https://github.com/FS-GG/.github/issues/290) owns it and additionally wants the typed `Fsgg.Registry` validator assertion published first; a green gate is a **precondition** of that flip, not the flip itself, and flipping `coherent: true` from this PR would be the same fail-open pattern (#266) #290 explicitly warns against. Follow-up filed against the producer: the adopted body carries a bare `(issue #245)` cross-repo ref that resolves to nothing in a generated product, and **cannot** be corrected here without re-breaking the byte-identical mirror this entry just restored.

- **2026-07-10** — COMPLETENESS — `fs-gg-feedback-report` added (20→21 product); the `declared-completeness` arm's first LIVE catch (owner github; .github#298, epic .github#266, producer FS.GG.Rendering#248 / PR [#251](https://github.com/FS-GG/FS.GG.Rendering/pull/251), ADR-0017/ADR-0014): `fs-gg-feedback-report` — a lifecycle-independent retrospective skill — was vendored into FS.GG.Rendering at `d481e72a` (2026-07-10 **05:52Z**) and declared in its producer manifest `template/skill-manifest/skill-manifest.json`, but never flipped into this catalog. The [.github#289](https://github.com/FS-GG/.github/issues/289) `declared-completeness` assertion (manifest ⊆ registry) merged **twelve minutes later** at `d719357` (PR [#293](https://github.com/FS-GG/.github/pull/293), 06:04Z) and caught it on the very next scheduled run. That timing is the whole point of the entry: #289 was written to drain a **backlog** of four rows that had been invisible for five days, and its first act in production was to catch drift **twelve minutes old** — the fail-open window (epic #266) closing from *days* to *one CI cycle*. This is a real catalog gap, **not a check bug**; diffing the 21 manifest ids against this file confirms `fs-gg-feedback-report` was the only one missing. Reconciled verbatim from Rendering's manifest by `scripts/fsgg-skill-registry-check --write --repos-root <dir>`, which **appends** the row (the #289 capability) with `scope`/`sha256`/`materializes-when` taken from the manifest entry, `source` derived from its `supplied-by`, and `owner` from the declaring repo; per `--write`'s own emitted comment the appended row was then **re-homed** out of the file tail into the product block beside its sibling and annotated. The row's bytes are the tool's — **registry = manifest = bytes** holds: `sha256 4093ccd31db484e16f926cfc7423c0f8049ec110eb5d5b45997b91681ece8170` is byte-equal to `sha256sum FS.GG.Rendering/template/feedback-report/skill/SKILL.md` (7137 bytes). **Two shape notes.** (1) `source` is `template/feedback-report/skill/SKILL.md`, NOT the `template/product-skills/<id>/` convention the other product rows follow — its sibling `fs-gg-feedback-capture` (`template/feedback/skill/`) already breaks it the same way and reconciles fine, because `source` is derived from the manifest's `supplied-by` rather than assumed from the id. (2) The predicate is **`"feedback == true"` with NO `lifecycle` clause**, where `fs-gg-feedback-capture` carries `"feedback == true and lifecycle == spec-kit"`: capture is a spec-kit lifecycle artifact, the report is lifecycle-independent and rides **every** lane with feedback on. Copied verbatim and **not** "normalized" toward its sibling — [.github#292](https://github.com/FS-GG/.github/issues/292) (`73be001`) now asserts `predicate-matches` **equality** against the manifest, so a tidied-up predicate would trip the gate rather than pass silently; the row is annotated in place to say so. **Set/count DOES change: 36 → 37 rows (16 process + 21 product)**, so — as with #289 and unlike every re-digest entry — the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) **is** edited (it names counts, not per-row digests: product `20 → 21`, Rendering-owned `16 → 17`). Note `scripts/check-projection.py` gates that projection against **`dependencies.yml`** only, so these skill counts are unenforced prose — drift here is exactly the class #266 exists to close, and it is corrected by hand in the same PR. No `schemaVersion` bump (a row append, no new field and no tightened schema rule — the #288/#289/#292 precedent). No `owner`/`source`/`sha256`/`materializes-when` change to any existing row. No coherence-state change: `skill-registry-published` stays ❌ as #292 left it — flipping it over a red gate is precisely the #266 fail-open pattern, and [.github#290](https://github.com/FS-GG/.github/issues/290) names this issue as one of three standing between the gate and that flip. The new row is Rendering-owned with a `feedback`-only predicate, so it neither joins nor alters the Rendering#77 grammar dependency. **Deliberately NOT fixed here** (separately owned, red on `main` independently, and both outside this change's declared touch-set): the five producer-re-staled digests ([.github#297](https://github.com/FS-GG/.github/issues/297), landed first — this entry sits above it) and the `fs-gg-audio` frozen-mirror divergence ([.github#295](https://github.com/FS-GG/.github/issues/295)), which needs a **producer** call about which body is canonical and cannot be resolved in `.github` at all. So `skill-registry-coherence` stays red on `main` after this lands, with #295 the sole remaining finding.

- **2026-07-10** — RE-DIGEST 5 rows — the second re-digest in three hours, against a disjoint row set (owner github; .github#297, epic .github#266, producers FS.GG.SDD#316/#317, FS.GG.Rendering#256, ADR-0017): `skill-registry-coherence` went green at `b400843` (05:06Z, the #288 re-digest) and was red again by the `73be001` run with **five fresh `digest-matches` findings** — on five rows #288 never touched. **This is not a regression of #288.** Neither commit that landed after it (`d719357` #293, `73be001` #296) changed the `sha256` of any row now failing; the declared digests are exactly as #288 left them. The producer bodies moved underneath them, all inside the ~2.5h gap. Reconciled from the producer bodies at `main`: `fs-gg-sdd-checklist` (`63aa9c23…`→**`c267d33f…`**), `fs-gg-sdd-lifecycle` (`628e1d53…`→**`64ea0db4…`**) and `fs-gg-sdd-tasks` (`5dad7efa…`→**`c62b4624…`**), all three from **`d8c014b`** (FS.GG.SDD#316, *"derive the per-stage merge policy from the write kind"*, 06:07Z); `fs-gg-sdd-plan` (`a0c20113…`→**`51a95d10…`**) from **`e7c1501`** (FS.GG.SDD#317, *"name the authored line that disposes a stranded id"*, 06:32Z); and `fs-gg-scene` (`7cd388db…`→**`f95e1a56…`**) from **`7f2c5bc`** (FS.GG.Rendering#256, *"ViewerOptions.LogicalSize"*, 06:33Z). **Both producers were internally consistent throughout** — each moved row's canonical body hashes to exactly what its own emitted manifest declares (`.agents/skills/skill-manifest.json` for the four SDD rows, `template/skill-manifest/skill-manifest.json` for `fs-gg-scene`), so every producer drift-guard stayed green and the drift was purely in this file. **No producer decision was required**, unlike [.github#295](https://github.com/FS-GG/.github/issues/295), where two repos disagree about who owns a body. Applied with `scripts/fsgg-skill-registry-check --write --repos-root <dir>`, which rewrites only the 64-hex tokens; `updated:` (already `2026-07-10`, unmoved — same-day) and this entry are the operator's, per the registry protocol. No `schemaVersion` bump (data reconcile, `sha256` only), no set/count change (36 rows: 16 process + 20 product), no `owner`/`source`/`materializes-when` change, no coherence-state change. Projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) needs no edit (it names counts, not per-skill digests, and the counts are unchanged). **The gate stays red after this lands, by design:** two of the run's seven findings are owned elsewhere and are deliberately NOT swept into this diff — `[frozen-mirror] fs-gg-audio` ([.github#295](https://github.com/FS-GG/.github/issues/295), which `--write` cannot fix because both bodies live in producer repos and their owners disagree) and `[declared-completeness] fs-gg-feedback-report` ([.github#298](https://github.com/FS-GG/.github/issues/298), which additionally needs the header's product count corrected 20→21 and the row re-homed out of `--write`'s appended block). All three are blockers of [.github#290](https://github.com/FS-GG/.github/issues/290); this entry clears one. **The recurring shape, still unfixed:** nothing propagates a producer skill-body change into this file. A producer merges, its own manifest regenerates, its own guard stays green, and this registry falls behind until the next `.github` PR trips the gate — so a re-digest buys hours, not a fix. Detection is sound (the `schedule:` trigger the 2026-07-08 entry argued for is what caught both); the missing edge is a **producer-release → `.github` dispatch** that opens the re-digest PR automatically, the fabric `publishing-and-deployment` already describes for package pins. Filed as [.github#299](https://github.com/FS-GG/.github/issues/299) rather than smuggled in here — and scoped there to `[digest-matches]` only, since auto-resolving a `[frozen-mirror]` or `[declared-completeness]` finding would re-introduce #266 inside the fix for it.

- **2026-07-10** — PREDICATE — `materializes-when` was never enforced; `fs-gg-testing` reconciled to Rendering's manifest (owner github; .github#292, epic .github#266, producer FS.GG.Rendering#90 closing the cross-repo request, grammar FS.GG.Rendering#77, ADR-0017/ADR-0022 §6): the gate emitted six finding kinds, and **every one is a predicate over a row's `sha256`/`source` or — since #289 — over its mere presence**. None compared the one field that is neither bytes nor presence: the **emission condition**. So a producer could widen or narrow a skill's `materializes-when` and this catalog silently kept the old one, with every digest green throughout. That matters more than a stale digest: `materializes-when` is what `scripts/skill-union-assert.sh --params` evaluates to decide whether a skill's **absence** from a scaffold is *legitimate* (ADR-0017's `[missing]`/`[unexpected]` classes), so a wrong predicate makes the union gate wrong **in a direction it cannot detect**. One row had diverged: **`fs-gg-testing`**, which this file called `profile == governed` while Rendering's manifest declared `profile in [app, headless-scene, governed, sample-pack, game]` — under `--profile app` the registry asserted the skill *should be absent* while the producer was emitting it. All other 35 rows agreed. **Which side was right (the question #289 deferred as "a cross-repo call"): the manifest.** The row was *faithful when written* — at the seeding reconcile (Feature 238, FS.GG.Rendering#76, commit `c550a75b`) the manifest really did say `(profile == "governed")`. Then **FS.GG.Rendering#90 — a `cross-repo:request` filed FROM this repo and accepted — deliberately widened it** (commit `f4abd68c`, 2026-07-04, *"widen fs-gg-testing materialization beyond governed"*), on the reasoning that the skill body is largely profile-agnostic and `game`/`app` authors were otherwise shipped a test surface and `FS.GG.UI.Testing` with no skill teaching either. No new owner's call was needed: Rendering's owner had already made it, on the record, and the registry simply never flipped — **stale for six days**. Reconciled to the manifest verbatim. **The gate:** a `predicate-matches` finding — for every row a producer manifest also declares, `materializes-when` must equal the manifest's (absent ⇒ `always`, ADR-0017), **failing closed**; `--write` now rewrites a diverged `materializes-when` in place exactly as it rewrites a stale `sha256`, touching nothing else on the row. Compared on a **whitespace-normalized** form (`profile in [a,b]` == `profile in [a, b]` — reformatting is not drift) but **never on grammar**: the C-style form Rendering shipped before **Rendering#77** (closed 2026-07-04, commit `cf93eabd`, which aligned the manifest on the ADR-0017 canonical grammar — both sides have emitted it since) is *semantically* identical and **still reported as a divergence**, because the ADR-0017 evaluator cannot parse it and reads such a predicate as **false**. Normalizing it away would make a producer's regression to an unevaluable predicate compare equal and report green — precisely the #266 fail-open shape, re-introduced inside the fix for it. (Rejecting non-canonical grammar *outright* is a separate tightening, .github#290.) A frozen mirror declares an id twice (ADR-0022 §6), so the row's `owner:` selects the authoritative manifest; two declarers that **contradict each other** are reported and `--write` refuses to pick a side rather than believing whichever repo sorts first. Manifests are now read **once** into a shared index feeding both `declared-completeness` and `predicate-matches`. No `schemaVersion` bump — no new field and no tightened *schema* rule; the strictness is in the **gate**, matching the 2026-07-08 and #289 precedents. No set/count change (36 rows: 16 process + 20 product), no `owner`/`source`/`sha256` change to any row. Projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) needs no edit (it names counts, not per-row predicates, and the counts are unchanged). No coherence-state change: `skill-registry-published` stays as #289 left it. **Not fixed here** (separately owned, and red on `main` independently of this change): five rows re-staled by producer merges, the `fs-gg-audio` frozen-mirror divergence ([.github#295](https://github.com/FS-GG/.github/issues/295)), and `fs-gg-feedback-report` — declared by Rendering's manifest since `d481e72a` and still absent from this catalog, which is `declared-completeness` doing exactly its job.

- **2026-07-10** — COMPLETENESS — 4 missing product rows, and the gate that could not see them (owner github; .github#289, epic .github#266, producers FS.GG.Rendering#125/#128/#130/#132, ADR-0017/ADR-0014): `scripts/fsgg-skill-registry-check` emitted exactly four finding kinds — `digest-matches`, `digest-shape`, `source-exists`, `frozen-mirror` — and **every one is a predicate over a row already declared here**. Nothing asserted the converse. A skill a producer *ships and declares in its manifest* but that is simply **absent** from this catalog was not a finding; it was nothing. Four were: **`fs-gg-collision`**, **`fs-gg-grids`**, **`fs-gg-line-drawing`**, **`fs-gg-visibility`** (all `profile in [game, sample-pack]`, owner `fs-gg-rendering`), authored 2026-07-05 and shipped in `fs-gg-ui-template` **0.1.65-preview.1** — real bodies, digests matching Rendering's manifest, emitted to `.agents/skills/<id>/` by `.template.config/template.json`, so **`dotnet new fs-gg-ui --profile game` had been shipping all four for five days** while ADR-0017's *"single authoritative catalog"* was missing a fifth of the product set and the gate guarding it was structurally unable to say so. This is **ADR-0014's presence-vs-content lesson recurring in the opposite direction**: the check validated the *content of what is listed* and never the *completeness of the list* — the #266 fail-open class, "a missing subject reports green". **The fix:** a `declared-completeness` finding (every skill any producer manifest declares has a row here, matched on `id` alone so Rendering's frozen re-declaration of a Game-owned skill is satisfied by the `fs-gg-game` row) and a `manifest-found` finding (a producer whose manifest vanished or will not parse **fails closed** — otherwise every skill it declares silently drops out of the completeness check, the same fail-open one level up). Producers are **derived, never hardcoded**: the repos named by `source:` prefixes, *plus* any other checkout under `--repos-root` that carries a manifest — so a brand-new producer whose rows are *all* missing is still read, rather than being invisible for want of a row to name it. `--write` now **appends** a missing row (reconciled from the manifest entry: `scope`/`sha256`/`materializes-when` verbatim, `source` from `supplied-by`, `owner` from the repo) as well as rewriting stale digests, then **re-checks from disk** and exits non-zero on anything left, so it can never report success over a registry that is still incoherent. A manifest entry with no `supplied-by` (SDD's process manifest has none) yields no `source:` to derive, so the row is **reported and not appended** — inventing a path would be hand-authoring the bytes this file's header forbids. Reconciled the four rows from `FS.GG.Rendering/template/skill-manifest/skill-manifest.json` @ `85381ba`; registry = manifest = bytes for all four (`3d4788ba…`, `6c61e946…`, `70da6b04…`, `09ca2157…`). **Set/count DOES change: 32 → 36 rows (16 process + 20 product)**, the first count change since the 13→16 catch-up — so unlike every re-digest entry above, the projection [`docs/registry/compatibility.md`](../docs/registry/compatibility.md) **is** edited (it names counts, not per-skill digests, and the counts moved: `12 → 16` Rendering-owned, `16 → 20` product). No `schemaVersion` bump — no new field and no tightened *schema* rule; the strictness is in the **gate**, matching the 2026-07-08 precedent that added checks 1-4 as "data reconcile + tooling". No `owner`/`source`/`materializes-when` change to any existing row, no coherence-state change: the four new rows carry Rendering's C-style-normalized predicates like the 12 beside them, so they **join** the `skill-registry-published` dependency on the Rendering#77 grammar alignment rather than altering it. **Found but NOT fixed here** (a distinct fail-open, same epic): the gate still never compares a row's `materializes-when` against the manifest's, and `fs-gg-testing` has diverged — this file says `profile == governed`, Rendering's manifest says `profile in [app, headless-scene, governed, sample-pack, game]`. Filed separately; resolving it is a cross-repo call about which side is right, not a set-completeness reconcile.

- **2026-07-10** — RE-DIGEST 5 rows — the first drift the SCHEDULE caught, and nobody actioned (owner github; .github#288, producers FS.GG.SDD#231/#271/#186/#187, FS.GG.Rendering#192, ADR-0017): `skill-registry-coherence.yml` went **green 07-08, red 07-09**, and stayed red on `main` until this entry. Five canonical bodies moved in producer merges with no `.github` commit — exactly the failure mode the 2026-07-08 entry above predicted when it justified the `schedule:` trigger (*"a producer body change stales this registry with no `.github` PR, so PR-only triggers would never fire"*). The schedule fired and said so; the gap was **response**, not detection. Reconciled from the producer bodies at `main`: `fs-gg-sdd-evidence` (`9294560c…`→**`581db14d…`**, FS.GG.SDD#231 — the shipped example made a fixpoint of its own generators), `fs-gg-sdd-refresh-agents` (`557ec14f…`→**`537dd263…`**, FS.GG.SDD#271 — dropped the computed-but-unread view `outputDigest`), `fs-gg-sdd-ship` (`45e67e5d…`→**`c98e3e86…`**, FS.GG.SDD#186 — `fsgg-sdd ship` committed compact verdict, ADR-0026), `fs-gg-sdd-troubleshooting` (`96eb7eaf…`→**`e1d92bc7…`**, FS.GG.SDD#187 — lifecycle authoring papercuts), and `fs-gg-ui-widgets` (`a0c0ce7a…`→**`e9353670…`**, FS.GG.Rendering#192 — cut `CustomControlDefinition`'s dead callbacks). **Every producer manifest agreed with its own body (0 disagreements across all 32 rows)**, confirming — as in the 2026-07-08 sweep — that the drift was purely in this file: each producer's own drift-guard (`registry skill-manifest --check`, `generate-skill-manifest.fsx --check`) was internally consistent and green throughout. Applied with `scripts/fsgg-skill-registry-check --write --repos-root <dir>`, which rewrites only the 64-hex tokens; `updated:` and this entry are the operator's, per the registry protocol. The header comment above asserting **registry = manifest = bytes** was false as written from 07-09 until this commit; it is true again. No `schemaVersion` bump (data reconcile, `sha256` only), no set/count change (32 rows: 16 process + 16 product), no `owner`/`source`/`materializes-when` change, no coherence-state change. Projection `docs/registry/compatibility.md` needs no edit (it names counts, not per-skill digests, and the counts are unchanged). **Landed alone, ahead of .github#289** (which teaches the check a `declared-completeness` finding and appends the 4 product rows the catalog is missing), so that structural change starts from a clean, green baseline rather than sweeping these five corrections into its diff.

- **2026-07-08** — MASS RE-DIGEST — 15 stale rows reconciled + the invariant finally GATED (owner github; .github#247, producers FS.GG.Game#23/#21, FS.GG.Rendering#161, ADR-0017/ADR-0022 §6): `skills.yml` declares each row's `source:` and the `sha256:` it was reconciled from, and every entry above restates **registry = manifest = bytes** — but **nothing enforced it**. No workflow read this file. Each producer drift-guards its *own* manifest against its *own* bodies (internally consistent while being wrong), so a producer merge staled the registry with no `.github` commit and no red check. By the time this was measured, **14 of 32 rows were stale** — across *all three* owners, not one careless repo. Reconciled all of them from the producer bodies at `main`, plus `fs-gg-game-core` (16th row, corrected in the same sweep — see below): `fs-gg-sdd-authoring-contracts` (8a49d3a2…→d7dfd453…),`fs-gg-sdd-charter` (9f06660b…→9855bd3c…) `fs-gg-sdd-checklist` (3965baf4…→63aa9c23…),`fs-gg-sdd-clarify` (69343132…→09d2ed97…) `fs-gg-sdd-evidence` (b79c867a…→9294560c…),`fs-gg-sdd-plan` (c3a04c98…→a0c20113…) `fs-gg-sdd-specify` (fa655f0b…→55c65444…),`fs-gg-sdd-tasks` (97f7a8d2…→5dad7efa…) `fs-gg-sdd-troubleshooting` (03c65640…→96eb7eaf…),`fs-gg-scene` (37ef7800…→7cd388db…) `fs-gg-elmish` (30406f46…→4662d8ff…),`fs-gg-skiaviewer` (331b9986…→6ea4c582…) `fs-gg-keyboard-input` (09eab05c…→fbad9e80…),`fs-gg-game-core` (16fd32b7…→f9f732cd…) `fs-gg-testing` (b7661998…→2b481203…). Every producer manifest agreed with its own body (0 disagreements), confirming the drift was purely in this file. **`fs-gg-game-core` was drift in the OTHER direction:** ADR-0022 P5 moved the sim primitives (`Rng`/`FixedStep`/`Pathfinding`/`SpatialGrid`) out of `FS.GG.UI.Canvas` into `FS.GG.Game.Core`; FS.GG.Rendering updated its **frozen copy** at the extraction, the **canonical** body in FS.GG.Game never was, and this registry declared the stale canonical. The mirror was right and the source wrong — fixed by adopting Rendering's bytes into FS.GG.Game (FS.GG.Game#23), which is why the row moves `16fd32b7…` → `f9f732cd…`. **The gate:** `scripts/fsgg-skill-registry-check` + `.github/workflows/skill-registry-coherence.yml` now assert, per row, (1) `source:` exists, (2) `sha256(canonical body) == sha256:`, (3) for `owner: fs-gg-game` rows, Rendering's frozen copy is byte-identical to the canonical body (ADR-0022 §6 two-copies cost, retired at P6; derived, so a retired copy is simply absent, not a finding), and (4) the declared digest is well-formed 64-hex (an unquoted all-digit digest parses as an int). It checks out the three producer repos (public, no token) and runs on PR, `push: main`, and **on a schedule** — a producer body change stales this registry with no `.github` PR, so PR-only triggers would never fire. `--write` reconciles stale digests in place, touching only the 64-hex tokens. No `schemaVersion` bump (data reconcile + tooling), no set/count change (32 rows: 16 process + 16 product), no `owner`/`source`/`materializes-when` change, no coherence-state change. Projection `docs/registry/compatibility.md` needs no edit (it names counts, not per-skill digests, and the counts are unchanged).

- **2026-07-08** — RE-DIGEST `fs-gg-audio` — body re-pointed `FS.GG.UI.Canvas` → `FS.GG.Audio.Core` (owner github; producer FS.GG.Game#21 closing FS.GG.Game#20, mirror FS.GG.Rendering#161 closing Rendering#160, epic .github#234, ADR-0024 step 4): reconciled the one product row whose canonical body changed. Canvas `0.3.0-preview.1` (FS.GG.Rendering#158, ADR-0024) **retired** the Audio request surface (`AudioEffect`/`SoundId`/`TrackId`/`Audio.*`) from `FS.GG.UI.Canvas`; the vocabulary now lives only in the standalone `FS.GG.Audio.Core` (`fs-gg-audio` **package** contract, `0.1.0-preview.1`). The skill body still taught `open FS.GG.UI.Canvas`, so an author following it could not resolve `AudioEffect` — a breaking removal in a library silently invalidating a shipped skill. FS.GG.Game (the row's `owner` since the ADR-0022 P4 migration) re-pointed the body to `FS.GG.Audio.Core` and regenerated its producer manifest `FS.GG.Game/template/skill-manifest/skill-manifest.json` (`dotnet fsi scripts/generate-skill-manifest.fsx`, drift-guarded in FS.GG.Game `gate.yml`). Updated the row's `sha256` (`64cbe6ef…` → **`4a469583…`**) to match that manifest — **registry = manifest = bytes** preserved, byte-equal to `sha256sum FS.GG.Game/template/product-skills/fs-gg-audio/SKILL.md`. FS.GG.Rendering's **frozen copy** (the ADR-0022 §6 two-copies cost, retired at P6) was mirrored byte-identically in Rendering#161 along with its own manifest, so all three copies agree at `4a469583…`; Rendering also swapped the bundled doc copy `template/base/docs/api-surface/Canvas/Audio.fsi` → `Audio.Core/Audio.fsi` and added `AudioSkillSurfaceTests` (member-resolution + bundled-namespace drift gate), the guard whose absence let the stale body ship. The other 19 product/process digests are unchanged. No `schemaVersion` bump (data reconcile, `sha256` only), no set/count change (still 16 product rows, 4 owned by `fs-gg-game`), no `materializes-when` change (`profile in [game, sample-pack]`), no `owner`/`source` change, no coherence-state change. Projection `docs/registry/compatibility.md` needs no edit (it names the count, not per-skill digests, and the count is unchanged) — same as the 2026-07-04 `fs-gg-sdd-getting-started` re-digest.

- **2026-07-06** — OWNERSHIP MIGRATED — 4 GAME product rows `fs-gg-rendering → fs-gg-game` (ADR-0022 P4) (owner github; epic .github#213/#218, producer FS.GG.Game#4, ADR-0022): migrated the four game product skills — `fs-gg-game-core`, `fs-gg-audio`, `fs-gg-persistence`, `fs-gg-model-swap` (all `scope: product`, `materializes-when: "profile in [game, sample-pack]"`) — from `owner: fs-gg-rendering` to **`owner: fs-gg-game`**, re-pointing each `source` to `FS.GG.Game/template/product-skills/<id>/SKILL.md` and reconciling each `sha256` from **FS.GG.Game's** new producer manifest `FS.GG.Game/template/skill-manifest/skill-manifest.json` (schema `skill-manifest` v1; generated by `dotnet fsi scripts/generate-skill-manifest.fsx`, CI-drift-guarded in FS.GG.Game `gate.yml`; merged FS.GG.Game#4). Each declared `sha256` is byte-equal to `sha256sum FS.GG.Game/template/product-skills/<id>/SKILL.md`, so **registry = manifest = bytes** holds for all 16 product rows (now split 12 fs-gg-rendering + 4 fs-gg-game). The bodies moved **BYTE-IDENTICALLY**: FS.GG.Rendering STILL ships frozen copies from `dotnet new fs-gg-ui --profile game` (the accepted two-copies cost, ADR-0022 §6, retired by the P6 provider epic) — so no Rendering change, and its manifest/tests are untouched. **Two digests CORRECTED in the same move:** the registry had carried STALE digests for `fs-gg-game-core` (`87b9c55c…` → **`16fd32b7…`**) and `fs-gg-model-swap` (`68edbe46…` → **`a48df195…`**) — their bodies changed in Rendering Feature 250 (0.1.67) and were never reconciled; the migrated bytes are the live ones, so the migration also closes that drift. The four migrated rows carry `materializes-when` in the ADR-0017 canonical grammar authored directly in FS.GG.Game's catalog (no C-style `template.json` to normalize yet — the `dotnet new fs-gg-game` package is P6), so **unlike the 12 rows still owned by fs-gg-rendering they do NOT depend on the Rendering#77 grammar alignment.** No `schemaVersion` bump (data reconcile, owner/source/sha256 fields only). Projection `docs/registry/compatibility.md` (§Skill registry dependency-graph line + `skill-registry` contract row + `skill-registry-published` + `game-extraction` coherence rows) updated in the same change.

- **2026-07-05** — PRODUCT ROWS ADDED — `fs-gg-audio` + `fs-gg-persistence` + `fs-gg-model-swap` (13→16; manifest catch-up) (owner github; Rendering#113 closing, Features 243/244 prior, ADR-0017): reconciled the three product rows by which the registry snapshot had fallen behind FS.GG.Rendering's published producer manifest `template/skill-manifest/skill-manifest.json`. **Two were pre-existing un-flipped reconciles:** `fs-gg-audio` (Rendering Feature 243, #92 — the `AudioEffect` / record-only-interpret request skill, `sha256 64cbe6efbfb75af430906832e918d1bb07279a6ffbcb7f3096eafb2b0998803c`) and `fs-gg-persistence` (Rendering Feature 244, #93 — the `PersistenceEffect` / record-only-interpret request skill, `sha256 af107dec4e0a3464bb39d2dadd3101872bdf47351945151854d52f17234d1619`) both shipped in the manifest but were never flipped here. **One is new:** `fs-gg-model-swap` (Rendering#113 — the starter model-swap recipe: re-export spine, durable vs replaceable vs re-point files, governance substring-trap + record-label-collision footguns, `sha256 68edbe4684224bdbbe10ad71d6004adb0153b950b34dc7b8bae6118be6ab38fd`). All three are `scope: product`, `owner: fs-gg-rendering`, `materializes-when: "profile in [game, sample-pack]"` (the sim-profile gate, alongside `fs-gg-game-core`); each declared `sha256` is byte-equal to `sha256sum FS.GG.Rendering/template/product-skills/<id>/SKILL.md` (the `Fsgg.SkillMirror` canonical-body digest), so **registry = manifest = bytes** holds for all 16 product rows. No `schemaVersion` bump (data reconcile, not a schema change); no coherence-state change (the enforcing `skill-registry-published` flip remains blocked only on Rendering's predicate-grammar alignment, Rendering#77). Projection `docs/registry/compatibility.md` (dependency-graph line + `skill-registry` contract row + `skill-registry-published` coherence row) updated in the same change: product count 13→16.

- **2026-07-04** — CONDITION WIDENED — `fs-gg-project` `lifecycle == spec-kit` → `profile in [app, headless-scene, governed, sample-pack, game]` (ADR-0017 §C2 seam CLOSED) (owner github; Rendering#91 / PR #101, reconcile cross-repo #183, ADR-0017): Rendering widened `fs-gg-project` (the product-orientation umbrella skill) from a `lifecycle == spec-kit`-only gate to a profile-scoped, **lifecycle-independent** predicate, so the skill now materializes on **every** lane whose profile matches (Option A of the ADR-0017 §C2 resolution). Reconciled the `materializes-when` verbatim from Rendering's published producer manifest `template/skill-manifest/skill-manifest.json` (`fs-gg-project` row); `sha256 3dc0b55b9681c99485c39cb0eabd1effaba70061d8a9e5eb57f270061c4aa16d` + `source` (manifest `supplied-by` path) **unchanged**, so registry = manifest = bytes holds. This CLOSES the ADR-0017 **§C2** supply gap — previously, under the `sdd`/`none` lanes NEITHER producer supplied `fs-gg-project` (SDD's `SeededSkills` deliberately excludes the product-internal project skill), a genuine gap the "declared ∧ absent" tolerance hid; the widened condition supplies it at source on all lanes, making SDD's `SeededSkills` exclusion moot for the orientation-skill gap. The row loses its bespoke per-lane `supplied-by` seam annotation and is now an ordinary profile-gated product row (same predicate as `fs-gg-scene`). No `schemaVersion` bump (data reconcile), no product-count change (still 13). ADR-0017 §C2 + §Consequences(Rendering) marked resolved; projection `docs/registry/compatibility.md` (`skill-registry` contract row) updated in the same change.

- **2026-07-04** — PRODUCT ROW ADDED — `fs-gg-game-core` (12→13; off-by-one reconcile fix) (owner github; Rendering#76 Feature 238, epic #163, ADR-0017): the .github#168 provisional snapshot recorded **12** product rows, but Rendering's published producer manifest `template/skill-manifest/skill-manifest.json` has always declared **13** — `fs-gg-game-core` (`scope: product`, `materializes-when: "profile in [game, sample-pack]"`, `sha256 87b9c55cc67bb0a25bc0fba6e7eb08d851a703ac253d6b99c6b517ebb9e5c866`, the deterministic fixed-step / seeded-RNG / AABB / culling simulation-core skill) was omitted. Reconciled it verbatim from the manifest; the declared `sha256` is byte-equal to `sha256sum FS.GG.Rendering/template/product-skills/fs-gg-game-core/SKILL.md` (the `Fsgg.SkillMirror` canonical-body digest), so registry = manifest = bytes holds for all 13 product rows. This is the highest-impact omission because `fs-gg-game-core` materializes for the **default** scaffold lane (`--provider rendering` defaults to `profile=game`), so every standard game scaffold vendored a skill the catalog did not list — a materialized-but-undeclared gap of exactly the class ADR-0017's condition-aware union gate exists to catch (and which the gate cannot currently catch, since the enforcing flip is still blocked on Rendering's predicate-grammar alignment, Rendering#77). No `schemaVersion` bump (data reconcile, not a schema change). Projection `docs/registry/compatibility.md` (dependency-graph line + `skill-registry` contract row) updated in the same change: product count 12→13.

- **2026-07-04** — RE-DIGEST `fs-gg-sdd-getting-started` (owner github; SDD#119 closing SDD#115, ADR-0017): reconciled the one process row whose canonical body changed. SDD#119 corrected a stale in-body count in `fs-gg-sdd-getting-started/SKILL.md` ("15"→"16" `fs-gg-sdd-*` process skills — the set had grown 15→16 with `fs-gg-sdd-troubleshooting` in SDD#108), regenerating SDD's emitted process manifest `.agents/skills/skill-manifest.json`. Updated the row's `sha256` (`a6ea0b8…`→`498a888…`) to match that emitted manifest — **registry = manifest = bytes** preserved. The other 15 process digests are unchanged. No `schemaVersion` bump, no set/count change (still 16), no `materializes-when` change (`always`), no coherence-state change. Projection `docs/registry/compatibility.md` needs no edit (it names the count, not per-skill digests, and the count is unchanged).

- **2026-07-04** — PROCESS ROWS RECONCILED FROM SDD's EMITTED MANIFEST — 15→16, no longer provisional (owner github; SDD#111 closing cross-repo #109, epic #163, ADR-0017): the process half is now producer-emitted, not digested-from-canonical-bodies. Reconciled all process rows against SDD's emitted process manifest `.agents/skills/skill-manifest.json` (schema `skill-manifest` v1; regenerable via `fsgg-sdd registry skill-manifest --write|--check`, drift-guarded against `SeededSkills.skillNames` + authored `SKILL.md` digests). The set grew **15→16**: **added** `fs-gg-sdd-troubleshooting` (`sha256 03c6564…`, `scope: process`, `always`; new in SDD#108), and **re-digested** four bodies changed by features 070/071 after the .github#168 provisional snapshot — `fs-gg-sdd-checklist` (`01d54e8…`→`3965baf…`), `fs-gg-sdd-clarify` (`6900858…`→`6934313…`), `fs-gg-sdd-lifecycle` (`a6f2f22…`→`628e1d5…`), `fs-gg-sdd-tasks` (`17c26ba…`→`97f7a8d…`). The other 11 process digests are unchanged (byte-verified against the emitted manifest). Each `sha256` is the same `Fsgg.SkillMirror` canonical-body digest the registry declares, so **registry = manifest = bytes** for the process half. All 16 remain `materializes-when: always` (canonical grammar). This CLEARS blocker (1) of coherence `skill-registry-published`; the enforcing flip now waits ONLY on Rendering's predicate-grammar alignment (FS.GG.Rendering#77). No `schemaVersion` bump. Projection `docs/registry/compatibility.md` (§Skill registry) + `dependencies.yml` (`github ← sdd` edge, `skill-registry` surface/validator, `skill-registry-published` note/resolved_by/tracking) updated in the same change.

- **2026-07-04** — CATALOG CREATED (`schemaVersion: 1`) — PRODUCT-COMPLETE, PROCESS-PROVISIONAL (owner github; .github#168, epic #163, ADR-0017): first cut of `registry/skills.yml`. **12 product skills** (owner `fs-gg-rendering`) reconciled verbatim from Rendering's published producer manifest `template/skill-manifest/skill-manifest.json` (Feature 238 / FS.GG.Rendering#76, closes Rendering#71) — authoritative `sha256` + emission condition; `materializes-when` recorded in the **ADR-0017 canonical grammar**, NORMALIZED from the C-style form the manifest currently ships (grammar-alignment tracked as FS.GG.Rendering#77). The `fs-gg-project` seam (ADR-0017 §C2) is recorded honestly: `materializes-when: lifecycle == spec-kit` + `supplied-by: {spec-kit: fs-gg-rendering, sdd: none}` — under the `sdd` lane it is legitimately unsupplied (condition false), not a blanket-tolerated gap. **15 process skills** (owner `fs-gg-sdd`, `materializes-when: always`) are **PROVISIONAL**: their `sha256` is computed from SDD's **canonical** `.claude/skills/fs-gg-sdd-*/SKILL.md` bodies (the bytes `SeededSkills.fs` seeds — which deliberately excludes the product-internal `fs-gg-sdd-project`), pending SDD emitting its own process producer manifest. (The `SkillManifest` contract TYPES landed via SDD#60 / spec `057-skill-manifest-contract` — types only; the process-manifest EMISSION is a separate SDD deliverable that follows P1 and is not yet authored.) Every predicate parses + evaluates under the gate evaluator (`scripts/skill-union-assert.sh`), and a `profile=game, lifecycle=sdd, feedback=false` scaffold reproduces the ADR-0017 investigation exactly: 8 of 12 product skills materialize, the other 4 (`fs-gg-testing`, `fs-gg-samples`, `fs-gg-feedback-capture`, `fs-gg-project`) are justified off-lane absences. The typed `Fsgg.Registry` validator + the enforcing flip (`skill-registry-published` → coherent) are P2-blocked; see `dependencies.yml`.
