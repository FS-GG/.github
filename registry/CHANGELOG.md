# Registry changelog

Reverse-chronological log of changes to [`dependencies.yml`](dependencies.yml) — the FS-GG
cross-repo contract & dependency registry. Its human projection is
[`../docs/registry/compatibility.md`](../docs/registry/compatibility.md).

**Protocol.** Every change to `dependencies.yml` **prepends one dated entry** at the top of
the Entries list below (newest first) and sets the file's `updated:` date to match. Entries
follow a loose `HEADER (owner; refs): body` grammar — name the contract/coherence id(s)
touched, the owner, and the issue/ADR refs. One entry per registry change keeps PR diffs
reviewable and avoids the merge conflicts the former single-line comment caused across the
org's parallel sessions.

This file replaces the ~42 KB single-line trailing comment that used to live on
`dependencies.yml`'s `updated:` field (freeze migrated verbatim below; see .github#129 /
review M4). The typed validator (`fsgg-sdd registry validate`) never saw that comment, so
no gate behaviour changes — this is purely how humans record the log.

## Entries

- **2026-07-07** — `fsgg-sdd-orchestrator-axis` **`coherent: false → true`** (orchestrator-axis re-cohere — both halves in lockstep at `minimum-fsgg-sdd` 0.6.0) (owner sdd; refs FS.GG.Templates#99 CLOSED via PR#100 merge eb3cbeb, FS.GG.SDD#121 + tag v0.6.0, ADR-0008, .github#87): the 2026-07-04 re-open (recorded below with the game-extraction landings) advanced the orchestrator-axis minimum coherent CLI `minimum-fsgg-sdd.version: 0.4.0→0.6.0` because FS.GG.SDD v0.6.0's feature 073 seeds a `.gitignore` for regenerable output into scaffolded products (0.6.0 is the oldest published CLI that does; 0.5.0 did not), growing the expected seeded surface and flipping this row false until **both** halves re-mirrored. **Registry half** was already done 2026-07-04 (FS.GG.SDD.Cli 0.6.0 LIVE on the org feed + nuget.org; `minimum-fsgg-sdd` pinned 0.6.0, publish-before-flip FR-007). **Provider half** is now confirmed done: FS.GG.Templates re-mirrored `providers/rendering.providers.yml` `minimumFsggSdd.version: 0.4.0→0.6.0` via **PR#100** (merge eb3cbeb, 2026-07-05, closing FS.GG.Templates#99), so **descriptor == registry == 0.6.0**, and the Templates **composition gate is green on main** across every subsequent commit (latest 033d85f, 2026-07-06 — the FS.GG.Game-extraction 0.2.0 source re-pin), exercising a full-stack scaffold with the pinned-minimum CLI. Both halves at 0.6.0 ⇒ the axis is coherent. Bookkeeping-only flip: no contract `version`/`package-version`/edge change and **no `schemaVersion` bump** — one `coherent:` flip + the row's `summary`/`resolved_by`/`impact`/`tracking` prose. `fsgg-sdd registry validate` → valid/0 diagnostics; projection `docs/registry/compatibility.md` coherence row updated in the same change.
- **2026-07-06** — `game-extraction` **`coherent: true`** (P5 publish-and-flip) + NEW contracts `game-sim-core` / `game-scene-adapter` + `fs-gg-ui-template` **0.1.64/0.1.70 → 0.2.0-preview.1** (owner game/rendering; refs .github#213 epic, .github#219 P5, FS.GG.Game v0.1.0-preview.1, FS.GG.Rendering#155 + v0.2.0-preview.1, ADR-0022): the extraction is **resolved**, publish-before-flip (FR-007) throughout. (1) **FS.GG.Game.Core + FS.GG.Game.Render 0.1.0-preview.1 published** to the org feed **and** nuget.org (release run 28803118900, both "Your package was pushed") — recorded as the new `game-sim-core` (BCL-only bottom layer: Rng/FixedStep/Pathfinding/SpatialGrid/Geometry/Primitives) and `game-scene-adapter` (FS.GG.Game.Render → FS.GG.UI.Scene) contracts. (2) The **Canvas + Scene SemVer majors released** as the FS.GG.UI coherent set **0.2.0-preview.1** (FS.GG.Rendering#155, release run 28807190647; ApiCompat gate green on the removals; the frozen `--profile game` fragments re-homed to consume `FS.GG.Game.Core` on the `$(FsGgGameVersion)` axis; the generated-product template-instantiation test built green). `fs-gg-ui-template` framework `version` 0.1.64→0.2.0, `package-version` 0.1.70→0.2.0 (re-coupled — a real src/ release), `package-tag` fs-gg-ui-template/v0.2.0-preview.1. (3) New dependency edges: **`rendering → game`** (`game-sim-core@0.1.0-preview.1` — template game/sample-pack sim source) and **`game → rendering`** (`game-scene-adapter@0.1.0-preview.1 → FS.GG.UI.Scene@0.2.0-preview.1` — the adapter edge); the `templates → rendering` edge bumped to `fs-gg-ui-template@0.2.0-preview.1`. The one-way rule holds: Game.Core reaches up to nothing; Game.Render reaches up to Scene; Rendering reaches up to nothing. **Still open:** `game-starter-two-copies` persists (retired at **P6** by the consumer `game` scaffold provider). No `schemaVersion` bump; additive contracts + edges + prose, one `coherent:` flip. `fsgg-sdd registry validate` → valid; projection `docs/registry/compatibility.md` + `docs/architecture.md` updated in the same change.
- **2026-07-06** — NEW coherence row `game-starter-two-copies` (`coherent: false`) + `game-extraction` P4 update — FS.GG.Game P4 skill-migration + `--profile game` freeze (owner game; refs .github#213 epic, .github#218 P4, FS.GG.Game#3 adapter, FS.GG.Game#4 + .github#225 skill migration, ADR-0022 §6): records the P4 freeze. The consumer `game` scaffold provider is out of scope for the extraction epic, so `dotnet new fs-gg-ui --profile game` in FS.GG.Rendering is **frozen** as-is (not re-sourced from FS.GG.Game) — leaving the game starter + the 4 game product skills in **two** places for the epic's duration: Rendering's frozen `--profile game` (template.json sources + product-skills + collision/grids/line-drawing/visibility fragments, still `open`ing `FS.GG.UI.Scene`/`Canvas`) AND FS.GG.Game's producer content (`template/product-skills` + `skill-manifest`; skill bytes byte-identical, FS.GG.Game#4). No consumer contract change between the copies (equal `sha256`; `--profile game` behaviour unchanged); the sole cost is duplicated maintenance. **Distinct retirement:** `game-extraction` flips at **P5** (packages published, majors released); `game-starter-two-copies` persists past P5 until the **P6** sequel provider epic ships the consumer `game` provider and retires the frozen profile. Also updated the `game-extraction` row (`summary`/`impact`/`tracking`) to reflect the P4 landings — the `FS.GG.Game.Render` Scene adapter (FS.GG.Game#3) + the skill-ownership migration (owner rendering → game, byte-identical) — and to move the "physical Canvas/Scene removal" from P4 to P5. No `schemaVersion` bump; additive `coherence:` row + prose. `fsgg-sdd registry validate` → valid/0 diagnostics; `check-projection.py` green. Projection `docs/registry/compatibility.md` (coherence table: new `game-starter-two-copies` row + `game-extraction` row update) + `docs/architecture.md` §4.5 (P4 status) updated in the same change.
- **2026-07-06** — NEW coherence row `game-extraction` (`coherent: false`) — FS.GG.Game P2 stand-up (owner game; refs .github#213 epic, .github#216 P2, ADR-0022): records the extraction of the render-independent game-simulation subsystem out of FS.GG.Rendering into the new sixth component **FS-GG/FS.GG.Game** (created public this landing) and its bottom layer **FS.GG.Game.Core** — a BCL-only (FSharp.Core-only) deterministic sim core reaching up to nothing, holding the moved `Rng`/`FixedStep`/`Pathfinding`/`SpatialGrid` (from `FS.GG.UI.Canvas`) + the `Geometry` collision module (from `FS.GG.UI.Scene`, P0 Option D) reimplemented over Game.Core's own BCL-only `Point`/`Rect`. Builds + tests **headless** (71/71 Expecto, zero Skia / zero Scene); public surface baseline committed (11 types); coordination kit + labels distributed (ADR-0019). **No `FS.GG.Math` leaf** (P0 #2 closed). `coherent:false` is the standing request until **P5 publish-and-flip**: the packages are not yet on the org feed, and the forced SemVer majors on **both** `FS.GG.UI.Canvas` and `FS.GG.UI.Scene` are **prepared, not released** — the physical removal is deferred to P4 (skill migration + freeze) / P5 (publish) because the frozen `--profile game` collision fragment still `open`s `FS.GG.UI.Scene.Geometry` and Package.Tests still assert those surfaces. Per-package `contracts:` rows (`game-sim-core`, `game-scene-adapter`) + the `Game.Render → FS.GG.UI.Scene` edge finalize against live versions at P4/P5 (ADR-0007). No `schemaVersion` bump; additive `coherence:` row only. Projection `docs/registry/compatibility.md` coherence table updated in the same change.
- **2026-07-06** — `fs-gg-ui-template` PACKAGE 0.1.69→0.1.70-preview.1 (template-only) + truly completes `scaffold-provider-identifier` (owner rendering; refs cross-repo FS.GG.Rendering#152 from FS.GG.SDD, FS.GG.Rendering#153, SDD#150, release run 28775303484): a **TEMPLATE-ONLY** release fixing a **second instance** of the #142/#149 raw-slug identifier-leak class that the 0.1.69 gate could not see. #149 shipped the `ScaffoldIdentifierLeakGuardTests` gate but scoped it to **`src/` only** and anchored the token at the **start** of an identifier — so a `let readProductFile` helper in the game/sample-pack **TEST** project (`BehaviorTests.fs`), with `Product` **embedded** mid-identifier, still scaffolded `let readRoquelike-DungeonCrawlerFile` → **FS0010** at `BehaviorTests.fs(215,30)` for a hyphenated name (caught by the SDD composition smoke running against the shipped 0.1.69, SDD#150). FS.GG.Rendering#153 routes the binding through `AppRoot`/`approot` (`effectiveIdentifier`, hyphen-free) and **widens the gate**: it now scans `template/base/tests` + `template/fragments/*/tests` and matches `product`/`Product` embedded **anywhere** in a declaration identifier (not just leading), with the embedded shape added to the synthetic self-test and `BehaviorTests.fs` to the enumeration backstop. **No `src/` change**, so the framework `version` pin **HELD at 0.1.64-preview.1** (the 16 `FS.GG.UI.*` libs byte-identical, packed @pin as `--skip-duplicate` no-ops) while the package **decouples**: `package-version` 0.1.69→**0.1.70-preview.1**, `package-tag` → `fs-gg-ui-template/v0.1.70-preview.1` (Feature-209 coherence: template fsproj `<Version>` == the `v*` release tag == 0.1.70). All release-only gates green (Package consumption + Generated-product template-instantiation, which scaffolds+builds+tests the shipped template) + org-feed/nuget.org publish; publish-before-flip (FR-007) satisfied — package LIVE before this flip. The `templates → rendering` edge stays `@0.1.64-preview.1`; FS.GG.Templates re-pins its provider `source: 0.1.69→0.1.70-preview.1`. Coherence `scaffold-provider-identifier` updated (the 0.1.69 "two-step complete" was itself premature — corrected to a **three-step** history; the honest lesson recorded: a static "green" is only as complete as the gate's scope + patterns). No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Projection `docs/registry/compatibility.md` (versioned-contracts `fs-gg-ui-template` row + the coherence row) updated in the same change.
- **2026-07-06** — `fs-gg-ui-template` PACKAGE 0.1.68→0.1.69-preview.1 (template-only) + completes `scaffold-provider-identifier` (owner rendering; refs cross-repo FS.GG.Rendering#149 from FS.GG.SDD, FS.GG.Rendering#150, SDD#150, release run 28772488214): a **TEMPLATE-ONLY** release that **completes** the hyphenated-name scaffold-compiles guarantee `scaffold-provider-identifier` claimed at 0.1.66. #142's identifier split rewired `module`/type/`open` positions but **missed** a residual `let productDefectMessage` binding in the game/sample-pack `EvidenceCommands.fs` whose IDENTIFIER still derived from the raw lowercased slug — so a legal-but-hyphenated product name (`Roquelike-DungeonCrawler`) still failed **FS0010** at 0.1.66–0.1.68, caught downstream by the SDD composition-acceptance smoke (SDD#150). FS.GG.Rendering#150 routes the binding through `approot` (`effectiveIdentifierLower`, the hyphen-free derived namespace) while the `product-defect` diagnostic-class **string** stays product-slug-imprinted, and adds the always-on `ScaffoldIdentifierLeakGuardTests` static gate (the repeatable backstop #142 shipped without — it fails at template-author time if any `product`/`Product` token begins an F# declaration identifier). **No `src/` change**, so the framework `version` pin **HELD at 0.1.64-preview.1** (the 16 `FS.GG.UI.*` libs are byte-identical, packed @pin as `--skip-duplicate` no-ops) while the package **decouples**: `package-version` 0.1.68→**0.1.69-preview.1**, `package-tag` → `fs-gg-ui-template/v0.1.69-preview.1` (Feature-209 coherence: template fsproj `<Version>` == the `v*` release tag == 0.1.69). All release-only gates green (Package consumption + Generated-product template-instantiation) + org-feed/nuget.org publish; publish-before-flip (FR-007) satisfied — package LIVE before this flip. The `templates → rendering` edge stays `@0.1.64-preview.1` (framework pin unchanged; template-only); FS.GG.Templates re-pins its provider `source: 0.1.66→0.1.69-preview.1`. Coherence `scaffold-provider-identifier` updated (0.1.66 "green" corrected to two-step; completed at 0.1.69). No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Projection `docs/registry/compatibility.md` (versioned-contracts `fs-gg-ui-template` row + the coherence row) updated in the same change.
- **2026-07-05** — `fs-gg-ui-template` PACKAGE 0.1.67→0.1.68-preview.1 (template-only) (owner rendering; refs FS.GG.Rendering#139/#137, release run 28754551912): a **TEMPLATE-ONLY** release — Feature 251 surfaces the **keyboard-only host-input boundary** where a game author first wires input: the game family's governed default host (`Viewer.runApp` / `GeneratedAppHost`) is keyboard-only (`MapKey` / `ViewerKey`, no mouse case), and a mouse-aimed scheme requires the pointer-aware interactive host (`InteractiveAppHost` / `Controls.Elmish.runInteractiveApp`, `MapPointer`) — a durable `Program.fs` host-wiring change. Delivered as a comment at the game-starter `Model.fs` input-wiring site (`profile == "game"` branch only), a "Capability boundary" note in the `fs-gg-keyboard-input` product-skill + its fragment README, a `scaffold-map.md` pre-design pointer, a generated-product source/contract assertion (note present + accurate to the emitted host contract), and a refreshed skill-manifest digest. Sibling of Feature 250 (#138); completes epic #137. **No `src/` change**, so the framework `version` pin **HELD at 0.1.64-preview.1** (the 16 `FS.GG.UI.*` libs are byte-identical, packed @pin as `--skip-duplicate` no-ops) while the package **decouples**: `package-version` 0.1.67→**0.1.68-preview.1**, `package-tag` → `fs-gg-ui-template/v0.1.68-preview.1` (Feature-209 coherence: template fsproj `<Version>` == the `v*` release tag == 0.1.68). All release-only gates green (Package consumption incl. the Feature-225 product-skill leak guard + Generated-product template-instantiation) + org-feed publish; publish-before-flip (FR-007) satisfied — package LIVE before this flip. The `templates → rendering` edge stays `@0.1.64-preview.1` (framework pin unchanged; template-only). No `schemaVersion` bump. Projection `docs/registry/compatibility.md` (versioned-contracts `fs-gg-ui-template` row) updated in the same change.
- **2026-07-05** — `fs-gg-ui-template` PACKAGE 0.1.65→0.1.67-preview.1 (template-only) + reconcile the skipped 0.1.66 field-flip (owner rendering; refs FS.GG.Rendering#138/#147/#148, release run 28753060627): a **TEMPLATE-ONLY** release — Feature 250 ships the collision-safe `Geometry.Vec2` (`Vx`/`Vy`, zero label overlap with `Scene.Point`/`Rect`) as a game/sample-pack fragment and re-expresses the game starter on it (`stepSim` + `FixedStep.drain` accumulator on `Tick`), so a game model never rediscovers the `X`/`Y`-vs-`Scene.Rect` record-label collision in the durable `LayoutEvidence.fs`; plus the `fs-gg-model-swap`/`fs-gg-game-core` skill updates + refreshed skill-manifest. **No `src/` change**, so the framework `version` pin **HELD at 0.1.64-preview.1** (the 16 `FS.GG.UI.*` libs are byte-identical, packed @pin as `--skip-duplicate` no-ops) while the package **decouples**: `package-version` 0.1.65→**0.1.67-preview.1**, `package-tag` → `fs-gg-ui-template/v0.1.67-preview.1` (Feature-209 coherence: template fsproj `<Version>` == the `v*` release tag == 0.1.67). All release-only gates green (Package consumption + Generated-product template-instantiation) + org-feed/nuget.org publish; publish-before-flip (FR-007) satisfied — package LIVE before this flip. **RECONCILES the skipped 0.1.66:** `FS.GG.UI.Template 0.1.66-preview.1` was published (Rendering #144, release run 28749394980) and recorded in the entry below (via the `scaffold-provider-identifier` coherence flip), but the `package-version`/`package-tag` **fields lagged at 0.1.65** — 0.1.67 supersedes both. The `templates → rendering` edge stays `@0.1.64-preview.1` — FS.GG.Templates pins 0.1.64/0.1.66 via its provider; the Rendering `template-dispatch` (run 28753060712, green) opened the re-pin PR to FS.GG.Templates (`→0.1.67`), a follow-up for the next Templates cut. No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Projection `docs/registry/compatibility.md` (versioned-contracts `fs-gg-ui-template` row) updated in the same change.
- **2026-07-05** — coherence `scaffold-provider-identifier` FLIP `false→true` (rendering; refs FS.GG.Rendering#142/#144, FS.GG.Templates#113, FS.GG.SDD#150): `FS.GG.UI.Template 0.1.66-preview.1` published (release run 28749394980; org feed + nuget.org — the release-only Generated-product gate scaffolded+built+tested against the shipped template), and FS.GG.Templates re-pinned `rendering.providers.yml` `source: 0.1.64→0.1.66-preview.1` + `identifierParameter: rootNamespace`. Publish-before-flip (FR-007) satisfied: package LIVE before the flip. A scaffolded product whose name is an illegal F# identifier (hyphenated) now compiles.
- **2026-07-05** — scaffold-provider 1.0.0→1.1.0 (additive: optional `identifierParameter`) + coherence `scaffold-provider-identifier` opened (rendering; SDD feature 080, refs FS.GG.Rendering#142/#144, FS.GG.Templates#113, FS.GG.SDD#150): the `fs-gg-ui` template splits the product-name token into a RAW name (string/path/.fsproj/.slnx) and an SDD-DERIVED valid-F# identifier (namespace/module/open via `rootNamespace`), so a product whose name is an illegal F# identifier (hyphenated `Roquelike-DungeonCrawler`) compiles. Backward-compatible (omit ⇒ None ⇒ identical to 1.0.0; byte-identical for valid names). In-flight — coherence stays `false` (publish-before-flip, FR-007) until the FS.GG.UI.Template release carries #142 and Templates re-pins `source:`.
<!-- Prepend new entries here, newest first:
- **YYYY-MM-DD** — HEADER (owner; refs): body
-->

- **2026-07-05** — RELEASE TRAIN — `fs-gg-ui-template` PACKAGE 0.1.64→0.1.65-preview.1 (template-only); + fsgg-sdd 0.7.0, fsgg-governance 1.3.0, FS.GG.Templates 0.3.0 published, no contract flip (owner github; refs below): four products cut together off their current `main`s (all publishes org-feed + nuget.org green, publish-before-flip FR-007). **Registry-tracked flip — `fs-gg-ui-template` (owner rendering; FS.GG.Rendering#132/#128/#130/#125, release run 28743063254):** a **TEMPLATE-ONLY** release — new opt-in fragments (collision/grids/line-drawing/visibility) + product-skills + refreshed skill-manifest landed in `template/`, but **no `src/` change**, so the framework `version` pin **HELD at 0.1.64-preview.1** (the 16 `FS.GG.UI.*` libs are byte-identical, packed @pin as `--skip-duplicate` no-ops; `FS.GG.UI.Scene` etc. newest on the feed stays 0.1.64 — verified) while the package **decouples**: `package-version` 0.1.64→**0.1.65-preview.1**, `package-tag` → `fs-gg-ui-template/v0.1.65-preview.1` (Feature-209 coherence: template fsproj `<Version>` == the `v*` release tag == 0.1.65). `templates → rendering` edge stays `@0.1.64-preview.1` — FS.GG.Templates still pins 0.1.64 (0.3.0 shipped that pin); the Rendering `template-dispatch` opened the re-pin PR [FS.GG.Templates#112](https://github.com/FS-GG/FS.GG.Templates/pull/112) (`0.1.64→0.1.65`), a follow-up for the next Templates cut. **No registry version flip (published, contract surface unchanged):** `fsgg-sdd` **0.7.0** (FS.GG.SDD#134–#138 features 076–079 — CLI-only; `FS.GG.Contracts` stays 1.4.0, `minimum-fsgg-sdd` stays 0.6.0), `fsgg-governance` **1.3.0** (FS.GG.Governance#99 — CLI-only; `governance-reference-gate-set` stays 1.2.1.1, byte-identical `--skip-duplicate` no-op), `FS.GG.Templates` **0.3.0** (FS.GG.Templates#111 — carries the already-registered `FS.GG.UI.Template 0.1.64` + `minimumFsggSdd 0.6.0` pins; consumer, not a registry contract). No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Projection `docs/registry/compatibility.md` (versioned-contracts `fs-gg-ui-template` row) updated in the same change.
- **2026-07-05** — `fs-gg-ui-template` FRAMEWORK PIN + PACKAGE ADVANCED → 0.1.64-preview.1 (owner rendering; FS-GG/FS.GG.Rendering#110, epic #115): fs-gg-ui **coherent-set** release carrying #110's new public **library** surface — the grid simulation primitives `FS.GG.UI.Canvas.Pathfinding` (deterministic A*/BFS over a walkability predicate; 4/8-neighbour, integer 10/14 costs + total frontier order so paths are byte-identical; no corner-cut; `maxVisited`-bounded) and `FS.GG.UI.Canvas.SpatialGrid` (uniform grid; exact `build`/`query`/`queryRadius` in insertion order). A real `src/` change (additive, alongside 239's `Rng`/`FixedStep`), so the framework pin re-couples with the package: contract `fs-gg-ui-template` `version` 0.1.63→**0.1.64-preview.1** (library snapshot tag `fs-gg-ui/v0.1.64-preview.1`), `package-version` 0.1.63→**0.1.64-preview.1** (`FS.GG.UI.Template` + the 16 `FS.GG.UI.*` libs live on the org feed + nuget.org; publish-before-flip FR-007, release run 28732754974 all gates + publish green), `package-tag` → `fs-gg-ui-template/v0.1.64-preview.1`; `templates → rendering` edge → `@0.1.64-preview.1`. Unblocks FS.GG.Rendering#112 (the `fs-gg-game-core` grid-sim recipe can now reference the shipped primitives). No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Downstream: FS.GG.Templates provider re-pin `FS.GG.UI.Template::0.1.63→0.1.64-preview.1`. Projection `docs/registry/compatibility.md` (dependency graph + versioned-contracts row) updated in the same change.
- **2026-07-04** — `minimum-fsgg-sdd` ADVANCED 0.4.0→0.6.0 + `fsgg-sdd-orchestrator-axis` RE-OPENED `coherent:true→false` (owner sdd; FS.GG.SDD#121 + tag v0.6.0, standing request FS.GG.Templates#99, ADR-0008/FR-011): FS.GG.SDD published **v0.6.0** (`FS.GG.SDD.Cli 0.6.0` LIVE on the org feed + nuget.org, release run 28720216874 gates + publish green; publish-before-flip FR-007) carrying **Feature 073** (transient-artifact taxonomy), which makes `fsgg-sdd` **seed a `.gitignore`** for regenerable output into scaffolded products. Per the maintainer decision that a product on the `fs-gg-ui-template` pin **must** contain the seeded `.gitignore`, the expected seeded-artifact surface grew, so per FR-011 the `fs-gg-ui-template.minimum-fsgg-sdd.version` **advances 0.4.0→0.6.0** (0.6.0 is the oldest published CLI that seeds it; 0.5.0 did not). Advancing the floor flips `fsgg-sdd-orchestrator-axis` `coherent:true→false` (registry half DONE) — a **standing request** for the provider half: FS.GG.Templates must re-mirror `providers/rendering.providers.yml` `minimumFsggSdd 0.4.0→0.6.0` ([Templates#99](https://github.com/FS-GG/FS.GG.Templates/issues/99)); the axis re-coheres when descriptor == registry == 0.6.0 and the Templates composition gate verifies a scaffold seeds the `.gitignore`. `FS.GG.Contracts` unchanged at 1.4.0 (CLI-only coherent advance). No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Projection `docs/registry/compatibility.md` (dependency-graph min-version + coherence-state row) updated in the same change.
- **2026-07-04** — `fs-gg-ui-template` FRAMEWORK PIN + PACKAGE ADVANCED → 0.1.63-preview.1 (owner rendering; FS-GG/FS.GG.Rendering#74, PRs #85/#86/#87): fs-gg-ui **coherent-set** release carrying Feature 241's new public **library** surface — `FS.GG.UI.Controls.Elmish.Authoring.Cmd.none` / `Sub.none` (product-authoring Elmish-convention no-op aliases for a bare `[]`). A real `src/` change, so the framework pin re-couples with the package: contract `fs-gg-ui-template` `version` 0.1.62→**0.1.63-preview.1** (library snapshot tag `fs-gg-ui/v0.1.63-preview.1`), `package-version` 0.1.62→**0.1.63-preview.1** (`FS.GG.UI.Template` + the 16 `FS.GG.UI.*` libs live on the org feed + nuget.org; publish-before-flip FR-007, release run 28706700281 all gates + publish green), `package-tag` → `fs-gg-ui-template/v0.1.63-preview.1`; `templates → rendering` edge → `@0.1.63-preview.1`. (First release publish was skipped fail-closed by a latent Feature-240 miss — a release-only lifecycle-validator skill-count 9→10 for `fs-gg-game-core`, fixed in PR #87 — so nothing bad shipped.) No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Downstream: FS.GG.Templates provider re-pin `FS.GG.UI.Template::0.1.62→0.1.63-preview.1`. Projection `docs/registry/compatibility.md` (dependency graph + versioned-contracts row) updated in the same change.
- **2026-07-04** — `skill-registry` github←sdd EDGE DE-PROVISIONALIZED + `skill-registry-published` NARROWED TO ONE BLOCKER (owner github; SDD#111 closing cross-repo #109, epic #163, ADR-0017): SDD now EMITS a process skill-manifest (`.agents/skills/skill-manifest.json`, schema `skill-manifest` v1), so the process half of `registry/skills.yml` is producer-emitted rather than digested from SDD's canonical `.claude/skills` bodies. Edge `github → sdd via skill-registry@1` updated: 15 provisional → **16 EMITTED** fs-gg-sdd-* skills, registry = manifest = bytes. `skill-registry` `surface`/`validator` prose updated to reflect both producer halves now emitted. Coherence `skill-registry-published` stays `coherent: false` but its blocker set narrows from **two → one**: condition (1) P2 SDD process-manifest emission is **DONE** (SDD#111); only condition (2) — Rendering's `materializes-when` grammar alignment (FS.GG.Rendering#77) — remains before the enforcing flip. No `schemaVersion` bump; `fsgg-sdd registry validate` → valid. Process-row digest reconciliation itself is logged in `registry/skills.CHANGELOG.md`; projection `docs/registry/compatibility.md` updated in the same change.

- **2026-07-04** — `fs-gg-ui-template` FRAMEWORK PIN + PACKAGE ADVANCED → 0.1.62-preview.1 (owner rendering; FS-GG/FS.GG.Rendering#72, PRs #78/#79/#80): fs-gg-ui **coherent-set** release carrying Feature 239's new public **library** surface — `FS.GG.UI.Scene.Geometry` (AABB) + `FS.GG.UI.Canvas.Rng` (value-type SplitMix64) + `FS.GG.UI.Canvas.FixedStep` (fixed-timestep drain). Unlike the template-only 0.1.59–0.1.61 bumps, this is a real `src/` change, so the framework pin re-couples with the package: contract `fs-gg-ui-template` `version` 0.1.58→**0.1.62-preview.1** (library snapshot tag `fs-gg-ui/v0.1.62-preview.1`), `package-version` 0.1.61→**0.1.62-preview.1** (`FS.GG.UI.Template` + the 16 `FS.GG.UI.*` libs live on the org feed + nuget.org; publish-before-flip FR-007, release run 28702520704 all gates + publish green), `package-tag` → `fs-gg-ui-template/v0.1.62-preview.1`; `templates → rendering` edge → `@0.1.62-preview.1`. No `schemaVersion` bump; `fsgg-sdd registry validate` → valid/0 diagnostics. Downstream: FS.GG.Templates provider re-pin `FS.GG.UI.Template::0.1.61→0.1.62-preview.1`. Projection `docs/registry/compatibility.md` (dependency graph + versioned-contracts row) updated in the same change.
- **2026-07-04** — `skill-registry` GOVERNED CONTRACT REGISTERED + `skill-registry-published` COHERENCE (owner github; .github#168, epic #163, ADR-0017/0015): added the `skill-registry` contract (owner github — the new org skill catalog `registry/skills.yml`; consumer github — the union gate + registry validation), two producer dependency edges (`github → rendering` and `github → sdd`, `via: skill-registry@1 <- …` the product/process skill-manifests), and the `skill-registry-published` coherence row (`coherent: false`). The catalog itself (`registry/skills.yml` + `registry/skills.CHANGELOG.md`, added in this change) lands **product-complete**: 12 product rows reconciled from Rendering's published skill-manifest (Feature 238 / Rendering#76, closes Rendering#71); 15 process rows **provisional** (canonical-body digests from SDD's `.claude/skills/fs-gg-sdd-*` bodies) pending SDD emitting a process manifest. Two producer-side conditions gate the flip to `coherent: true`: (1) P2 — SDD emits a process manifest so digests are producer-emitted and the typed `Fsgg.Registry` validator can assert registry = manifest = bytes (the `SkillManifest` types are done via SDD#60/spec-057; the emitting step is a separate, not-yet-authored SDD deliverable); (2) Rendering aligns its shipped manifest `materializes-when` **grammar** with the ADR-0017 gate grammar (FS.GG.Rendering#77 — #76 ships C-style predicates the gate reads as false, so `skills.yml` records the normalized canonical forms). No `schemaVersion` bump (`registry-schema` unchanged); `fsgg-sdd registry validate` → valid/0 diagnostics. Projection `docs/registry/compatibility.md` (versioned-contracts row + dependency graph + coherence row) updated in the same change.

- **2026-07-04** — FS.GG.TEMPLATES ADDED TO `nuget-org-published` SCOPE (owner github; FS.GG.Templates#83, PR Templates#84, ADR-0012/0013): `FS.GG.Templates` was a currently-published package that resolved on the org feed only — silently violating the `nuget-org-published` claim that *all* currently-published packages dual-publish to nuget.org. Its `release.yml` now wires the ADR-0013 Trusted-Publishing (OIDC) `NuGet/login@v1` + a byte-identical push to `api.nuget.org` after the org-feed push (gated on `vars.NUGET_ORG_PUBLISH`), an org-owner created the Active `fs-gg-Templates-publishing` policy (account `Paradigma11`), and `FS.GG.Templates 0.2.0` was re-cut and verified live (`dotnet new install FS.GG.Templates` with no `--add-source` → success). Registry deltas: added `FS.GG.Templates` to the coherence entry's scope, bumped the policy count three→four and the in-scope package count 22→23, and added the Templates policy/PR refs. No versioned-surface `contract-change` — `coherent: true` is unchanged (the entry was already true; this makes its prose accurate). Projection `docs/registry/compatibility.md` updated in the same change.

- **2026-07-02** — REGISTRY SCHEMA REGISTERED AS A GOVERNED CONTRACT (owner github; .github#135, epic #124, review §6.9, ADR-0015): added the `registry-schema` contract (owner sdd — `Fsgg.Registry` is the typed authority; consumer github — the contract-coherence gate), a `github → sdd` dependency edge (`via: registry-schema@1`), and the `registry-schema-governed` coherence row (`coherent: true`). `.github` was added to the `repos:` map so the typed validator resolves the new consumer/edge references (it cross-checks `consumers` + edge endpoints — verified: without the entry, `UnknownComponent` reds the gate). New optional field `validator` (registry-schema only) names the gate pin. Closes the H2+M5 gap (the typed-validator investment only pays off if the schema is versioned AND the gate pin advances with it — #127 found the pin frozen for three minors while the schema grew fields under additive tolerance): schema growth is now a tracked contract-change — bump `registry-schema.version` + the file's `schemaVersion` in the same PR that teaches `Fsgg.Registry`, and advance the `contract-coherence.yml` `FS.GG.SDD.Cli` pin. No behavioural gate change lands with the registration itself (`schemaVersion` stays 1; `fsgg-sdd registry validate` → valid/0 diagnostics). Projection (`docs/registry/compatibility.md`), `docs/architecture.md` §5, and the ADR index updated in the same change.

- **2026-07-02** — CHANGELOG RESTRUCTURE (owner github; .github#129, epic #124, review M4): the registry log moved out of the single ~42 KB trailing YAML comment on `dependencies.yml`'s `updated:` field (≈37% of the file — unreviewable diffs, a merge-conflict magnet for the org's parallel sessions) into this file. The `updated:` field keeps only the date + a one-line pointer; the former comment's 29 entries are frozen verbatim below under the migration marker. New protocol: prepend one dated entry here per registry change and set `updated:` to match. Docs updated in the same change: `docs/coordination/README.md`, the `cross-repo-coordination` + `publishing-and-deployment` skills (both `.claude`/`.agents` roots). No contract surface/version/coherence flip — the typed validator never read the comment, so gate behaviour is unchanged.

<!-- ─────────────────────────────────────────────────────────────────────────────
     Entries below this marker were migrated verbatim (2026-07-02, .github#129 / M4)
     from the former `dependencies.yml` inline `updated:` comment. They were recorded
     without per-entry dates; the most recent predate 2026-07-02. Do not rewrite them.
     ───────────────────────────────────────────────────────────────────────────── -->

- VALIDATOR PIN ADVANCED 0.2.1→0.5.0 (owner github; .github#127/H2, epic #124): contract-coherence.yml pinned FS.GG.SDD.Cli 0.2.1 while the org feed shipped 0.5.0 (feed serves 0.5.0/0.4.0/0.3.0/0.2.1/0.2.0), so the registry schema grew three minors under a frozen validator and additive-tolerance degraded the typed gate toward a YAML-parses check. Advanced the install pin to 0.5.0 — `fsgg-sdd registry validate` (CLI 0.5.0) stays valid/0-diagnostics over THIS canonical registry — and COUPLED it against silent re-freeze: gate-pin == feed(newest) FS.GG.SDD.Cli == 0.5.0, enforced by an org-Renovate-preset annotation manager tag (`# renovate: datasource=nuget depName=FS.GG.SDD.Cli` above the install line; the preset's provisioning blocker #21 is closed) that auto-bumps the literal on the next CLI publish. Annotation-regex hardening tracked in .github#134/M8. No contract surface/version/coherence flip — gate-config currency only.

- SKILL-UNION-ASSERT --MANIFEST ARM ALIGNED WITH PRODUCER SEMANTICS (owner github; .github#120, closes the residual known gap recorded on skill-mirror-verified below — no coherence flip, the row is already true): check 3 of scripts/skill-union-assert.sh now follows the SHIPPED producer manifests (Fsgg.SkillMirror, FS.GG.Contracts 1.4.0 / fs-gg-ui-template 0.1.61-preview.1) — digest = canonical-body sha256 of SKILL.md only (byte-equivalent to sha256sum SKILL.md; multi-file remainder covered by the checks-1-2 cross-root identity), manifest = superset catalog (declared-and-absent is legitimate; declared-and-present must digest-match; present-in-some-roots-only still partitions), and undeclared co-tenant process skills (fs-gg-sdd-*/speckit-*) are admitted via a new --co-tenants glob allowlist (also a co-tenants input on the workflow_call wrapper). Selftest fixture extended to 11 cases incl. digest==sha256sum-SKILL.md parity. Follow-up: Templates swaps its inline producer-semantics manifest arm for --manifest.

- Previous: AGENT-SKILL-MIRROR -> COHERENT:TRUE (owner sdd; ADR-0011 rollout tracker, follow-up to the skill-mirror-verified/orchestrator-axis flip below — completes the coherence-row set for the ADR-0014 P4 landing): both halves of the ADR-0011 invariant are shipped and VERIFIED — SDD half fsgg-sdd 0.4.0 (union → 3 roots fan-out, guard claude≡codex≡agents); Rendering/Templates half via Templates#49 / PR Templates#51 merged 574e90c (fs-gg-ui emits UI skills .agents-only on the sdd path @0.1.61-preview.1; descriptor minimumFsggSdd 0.3.0->0.4.0), and the composition gate hard-asserts the byte-identical 3-root union in BOTH lanes (Templates CI run 28585460049) — the row's own flip condition ("Rendering half ships + full-stack scaffold produces three byte-identical union roots with no diagnostic") is met, and its tracking issues (SDD#55, Templates#47/#49) are all closed. Also adds the MISSING agent-skill-mirror row to the compatibility.md coherence projection (projection gap since the ADR-0011 registration — the registry row was never projected).

- SKILL-MIRROR-VERIFIED + FSGG-SDD-ORCHESTRATOR-AXIS -> COHERENT:TRUE (owner github/sdd; FS.GG.Templates#49 / PR Templates#51 merged 574e90c — the last child of epic .github#110, ADR-0014 P3.T3.2+P4): Templates re-pinned providers/rendering.providers.yml to FS.GG.UI.Template::0.1.61-preview.1 + minimumFsggSdd 0.3.0->0.4.0 (re-mirrors minimum-fsgg-sdd — both halves in lockstep at 0.4.0), and the composition gate now asserts the byte-identical 3-root skill union HARD in BOTH lanes (orchestrated fsgg-sdd scaffold + standalone spec-kit materialize --enforce; P3.G3.1 script + producer-semantics manifest digest/dangling arm; providerWroteSddTree grep-and-SKIP retired; 45/45 green locally + CI run 28585460049). The #47 lockstep chain is CLOSED. Follow-up .github#120: align skill-union-assert --manifest (tree-hash + exact-set) with the shipped producer manifests (canonical-body sha256 + superset catalog).

- FS-GG-UI-TEMPLATE 0.1.61 PUBLISHED → REGISTRY FLIP (owner rendering; FS.GG.Rendering#43, Feature 231, ADR-0014 P2; publish-before-flip FR-007): fs-gg-ui-template `package-version` 0.1.60→0.1.61-preview.1 + `package-tag` fs-gg-ui-template/v0.1.61-preview.1 + consuming edge templates→rendering via @0.1.61-preview.1. The template now (1) publishes the provider PRODUCT skill-manifest (skill-manifest schema v1, 12 entries, canonical-body sha256) to .agents/skills/skill-manifest.json in EVERY lifecycle; (2) replaces Feature 230's 24 per-skill .claude/.codex template.json twins + 2 blanket copies with ONE standalone materialize step (a vendored byte-equivalent of Fsgg.SkillMirror from FS.GG.Contracts 1.4.0, invoked by the FsGgMaterializeSkillRoots MSBuild target on first build; behavioral-parity gate in Package.Tests, roadmap §6); (3) stops vendoring the repo dev surface — the ~17 dangling fs-gg-product-*/framework wrappers (audit F3) no longer ship, speckit-* only; (4) emits skill bodies verbatim/name-neutral (F5) with a no-dangling-route release gate (R2.4). sdd/none placement UNCHANGED (.agents-only; Templates#47 stays closed). Framework `version` UNCHANGED at 0.1.58-preview.1 (Tier-2 content/config, no src/; NO fs-gg-ui/v0.1.61 tag — generated products still pin FS.GG.UI.* @ 0.1.58). Published via release run 28583689045 (all gates + org-feed push + nuget.org OIDC push green). skill-mirror-verified STAYS coherent:false (P3 composition assert + P4 enforcing flip pending). Templates re-pin (0.1.60→0.1.61) pending — P4.A4.2.

- FSGG-CONTRACTS package-version 1.2.0→1.4.0 (owner github; SDD#63, ADR-0014 P1 publish-before-flip step 2): SDD cut the coherent release fsgg-sdd v0.5.0 (2026-07-02) carrying FS.GG.Contracts 1.4.0 + FS.GG.SDD.Cli 0.5.0 to the org feed + nuget.org (release.yml runs 28578885762/28578886260, both "Push to the org feed" green). The feed now serves 1.4.0, so `package-version` advances 1.2.0→1.4.0: `package-version` == `version` == source == feed(newest) == 1.4.0 (publish-before-flip FR-007 satisfied). Unblocks Rendering#43 (P2 — pin FS.GG.Contracts >= 1.4.0).

- FSGG-CONTRACTS version 1.3.0→1.4.0 (owner github; ADR-0014 P1, tracks SDD#61 landing): advance the registry `fsgg-contracts` `version` field to the SDD source authority (Contracts 1.4.0 — fsproj <Version> + ContractVersion.value both 1.4.0, merged b9f946c). P1 landed the pure BCL-only `Fsgg.SkillMirror` library (the one `mirror`/`verify` materialize-and-verify code path) additively (1.3.0→1.4.0). This is the SAME contract-coherence version-coupling drift #116 fixed, re-triggered by P1's parallel same-day landing: the gate asserts registry `version` == SDD source, so leaving it at 1.3.0 reds main (`fsgg-contracts pin drift: registry declares 1.3.0 but actual is 1.4.0`). `package-version` STAYS 1.2.0 (1.3.0 AND 1.4.0 both UNPUBLISHED — publish-before-flip; the 0.5.0 feed release + orchestrator-axis minimum flip is the separate release dance, sequenced after the pending 0.4.0 publish #57). Restores gate green.

- FSGG-CONTRACTS version 1.2.0→1.3.0 (owner github; ADR-0014 P0.D0.2, closes SDD#60): advance the registry `fsgg-contracts` `version` field to the SDD source authority (Contracts 1.3.0, merged FS.GG.SDD#62) — the COUPLED bump the scaffold-provenance 1.1.0 landing (#114) noted in prose but left unapplied to the field, which reddened contract-coherence on main (`fsgg-contracts pin drift: registry declares 1.2.0 but actual is 1.3.0`, run 28573254954). `package-version` STAYS 1.2.0 (1.3.0 UNPUBLISHED — publish-before-flip; advances at the ADR-0014 P1 CLI release). Restores gate green.

- SCAFFOLD-PROVENANCE 1.0.0→1.1.0 (owner sdd; ADR-0014 P0.D0.2, FS.GG.SDD#60): the `scaffold-provenance` contract gains an ADDITIVE optional per-path `sha256` on the produced/mirrored path objects — the content-addressed half of ADR-0014 §Decision 3. FS.GG.Contracts (unpublished; the coherent CLI/Contracts release is ADR-0014 P1) defines the shapes: SkillManifest ({id,scope:process|product,sha256,body|resolvablePath}) + the AGENT_SKILL_ROOTS constant (.claude/.codex/.agents) + ScaffoldProducedPathEntry.Sha256, package contract 1.2.0→1.3.0. The runtime emitter OMITS `sha256` when absent so current output stays BYTE-IDENTICAL and the in-code schemaVersion stays 1 (digest population is P1). ADDITIVE/backward-compatible: a 1.0.0 consumer ignores the unknown field (typed validator stays additive-tolerant, SDD#32/#49). No coherence flag flips — `skill-mirror-verified` STAYS coherent:false (flips true in P4). Contract touched: scaffold-provenance (minor, +sha256).

- FS-GG-UI-TEMPLATE 0.1.60 PUBLISHED → REGISTRY FLIP (owner rendering; FS.GG.Rendering#42, Features 229+230; publish-before-flip FR-007): fs-gg-ui-template `package-version` 0.1.58→0.1.60-preview.1 + `package-tag` fs-gg-ui-template/v0.1.60-preview.1 + consuming edge templates→rendering via @0.1.60-preview.1. The template now emits UI product-skills to `.agents/skills/` ONLY under sdd/none (no `.claude/`/`.codex/` leak — unblocks the Rendering half of Templates#47) and, under standalone spec-kit (no orchestrator), self-mirrors the byte-identical union into all three roots per ADR-0011 §1. Framework `version` UNCHANGED at 0.1.58-preview.1 (Tier-2 content/config only, no src/; NO fs-gg-ui/v0.1.60 tag pushed — the generated product still pins FS.GG.UI.* @ 0.1.58, live on the org feed + nuget.org; pushing a fs-gg-ui/v0.1.60 tag would trip the Feature 209 pin-lags-tag guard). Published via release run 28572384073 (all gates + org-feed push + nuget.org OIDC push green); org feed serves FS.GG.UI.Template 0.1.60-preview.1. Templates provider re-pin (0.1.58→0.1.60) pending — dispatched via fs-gg-ui-template/v0.1.60-preview.1.

- SKILL-VENDORING ROBUSTNESS REGISTERED (owner github; ADR-0014, extends ADR-0011): new coherence id `skill-mirror-verified` (coherent:false) — a four-repo audit found the skill-mirror satisfies ADR-0011's invariants via FOUR hand-maintained "materialize union → 3 roots" implementations (SDD scaffold/refresh/doctor + the fs-gg-ui template's Feature 230 36-twin standalone matrix) with NO content verification (doctor/composition check presence only; scaffold-provenance has no digest, so ADR-0011's sha256 rationale is counterfactual) and ~13 dangling dev-surface wrappers shipped into products. ADR-0014 consolidates to ONE content-addressed algorithm: a per-producer skill-manifest ({id,scope,sha256,body}) + one mirror/verify library in FS.GG.Contracts (root-set parameterized via AGENT_SKILL_ROOTS) consumed by both lanes (orchestrated fsgg-sdd + standalone template single-materialize), scaffold-provenance gains per-skill sha256 (additive minor), doctor+composition assert present∧byte-identical-across-roots∧matches-manifest for process∧product skills, and a product/dev boundary + no-dangling guard remove the wrappers. Phased/publish-before-flip/additive (P0 contract → P1 SDD library+content-drift → P2 Rendering manifest+single-materialize → P3 .github assertion + Templates composition-asserts → P4 flip enforcing+re-pin+delete residual, superseding the #47 lockstep). Roadmap: docs/reports/2026-07-01-skill-vendoring-robustness-roadmap.md. Contract touched: scaffold-provenance (minor, +sha256), fs-gg-ui-template, scaffold-provider. No coherence flag flips true→false (new id only).

- NUGET-ORG PUBLISHED → COHERENT (owner github; ADR-0012/0013, closes .github#103 + SDD#56 + Rendering#40 + Governance#41): `nuget-org-published` flips coherent:false→TRUE — all 22 in-scope packages now RESOLVE on public nuget.org at their current version (FS.GG.Contracts 1.2.0, FS.GG.SDD.Cli 0.4.0, FS.GG.Governance.Cli 1.2.0, FS.GG.Governance.ReferenceGateSet 1.2.1.1, the FS.GG.UI.* 17-lib set + FS.GG.UI BOM + FS.GG.UI.Template @0.1.58-preview.1), dual-published via Trusted Publishing (OIDC), byte-identical to the org-feed artifacts. Delivered: three Active per-producer TP policies (owner Paradigma11) + `id-token: write`/NuGet/login@v1/nuget.org push wired into each producer release workflow (SDD#58, Rendering#41, Governance#42, merged); SDD v0.4.0 + Governance v1.2.0 releases + a Rendering 0.1.58-preview.1 dispatch all pushed green. Consumer docs dropped `--add-source`; docs/architecture.md distribution picture reconciled to dual-feed. Org GitHub Packages feed STAYS the coherence/-preview source of truth (Renovate/contract-coherence gate/`package-version` unchanged). No contract surface/version change. Optional follow-on: reserve the FS.GG. prefix + ADR-0012 §5 listing metadata.

- ORCHESTRATOR AXIS → 0.4.0 (owner sdd; ADR-0008, FS.GG.SDD#57): FS.GG.SDD published v0.4.0 — feature 056 (orchestrator skill fan-out) makes fsgg-sdd the SOLE mirror authority, seeding the fs-gg-sdd-* process skills into a THIRD agent-skill root .agents/skills/ and fanning the byte-identical UNION into .claude/.codex/.agents (FR-011 grew the seeded surface). FS.GG.SDD.Cli 0.4.0 LIVE on the org feed (publish-before-flip FR-007; release runs 28531459650/28531459644 green). Registry ADVANCES fs-gg-ui-template minimum-fsgg-sdd 0.3.0→0.4.0 + the templates→sdd edge; `fsgg-sdd-orchestrator-axis` flips coherent:true→false pending the Templates provider re-mirror (minimumFsggSdd 0.3.0→0.4.0 + .agents-only UI-skill emission, Templates#47); `agent-skill-mirror` SDD half DONE (shipped in 0.4.0), Rendering half pending Templates#47. No contract surface/schema-version change (fs-gg-ui-template stays 0.1.58-preview.1; scaffold-provider@1).

- NUGET-ORG AUTH → TRUSTED PUBLISHING (owner github; ADR-0013 SUPERSEDES ADR-0012 §6): the nuget.org push authenticates via GitHub OIDC (NuGet/login@v1 → short-lived key), NOT a long-lived NUGET_ORG_API_KEY secret. Login+push live in EACH PRODUCER'S OWN release.yml — a cross-repo reusable workflow trips the OIDC policy match (NuGet/login#6), so the .github#104 reusable nuget-org-push.yml is RETIRED. The #103 admin gate becomes: own the FS-GG nuget.org org + reserve the `FS.GG.` prefix + create ONE Trusted Publishing policy per producer repo (Repo Owner FS-GG; Repository FS.GG.SDD/FS.GG.Rendering/FS.GG.Governance; Workflow File = release.yml) [+ optional NUGET_USER]; fail-closed is intrinsic (no policy → NuGet/login 401 → release fails loud). `nuget-org-published` STAYS coherent:false (auth-mechanism swap only; ADR-0012 §1–§5 dual-publish/scope/byte-identical/gated-ordering/metadata unchanged). No contract surface/version change.

- NUGET-ORG DUAL-PUBLISH REGISTERED (owner github; ADR-0012): new coherence id `nuget-org-published` (coherent:false) — FS-GG dual-publishes ALL currently-published packages (the two .Cli tools, FS.GG.Contracts, the FS.GG.UI.* set + BOM + Template, FS.GG.Governance.ReferenceGateSet) to the PUBLIC nuget.org IN ADDITION to the org GitHub Packages feed; the org feed stays the coherence source of truth (Renovate/contract-coherence gate/`package-version` keep reading it), nuget.org is an additive public distribution target. Each producer release pushes the BYTE-IDENTICAL .nupkg (gate-verified) to nuget.org with --skip-duplicate ONLY AFTER the org-feed push + gates are green. Standing request pending an ADMIN gate (provision the FS-GG nuget.org org + reserve the `FS.GG.` ID prefix + add org secret NUGET_ORG_API_KEY; push steps FAIL CLOSED until it lands — same forward-guardrail model as #21) + three producer wirings (SDD/Rendering/Governance release.yml add the nuget.org push + listing metadata). nuget.org IDs are PERMANENT (unlist != delete) so this freezes the current FS.GG.* IDs as public identities (no rename — ADR-0003). No contract surface/version change (new coherence id + additive distribution target only).

- AGENT-SKILL-MIRROR REGISTERED (owner sdd; ADR-0011): new coherence id `agent-skill-mirror` (coherent:false) — every agent-skill root (.claude/.codex/.agents skills/) must carry the byte-identical UNION of SDD process + provider UI skills so runtimes are interchangeable; `fsgg-sdd` is the sole mirror authority (materialized fan-out, not symlinks), providers confined to .agents/skills/, isSddTree guard stays strict. Standing request pending SDD fan-out (SDD#55) + fs-gg-ui template emitting .agents-only (Templates#47); surfaced by a full-stack scaffold blocking on scaffold.providerWroteSddTree at FS.GG.UI.Template::0.1.58-preview.1. No contract surface/version change (new coherence id only).

- ORCHESTRATOR AXIS COHERENT (owner github; CLOSES epic #85): coherence id `fsgg-sdd-orchestrator-axis` flips coherent:false->TRUE — the axis is now LIVE end-to-end with both halves in lockstep at 0.3.0. Registry half (below): FS.GG.SDD.Cli 0.3.0 published + `minimum-fsgg-sdd.version: 0.3.0` pinned. Provider half: FS.GG.Templates mirrored it — providers/rendering.providers.yml `minimumFsggSdd.version: null->0.3.0` (Templates#45, PR Templates#46, merged; composition run.sh 32/0). Verified end-to-end: a behind-CLI scaffold emits diagnostic `scaffold.cliBehindMinimum` AND stamps generator.version + requiredMinimumCliVersion into .fsgg/scaffold-provenance.json; at installed==minimum (0.3.0) it correctly does NOT warn (coherent steady state). This is the last sub-item of #85 (ADR #86, registry field #87, CLI SDD#49 all closed) — epic #85 can close. No surface/version change (additive field already registered).

- ORCHESTRATOR MINIMUM PINNED (owner github; epic #85): now that the REPUBLISH TRAIN below published FS.GG.SDD.Cli 0.3.0, the fs-gg-ui-template coherent set's `minimum-fsgg-sdd.version` advances null->"0.3.0" — 0.3.0 is the oldest published CLI that seeds the artifacts a product on the pin is expected to contain (049 early-stage-guidance + 051 fs-gg-sdd-* skills; 0.2.1 predated them), so FR-007 publish-before-flip is now satisfied. Coherence id `fsgg-sdd-orchestrator-axis` STAYS coherent:false: a scaffold reads the minimum from the Templates provider descriptor (providers/rendering.providers.yml), which still records minimumFsggSdd null — so it isn't live end-to-end. Flips true when Templates mirrors 0.3.0 (Templates#43 follow-up) + a behind-CLI scaffold is verified to warn (SDD#49, shipped in 0.3.0). This entry pins the .github-owned half only; the #94 republish train (package-version 1.2.0, fs-gg-ui-template 0.1.58) is recorded next.

- REPUBLISH TRAIN (owner sdd/rendering/templates): (1) FS.GG.Contracts 1.2.0 + FS.GG.SDD.Cli 0.3.0 PUBLISHED to the org feed via SDD release v0.3.0 (release.yml runs 28508315780/28508315653, both jobs pushed) — CLI 0.3.0 ships the accumulated SDD work merged since 0.2.1/Feature 045 (Features 046–054, incl. 054 surface-provider-output: additive `providerInvocation` block on provider-defect scaffold reports, provenance stays v1); fsgg-contracts `package-version` ADVANCES 1.1.1→1.2.0 (publish-before-flip satisfied). (2) fs-gg-ui-template coherent set RE-RELEASED 0.1.57→0.1.58-preview.1 (FS.GG.Rendering Feature 227 fs-gg-layout consumer product-skill; release run 28509096357; tags v0.1.58-preview.1 + fs-gg-ui/v0.1.58-preview.1 + fs-gg-ui-template/v0.1.58-preview.1; feed serves FS.GG.UI.Template + FS.GG.UI.* @0.1.58) — content-only, coherent advance; FS.GG.Templates re-pinned providers/rendering.providers.yml 0.1.57→0.1.58 (composition run.sh 35/0, gated build+govern ran). FS.GG.Governance NOT republished — CLI source == published 1.2.0, no CLI/adapter source change since (post-1.2.0 commits are agent-skill/README docs, not in the package). fsgg-sdd-orchestrator-axis STAYS coherent:false pending the concrete `minimum-fsgg-sdd` pin (0.3.0 now satisfies it — post-049/051 CLI is published — but the Templates provider `minimumFsggSdd.version` + this coherence flag are left null/false in lockstep as a deliberate follow-up, not a half-flip). No coherence flag flips true→false.

- fsgg-contracts version 1.1.1→1.2.0 (owner sdd; .github#91 follow-up / drift fix): the contract-coherence gate went red — registry declared `version: 1.1.1` but FS.GG.SDD source had advanced to 1.2.0 (SDD Feature 052, commit 3be5513 "CLI version coherence in scaffold provenance", fixes SDD#49) implementing the ADR-0008/0009 orchestrator axis (the CLI stamps used+minimum into scaffold-provenance). Advanced `version` 1.1.1→1.2.0 to track the SDD source authority — this clears the gate (it asserts registry `version` == SDD actual source == 1.2.0). `package-version` STAYS 1.1.1: 1.2.0 is UNPUBLISHED (org feed serves 1.1.1/1.1.0/1.0.1), so per publish-before-flip (FR-007) the published pin stays 1.1.1 until SDD releases 1.2.0 (advances then). Minor/additive (new provenance schema surface; no .fsi break → apicompat cannot trip → no major). No coherence-flag flip; consumers keep resolving the published 1.1.1.

- ADR-0008 orchestrator axis (.github#87, sub-issue of #85): the fs-gg-ui-template coherent set now carries a `minimum-fsgg-sdd` block — the CLI is a first-class member of the set (its third axis alongside template+framework), because a scaffolded product = template@<pin> + fsgg-sdd CLI@<installed> and the CLI seeds artifacts a product on the pin is expected to contain (fs-gg-sdd-* process skills, FS.GG.SDD Feature 051; .fsgg/early-stage-guidance.md, Feature 049). New coherence id `fsgg-sdd-orchestrator-axis` (coherent:false): no published CLI satisfies the axis yet — feed newest fsgg-sdd 0.2.1 (published 2026-06-29 / Feature 045) PREDATES 049+051 (merged to FS.GG.SDD main 2026-06-30, unreleased), so `minimum-fsgg-sdd.version` stays null (pending-publish, FR-007). Flips true when FS.GG.SDD publishes the post-051 CLI (#49) and the concrete minimum is pinned; provider descriptor records it (Templates#43); fsgg-sdd stamps used+minimum into scaffold-provenance + warns when behind (SDD#49). Contract-change per ADR-0008; typed validator (fsgg-sdd registry validate) accepts the additive field.

- Feature 226 (FS.GG.Rendering#38): fs-gg-ui-template coherent set RE-RELEASED at 0.1.57-preview.1 (release run 28481261579; tags v0.1.57-preview.1 + fs-gg-ui-template/v0.1.57-preview.1 + fs-gg-ui/v0.1.57-preview.1) DELIVERING the fs-gg-styling consumer theming/styling product skill — the consumer slice (pick a theme, set a control StyleVariant/StyleClass, consume the resolved style), wired on the controls-bearing profiles (app, game) mirroring fs-gg-ui-widgets, cross-linked from fs-gg-ui-widgets, enumerated by skill-parity + the Feature 224 catalog + Feature 225 leak guards. Its three non-compiling F# examples (bare RequireQualifiedAccess StyleVariant cases; bare ValidationState.Invalid; a Ghost-variant name collision) were caught + fixed and compile-verified 0/0 against the built assemblies, and the Feature 219 skill-emission matrix was updated to include it (the release-only Package.Tests gate caught the omission and correctly skipped the first publish, run 28480885576). Content-only: no public surface, no parameter add/remove/rename; a coherent advance (no coherence flag flips true->false). Consumers receive it via the FS.GG.Templates providers pin bump 0.1.56->0.1.57-preview.1. Closes FS.GG.Rendering#38.

- Feature 224 (FS.GG.Rendering#36) + Feature 225 (#37): fs-gg-ui-template coherent set RE-RELEASED at 0.1.56-preview.1 (release run 28478421428; tags v0.1.56-preview.1 + fs-gg-ui-template/v0.1.56-preview.1 + fs-gg-ui/v0.1.56-preview.1) — CONTENT-ONLY currency/de-leak of the shipped consumer skill docs. F224 (#36): docs/skillist-reference.md + scaffold-map.md referenced a DEFUNCT skill taxonomy (fs-gg-controls-host/typed-controls/viewer-host/design-tokens/… + fsdocs-*/fsharp-*) behind a FICTIONAL "GENERATED from the live SkillRegistry / currency-checked by TargetMetadataDrift" header (that machinery never existed) — regenerated the catalog to the live produced surface (7 product skills + 16 speckit-* command skills, each with a consumer .agents/skills/<id>/SKILL.md path), repointed scaffold-map.md prose (typed-controls/controls-host → fs-gg-ui-widgets; viewer-host → fs-gg-skiaviewer), and added a repo-owned currency gate (tests/Package.Tests/Feature224SkillCatalogCurrencyTests.fs) resolving every referenced skill-id against the existing SkillParity discovery and failing naming id+doc+line (no second enumerator, empty allowlist). F225 (#37): de-leak product-skill vocabulary + repo-owned leak guard. Both content-only (no public surface, no parameter add/remove/rename); proven live (publish-before-flip, FR-007): org feed serves 0.1.56-preview.1 for the template AND its FS.GG.UI.* siblings, template-product-tests green. No coherence flag flips true→false — a coherent advance. Consumers receive it via the FS.GG.Templates providers pin bump 0.1.55→0.1.56-preview.1. Closes FS.GG.Rendering#36 (+ #37).

- Feature 223 (FS.GG.Rendering#35): fs-gg-ui-template RE-RELEASED at 0.1.55-preview.1 (release run 28473123639; tags v0.1.55-preview.1 + fs-gg-ui-template/v0.1.55-preview.1 + fs-gg-ui/v0.1.55-preview.1) DELIVERING the fs-gg-symbology product skill to consumers (was authored-but-stranded): sourced into the ship list for the fs-gg-scene profile set (app, headless-scene, governed, sample-pack, game) on both .agents/.claude surfaces + the fs-gg-product-symbology consumer wrapper; the skill-parity blind spot is closed (a product skill is satisfied only by its fs-gg-product-* alias, not a bare framework wrapper); the Feature 219 "not-vendored" record is reversed (GV-3 stays diff-vs-today=none — an ungated source is byte-identical-neutral, verified live). Publish-before-flip proven (FR-007): coherent set @0.1.55-preview.1 on the org feed (FR-001); template-product-tests built+tested+ran the generated product green; `dotnet new --profile game/app` emits the real ~12.7 KB skill to .agents+.claude under every lifecycle. Closes #35. No coherence flag flips true→false — a coherent advance.

- Feature 222 (FS.GG.Rendering#33): fs-gg-ui-template RE-RELEASED at 0.1.54-preview.1 (release run 28468936061, 18 packages pushed; tags v0.1.54-preview.1 + fs-gg-ui-template/v0.1.54-preview.1 + fs-gg-ui/v0.1.54-preview.1) carrying the Feature 220 `game` profile — `game` is now RELEASED on the org feed (was UNRELEASED). Publish-before-flip proven live (FR-007): coherent set @0.1.54 (FR-001), content gate b78e72a ancestor-of-tag (FR-002), consumer install exit-0/no-103 (FR-003), `dotnet new --profile game` → build 0/0 → governance 26/26 ZERO GovernanceTests edits (FR-004), non-game src+tests byte-identical to the F220 baseline (FR-005). Closes #33; #31 Blocked mirror cleared; SDD#44 notified. No coherence flag flips true→false — a coherent advance.

- Feature 220 (FS.GG.Rendering#31): fs-gg-ui-template ADDS the additive `game` profile (minimal replaceable Pong starter) as the intended game/rendering DEFAULT; `app` stays the explicit controls option; headless-scene/governed/sample-pack BYTE-IDENTICAL (FR-007, diff-verified); the durable governance entrypoint assertion relaxed to family-agnostic (game -> Viewer.runApp generatedHost, satisfiable by skeleton AND Pong swap, no -- pong flag). UNRELEASED until next fs-gg-ui-template republish (DONE — RELEASED @ 0.1.54-preview.1 by Feature 222 / #33). Cross-repo: SDD#44 (enumerate + flip default app->game), Templates#36 (governance expectations), sibling FS.GG.Rendering#32. ADR FS.GG.Rendering docs/product/decisions/0010-fs-gg-ui-template-default-starter.md (Accepted).

- templates→rendering EDGE reconciled 0.1.50→0.1.53-preview.1 (owner github): the dependency-edge annotation trailed the fs-gg-ui-template contract block (already 0.1.53 since Feature 218) and the compatibility.md diagram (already 0.1.53) by three releases — predating the 0.1.52 (Feature 215) and 0.1.53 (Feature 218) coherent sets, so it misread as "Templates composes 0.1.50". Pure projection-drift fix — no surface/version/coherence-flip change. Resolves .github#67 (from FS.GG.Templates#34).

- PRIOR: Feature 218 (FS.GG.Rendering#29/#26): fs-gg-ui-template RELEASED at 0.1.53-preview.1 (carries Feature 217 --productName, no more exit-127) and the whole coherent set (template + 16 FS.GG.UI.* libs) flipped private→PUBLIC (consumer-CI installs without exit-103; internal unavailable on the free org). Both gates proven live for one V (FR-004). resolved_by → fs-gg-ui-template/v0.1.53-preview.1; closes #29 + #26.

- PRIOR: fs-gg-ui-template productName param ADDED (additive, owner rendering): Feature 217 / FS.GG.Rendering#27 wires an additive --productName symbol on the fs-gg-ui template (coalesce(productNameTrimmed ?? name) drives the Product rename; projectSlug sources the effective name; sourceName removed -> single driver; effectiveNameLower handles the lowercase `product` content form) so `dotnet new fs-gg-ui --productName Acme` (no -n) instantiates instead of exit-127, conforming to the SDD scaffold-provider convention. ADDITIVE / backward-compatible -- absent productName => byte-identical on every existing path (SC-003 real dotnet new byte-diff, M1..M4 0 diffs), --productName Acme == -n Acme (SC-004 0 diffs), Acme builds 0 warn/0 err (SC-002); empty/whitespace => falls back to -n (FR-006). Canonical param stays `name` (ADR-0005); productName is an accepted alias. UNRELEASED on the feed (lands next fs-gg-ui-template release). Cross-ref #27 <-> SDD#35. governance-cli-handoff-consumer-published coherent:false→TRUE (owner templates, consumer-side verification): the SDD→Governance enforcement loop is now proven end-to-end through the composed product. The FS.GG.Templates#25 composition `govern` stage installs the published FS.GG.Governance.Cli 1.2.0 from the org feed in CI and asserts the FULL matrix GREEN (tests/composition/run.sh → 32 passed, 0 failed: strict+failing→exit2, strict+satisfied→exit0, light+failing→exit0). Two coupled fixes closed the last (light) cell: FS.GG.Governance#34 shipped PROFILE-AWARE enforcement in 1.2.0 (route --mode gate → Verify boundary; strict tightens, light relaxes), and FS.GG.Templates#28/#29 moved the governance overlay's descriptor into the Governance-owned .fsgg/governance.yml slot (it had shipped the SDD-owned .fsgg/project.yml, so the CLI never read defaultProfile → Strict fail-safe → over-blocked every profile). Supersedes the earlier "1.1.0 strict-only baseline / light row skip-gated / 30/31 one-pending" framing on this entry. No contract surface/version change (governance-handoff@1.0.0 unchanged). Closes Templates#25 + Templates#28. #49 typed-CLI gate swap LANDED → registry-validator-typed coherent:false→true (owner github): contract-coherence.yml now validates THIS registry with the TYPED Fsgg.Registry validator (`fsgg-sdd registry validate`, FS.GG.SDD.Cli 0.2.1 pinned + installed from the org feed via setup-dotnet net10.0) instead of the Python stand-in; scripts/validate-registry.py is RETIRED (deleted) and the fsgg-contracts version-coupling assertion (registry declared `version` == actual FS.GG.Contracts package version) is kept as a dedicated gate step. Unblocked by SDD#32 (4-segment grammar fixed; CLI 0.2.1 / Contracts 1.1.1 published) — verified the typed CLI returns valid/exit 0 over THIS canonical file, matching the retired stand-in. The gate gains a FEED dependency: every caller must grant `packages: read` so the run-scoped GITHUB_TOKEN authenticates the NuGet restore (viable since the H4 provisioning blocker #21 closed) — the .github self-gate (coherence.yml) is updated; the four product-repo callers adopt the same grant as a #49 follow-up. No contract surface/version change. Resolves .github#49. fsgg-contracts 1.1.0→1.1.1 (owner sdd): SDD#32 / feature 045 RESOLVED the 4-segment grammar gap — Fsgg.Registry's private semVerRegex now accepts the major.minor.patch.revision form (e.g. governance-reference-gate-set 1.2.1.1, ADR-0007) by adding an optional `(\.\d+)?` segment, restoring byte-for-byte parity with scripts/validate-registry.py's `(?:\.\d+)?`. Verified: typed CLI and the Python authority both return valid/exit 0 over THIS canonical file. Non-breaking (private regex; no .fsi/output/exit-code change → apicompat cannot trip; no major). FS.GG.Contracts 1.1.1 + FS.GG.SDD.Cli 0.2.1 PUBLISHED to the org feed (release.yml run 28354836312; "Your package was pushed" for both). This advances version+package-version 1.1.0→1.1.1 (feed newest == SDD source == 1.1.1; contract-coherence pin assertion stays green). registry-validator-typed STAYS coherent:false pending ONLY the .github#49 gate swap — now UNBLOCKED on the SDD side: the typed validator no longer rejects the valid registry, so #49 can pin 0.2.1 and switch contract-coherence.yml from the Python stand-in to the typed CLI. #49 typed-CLI gate swap ATTEMPTED + RE-BLOCKED (owner github): wiring contract-coherence.yml onto the typed `fsgg-sdd registry validate` is now POSSIBLE (SDD#31 closed — FS.GG.SDD.Cli 0.2.0 is published + PUBLIC on the org feed; SDD#27 closed — Contracts 1.1.0 published) but on first real wiring the typed validator FAILS the valid registry: `fsgg-sdd registry validate` exits 1 with MalformedVersion on governance-reference-gate-set's 4-segment `version`/`package-version` `1.2.1.1` (ADR-0007 derives it from 4 schemaVersions), which the Python stand-in accepts (its regex permits the optional 4th segment). Fsgg.Registry's grammar does not accept 4-segment NuGet/System.Version, so SDD#26's "rule kinds mirror validate-registry.py" is INCOMPLETE. Swapping as-is would red contract-coherence on every FS-GG repo (registry valid, validator wrong), so the gate STAYS on validate-registry.py and registry-validator-typed STAYS coherent:false. Filed SDD#32 (fix 4-segment grammar + republish CLI) — .github#49 re-blocked on it. No surface/version/coherence change — gate behavior unchanged. fsgg-contracts package-version 1.0.1→1.1.0 (owner sdd): FS.GG.Contracts 1.1.0 is now LIVE on the org GitHub Packages feed (SDD#27 / feature 043, release.yml dispatch version=1.1.0, run 28334268175 "Your package was pushed"; feed serves 1.1.0 + 1.0.1), closing the release lag #42 left open. This advances `package-version` 1.0.1→1.1.0 to match the already-1.1.0 `version`, so package-version == version == feed(newest) == SDD source == 1.1.0 and the contract-coherence pin assertion is fully coherent. No surface/contract change, no coherence flip (registry-validator-typed still funnels its step (2) — switch .github#18 to the typed CLI — through .github#49; this entry resolves only step (1)'s downstream pin). Resolves .github#47. #15: register governance-reference-gate-set@1.2.1.1 (H3, epic #16 Pillar 3) — Governance publishes the validated reference .fsgg gate set as the content-only NuGet package FS.GG.Governance.ReferenceGateSet (packs the four samples/sdd-reference-gate-set/.fsgg/ files in place, byte-identical, no assembly/no deps), version DERIVED from the contained schemaVersions ({gov}.{caps}.{policy}.{tooling}=1.2.1.1; ADR-0007 Accepted), GATED on the G1–G7 guard so shipped==tested. New consumer edge templates→governance via the package; new coherence id reference-gate-set-published (coherent:false — standing request for the Templates#14 overlay drift-gate consumer; org-feed push deferred .github#21). Unblocks Templates#14. registry-validator-typed CONVERGENCE recorded (owner sdd): SDD#12 RESOLVED/CLOSED by SDD#26 (merged) — Fsgg.Registry now types + validates THIS file (RegistryDocument + validateDocument w/ rule kinds mirroring validate-registry.py + RegistryDocument.load YAML edge + `fsgg-sdd registry validate` CLI), shipped in FS.GG.Contracts 1.1.0. The "no loader / abstract model diverges" gap is CLOSED. STAYS coherent:false pending two coupled steps, both funneling through SDD#27: (1) publish 1.1.0 to the org feed (source 1.1.0, package-version still 1.0.1); (2) switch .github#18 contract-coherence.yml from the Python stand-in to the typed CLI. Prose-only registry maintenance — no coherence flip, no surface/version change. Board reconciled + epic #16 STARTED (owner github): epic FS-GG/.github#16 (Homogeneous build · contracts · auto-update fabric, multi-repo) given a Start date 2026-06-28 on the Coordination board — it was already In progress but the timeline Start field was empty (so it never anchored on the Roadmap). Board↔issue drift fixed: FS-GG/.github#22 (H4 dispatch-sender + org Renovate preset, Pillar 4) moved Done→In review — its provisioning blocker #21 (org GitHub App + Packages feed + dispatch/feed secrets) is CLOSED and both fabric halves are merged (#34/#37/#38), so Done overstated it and it is no longer blocked; the issue stays OPEN pending the FIRST green FS.GG.* Renovate sweep (the single remaining gate on coherence id cross-repo-auto-update). No registry surface/contract/version change — board-state reconciliation only. #20 RESOLVED → apicompat-publicapi-gate coherent:true (owner github): both contract-package repos ratcheted the public-API breaking-change gate advisory→ENFORCING via a dedicated api-compatibility-gate CI job (run fails on a real CP#### break) — FS.GG.Rendering over the 17 FS.GG.UI.* packables vs 0.1.52-preview.1 (Rendering#22 adopt + #25 ratchet), FS.GG.SDD over FS.GG.Contracts vs 1.0.1 (SDD#28 adopt + #29 ratchet). F#-correct (SDK Package Validation/ApiCompat, not PublicApiAnalyzers); detector fails only on genuine breaks (NoBaselineYet/Indeterminate never fail); workflow-level enforcement (no branch protection — same model as lockfile-restore-enforcement). Governance keeps its own spec-088 maturity track. #20 closed. SDD#17 RESOLVED (owner sdd): FS.GG.Contracts 1.0.1 PUBLISHED to the org GitHub Packages feed (nuget.pkg.github.com/FS-GG) via SDD release v1.0.1 (release.yml producer half, feature 039; run push step verified — "Your package was pushed"), closing the empty-feed gap. The feed is now FULLY POPULATED — FS.GG.Rendering's release (fs-gg-ui-template/v0.1.52-preview.1) also pushed FS.GG.UI.Template + the full FS.GG.UI.* set, so 19 FS.GG.* packages resolve and the feed-presence blocker is RESOLVED (supersedes the historical "feed is EMPTY" note further below). cross-repo-auto-update STAYS coherent:false for ONE remaining reason — no green FS.GG.* Renovate sweep has run yet (only third-party Renovate PRs observed); it flips coherent:true on the first. fsgg-contracts pin 1.0.1→1.1.0 (owner sdd): SDD#26 (feature 042) advanced the FS.GG.Contracts source version to 1.1.0 (additive — typed registry validator over the real dependencies.yml) but did not update this registry (contract-change protocol) or publish, so the contract-coherence gate went red (registry 1.0.1 != SDD source 1.1.0). This advances `version` to 1.1.0 to track the source authority; `package-version` stays 1.0.1 (last published — 1.1.0 not yet on the feed, release lag; SDD to publish via SDD#17). #20 coherence note (owner github): apicompat-publicapi-gate updated — mechanism correction (PublicApiAnalyzers is a Roslyn/C# analyzer, does NOT analyze F#; for the F# packables the operative detector is the SDK ApiCompat/Package Validation at the I/O edge — no new dependency in the pure leaves; .fsi + surface/*.surface.txt stay the source record) + consumer adoption recorded. Feed-status CORRECTION: the org GitHub Packages feed is now POPULATED (FS.GG.Contracts 1.0.1 + FS.GG.UI.* 0.1.52-preview.1), so baselines EXIST for the SDD/Rendering packables and the H4-feed coupling is resolved for them (only FS.GG.Governance.* stay unpublished → NoBaselineYet). FS.GG.Governance landed its advisory gate (spec 088: ReleaseRuleKind.ApiCompatibility + pack-and-apicheck.fsx over 71 IsPackable, advisory continue-on-error job); FS.GG.Rendering adopts the gate on the 17 FS.GG.UI.* packables vs the 0.1.52-preview.1 baseline. Still coherent:false (per-repo adoption + ratchet to required pending). No contract surface/version change. fsgg-contracts pin 1.0.0→1.0.1 (owner sdd): Governance-owned `capabilities` schemaVersion corrected 1→2, package republished (SDD#18 / feature 040, merged SDD#22). auto-update fabric VERIFIED end-to-end (dispatch smoke-tested; Renovate installed+resolving preset; read:packages feed-auth HTTP 200) but the org GitHub Packages feed is EMPTY — FS.GG.* was never published there so every lookup is no-result; producer publish steps now the blocker: FS.GG.Rendering#15 (release-time pack+push, MERGED) + FS.GG.SDD#17 (FS.GG.Contracts, filed); feed hostRules token moved out of the shared preset into each repo's own renovate.json (Renovate won't substitute {{secrets}} in an `extends` preset). #22: cross-repo auto-update fabric authored (H4, epic #16 Pillar 4) — reusable dispatch-sender workflow (.github/workflows/dispatch-sender.yml, workflow_call; App-token repository_dispatch, since GITHUB_TOKEN can't dispatch cross-repo) + org-shared Renovate preset (default.json, `extends: github>FS-GG/.github`) with custom managers for every embedded pin the nuget manager misses (FsGgUiVersion->FS.GG.UI.Template + annotation-driven gate-set) on the org GitHub Packages feed. New coherence id cross-repo-auto-update (coherent:false) — DORMANT/fail-closed until org-admin provisions the App + feed (#21). No contract surface/version change. #20: api-breaking-change-gate carried by shared-build-config (H3, epic #16 Pillar 5) — PublicApiAnalyzers (centrally pinned 3.3.4) + SDK Package Validation/ApiCompat wired into dist/dotnet as an OPT-IN, advisory-by-default gate (FsggApiGate); off by default so a plain re-sync stays non-breaking under locked restore (enabling it adds a PackageReference → the adopting repo regenerates its lockfile). New coherence id apicompat-publicapi-gate (coherent:false) tracks per-repo adoption on FS.GG.Contracts + FS.GG.UI.*. #18: reusable contract-coherence gate landed (H3, epic #16 Pillar 3) — .github/workflows/contract-coherence.yml (workflow_call) + scripts/validate-registry.py; validates this registry's schema, asserts the declared fsgg-contracts pin == the actual FS.GG.Contracts package version (FS.GG.SDD), and runs sync-build-config.sh --check for build-config drift; .github gained its first workflows (coherence.yml self-gate). New coherence id registry-validator-typed (coherent:false) tracks converging Fsgg.Registry onto the real registry schema (FS.GG.SDD#12). #29: shared-build-config defect fix — XML-invalid comment (`--` inside <!-- -->) in dist/dotnet/Directory.Build.props broke every adopter's build (MSB4024); reworded + sync-build-config.sh now asserts source .props well-formedness (surface unchanged, no version bump); #27: register fsgg-contracts@1.0.0 (H2, epic #16 Pillar 2 — typed coherence backbone; SDD#8); #19: ADR-0006 shared-build-config contract + lockfile-restore-enforcement gate unified to GITHUB_ACTIONS (.github source of truth); #17: ADR-0005 .fsgg slot ownership (SDD project.yml / Governance governance.yml; canonical scaffold param `name`); #14: governance-overlay-populated coherent; #7: lockfile-restore-enforcement coherent
