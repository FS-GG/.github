interface:
  display_name: "PAdd"
  short_description: "Clean, triage, and add Coordination work"
  default_prompt: "Use $p-add to reconcile and triage the board, then file this described work in its owning repository."

policy:
  allow_implicit_invocation: false
