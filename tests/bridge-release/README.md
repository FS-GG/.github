# GS2-08.7 P3 release binding

Run `python3 tests/bridge-release/run.py`. The fixture is offline and uses synthetic package archives,
source identities, attestation verification output, and signature bundles. It checks the P1/P2 release
binding, provenance barrier, partial recovery, and exact stored qualification bytes without credentials,
feeds, releases, tags, dashboards, or provider mutations.
